---
phase: 01.1-ci-feedback-loop
plan: 01
subsystem: infra
tags: [git-worktree, github-issues, gsd-planning, symlink, process, ci]

# Dependency graph
requires:
  - phase: 01-red-differential-scaffold
    provides: "`.planning/phases/FEATURES/` layout + README roster tracked on `develop` (this plan inserts the decimal-phase row into it)"
provides:
  - "Tracking issue JMSBPP/cfmm-vol-markets#58 for the inserted Phase 1.1"
  - "Git worktree at `/home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop` on branch `feat/ci-feedback-loop`, pushed to `origin` at `90dacaa`"
  - "`.planning/phases/FEATURES/feat-ci-feedback-loop` tracked on `develop` as a mode-120000 symlink into `01.1-ci-feedback-loop`"
  - "FEATURES README documents that decimal/INSERTED phases use the identical symlink convention"
affects: [01.1-02, 01.1-03, 01.1-04, 01.1-05, 01.1-06, 01-03, 01-04, 01-05]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Inserted (decimal) phases open with the same ritual as integer phases: FEATURES symlink on develop -> tracking issue -> worktree branched from the pushed develop tip"
    - "A feature branch pushed BEFORE its own workflow file exists deliberately triggers nothing — GitHub selects push-event workflows from the pushed ref"

key-files:
  created:
    - .planning/phases/FEATURES/feat-ci-feedback-loop (symlink -> ../01.1-ci-feedback-loop)
  modified:
    - .planning/phases/FEATURES/README.md

key-decisions:
  - "Branch `feat/ci-feedback-loop` created from the pushed `develop` tip 90dacaa, so the FEATURES layout for this phase is an ancestor of every Phase 1.1 code commit"
  - "The branch was pushed to `origin` in this plan even though it carries no new content — it reserves the name CI-07 names as its demonstration subject, and proves plan 03's push is the FIRST that can trigger the push build"
  - "`scripts/wt-setup.sh` was read but deliberately NOT run: its pass 2 does `git submodule update --init --recursive`, violating the milestone's no-local-build rule. Plain `git worktree add` used instead"
  - "CI-07 is NOT marked complete by this plan: it demands a push build that immediately triggers, which does not exist until plan 03/04 — this plan delivers only the branch CI-07 names as its demonstration subject"
  - "Tasks 2 and 3 produce no repo commit — their artifacts (a GitHub issue, a branch ref and a worktree) live outside the git tree and are evidenced by issue number and pushed SHA"

patterns-established:
  - "Decimal-phase FEATURES entries carry the decimal in the numbered target (`01.1-ci-feedback-loop`) and are the same tracked symlink kind as integer phases — now written into the README, not just practiced"
  - "The phase-opening push to `origin develop` bypasses the `gate` required check (a `.planning/`-only push triggers no PR workflow); this is expected and does not apply to code, which lands via PR"

requirements-completed: []  # CI-07 deliberately NOT ticked — see "Deliberately NOT done". This plan
                            # delivers only CI-07's named demonstration BRANCH; the push build that
                            # satisfies it is plans 03/04, closed out in plan 06.

# Metrics
duration: 2min
completed: 2026-08-27
---

# Phase 1.1 Plan 01: Phase Opening — FEATURES Layout, Tracking Issue, Worktree Summary

**The inserted Phase 1.1 is open per the ROADMAP's binding ritual: `.planning/phases/FEATURES/feat-ci-feedback-loop` is a tracked mode-120000 symlink on `origin/develop` (`90dacaa`), tracking issue #58 names the branch and locks the three already-taken decisions, and the `feat/ci-feedback-loop` worktree exists locally and on `origin` at the `develop` tip with all 13 submodules uninitialized and nothing built.**

## Performance

- **Duration:** 2 min
- **Started:** 2026-08-27T18:00:27Z
- **Completed:** 2026-08-27T18:02:42Z
- **Tasks:** 3
- **Files modified:** 2 (1 created symlink, 1 modified regular file)

## Accomplishments

- **The FEATURES layout now covers the inserted decimal phase, and says so.** `git ls-files -s`
  reports mode `120000` for `feat-ci-feedback-loop`, the roster carries a
  `| 1.1 (INSERTED) |` row between phases 1 and 2, and an appended paragraph states explicitly
  that decimal phases use the identical convention with the decimal in the numbered target
  directory. The README diff is `5 insertions, 0 deletions` — an edit, not a rewrite.
- **Tracking issue #58 opened** on `JMSBPP/cfmm-vol-markets`, carrying the branch, the worktree
  path, the plans directory, CI-05/CI-06/CI-07, the six success criteria as unchecked boxes, and
  the three "do not re-open" decisions (separate workflow file, no manual approval,
  `branches-ignore: [develop]`). Exactly one open issue matches the title.
- **Feature worktree live at the `develop` tip.**
  `/home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop` on `feat/ci-feedback-loop`, HEAD
  `90dacaa` == `origin/develop`, pushed with an upstream tracking ref. Plans 02, 03 and 05 have a
  worktree to write into, and CI-07's named demonstration branch exists on `origin`.
- **Nothing was built and no submodule was initialized.** All 13 submodules report the `-`
  (not-checked-out) prefix; no `out/`, `cache/` or `node_modules/` exists in the new worktree.
  `gh run list --branch feat/ci-feedback-loop` shows zero runs — correct, since the branch does
  not yet contain `push-build.yml` and `develop-gate` is PR-only.

## Task Commits

1. **Task 1: Commit the Phase 1.1 FEATURES symlink and roster row to develop** — `90dacaa`
   (docs), pushed to `origin/develop`
2. **Task 2: Open the Phase 1.1 tracking issue** — no repo commit (GitHub artifact: issue #58)
3. **Task 3: Create the feat/ci-feedback-loop worktree** — no repo commit (git worktree + branch
   `feat/ci-feedback-loop` pushed to `origin` at `90dacaa`)

**Plan metadata:** see the final `docs(01.1-01)` commit.

## Key Artifacts

| Artifact | Value |
|----------|-------|
| Tracking issue | [JMSBPP/cfmm-vol-markets#58](https://github.com/JMSBPP/cfmm-vol-markets/issues/58) |
| Feature worktree | `/home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop` |
| Feature branch | `feat/ci-feedback-loop` (tracking `origin/feat/ci-feedback-loop`) |
| `develop` SHA carrying the FEATURES roster/symlink | `90dacaa74722dcc8d16fb36f612a02ccb877135f` |
| `feat/ci-feedback-loop` SHA on `origin` | `90dacaa74722dcc8d16fb36f612a02ccb877135f` (identical — zero divergence) |
| Main/planning worktree | `/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets` (`develop`) |

## Files Created/Modified

- `.planning/phases/FEATURES/feat-ci-feedback-loop` — git-tracked symlink (mode `120000`) to
  `../01.1-ci-feedback-loop`. Existed on disk untracked before execution; this plan committed it
  by path so git recorded the link rather than following it and copying the tree.
- `.planning/phases/FEATURES/README.md` — one roster row for `1.1 (INSERTED)` plus a paragraph
  after the table stating the decimal-phase convention. Heading, symlink rationale and the V2-05
  deferral line untouched.

## Verification Performed

All verification was static (git plumbing, filesystem, `gh` API). **No local build was run** —
per the milestone's binding constraint, CI is the only build environment, and this phase exists
precisely because that constraint currently has no push-triggered feedback path.

| Criterion | Result |
|-----------|--------|
| `git ls-files -s …/feat-ci-feedback-loop` begins with `120000` | PASS |
| `readlink …/feat-ci-feedback-loop` == `../01.1-ci-feedback-loop` | PASS |
| `test -f …/FEATURES/feat-ci-feedback-loop/01.1-01-PLAN.md` | PASS |
| `grep -c '^\| 1.1 (INSERTED) \| \`feat-ci-feedback-loop\` \|'` == 1 | PASS (1) |
| README numstat vs `HEAD~1` shows ≤5 deletions | PASS (`5  0` — zero deletions) |
| `git status --porcelain .planning/phases/FEATURES` empty | PASS |
| `git log origin/develop -1 --name-only` lists README + symlink | PASS (both) |
| Pre-existing dirty entries untouched | PASS (`AGENTS.md`, `TODO.md`, `offchain`, `spec`, `DynamicFeeMod.plk`, `config.json`, `docs/superpowers/…` all still dirty/untracked) |
| Exactly one open issue titled `Phase 1.1: CI Feedback Loop (feat/ci-feedback-loop)` | PASS (#58) |
| Issue body contains `CI-0` | PASS |
| Issue body contains `feat/ci-feedback-loop`, `branches-ignore`, `push-build.yml`, `b0a9dd9` | PASS (all four) |
| Issue body `grep -c '^- \[ \]'` == 6 | PASS (6) |
| Issue body contains `do not re-open` | PASS |
| `worktree list` shows `ci-feedback-loop [feat/ci-feedback-loop]` | PASS |
| Worktree HEAD == `origin/develop` (`90dacaa`) | PASS |
| `git ls-remote --heads origin feat/ci-feedback-loop` → 1 ref | PASS |
| Worktree contains `.github/workflows/develop-gate.yml` | PASS |
| Worktree resolves `.planning/phases/FEATURES/feat-ci-feedback-loop/01.1-01-PLAN.md` | PASS |
| Worktree has no `out/`, no `cache/`, no `node_modules/` | PASS |
| `submodule status \| grep -c '^-'` == total submodule count | PASS (13 of 13) |
| `gh run list --branch feat/ci-feedback-loop` shows no run | PASS (empty) |

## Decisions Made

- **No commits for Tasks 2 and 3.** Both produce artifacts outside the git tree (a GitHub issue; a
  worktree and a branch ref). Their evidence is the issue number and the pushed SHA, recorded
  above rather than manufactured as an empty commit. Same treatment as Phase 1 plan 01.
- **`scripts/wt-setup.sh` read, not run.** Its pass 2 (`scripts/wt-setup.sh:66`) runs
  `git submodule update --init --recursive`, which would initialize all 13 submodules locally and
  break the milestone's no-local-build rule. Plain `git worktree add` was used.
- **The branch was pushed now, with no new content.** It reserves the name that CI-07's success
  criterion names as the subject of its proof, and it establishes the baseline: GitHub selects
  `push`-event workflows from the pushed ref, so a branch without `push-build.yml` triggers
  nothing. Plan 03's push is therefore the first observable trigger, by construction rather than
  by assumption.
- **Left the pre-existing dirty working tree untouched.** Every `git add` named explicit paths; no
  `git add -A` / `git add .` / `git commit -a` was used anywhere.

## Deviations from Plan

**Inside the three plan tasks: none.** Tasks 1–3 executed exactly as written and every acceptance
criterion passed on the first attempt. All deviations below are in the post-task `.planning/`
bookkeeping, and all are Rule 3 (blocking) fixes to known-defective `gsd-tools` output. No
protocol source, test, workflow or CI file was touched.

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Repositioned STATE.md to Phase 1.1 by hand instead of running `state advance-plan`**
- **Found during:** post-task state updates (not a plan task)
- **Issue:** STATE.md's discrete `Current Plan: 2` / `Total Plans in Phase: 6` fields described
  **Phase 1**, which is paused at plan 2 of 6. `state advance-plan` is phase-agnostic — it would
  have incremented that counter to `3`, silently claiming Phase 1 plan `01-03` was done. Phase 1.1
  is an inserted phase with its own independent 6-plan count.
- **Fix:** Set `Phase: 1.1 of 12`, `Plan: 1 of 6`, `Current Plan: 1`, `Total Plans in Phase: 6` by
  hand, and made the Status line state explicitly that Phase 1 is PAUSED at 2 of 6 with `01-03`
  resuming after 1.1 merges — so the paused position is not lost. `advance-plan` was not run.
- **Files modified:** `.planning/STATE.md`
- **Verification:** `sed -n '27,36p' .planning/STATE.md` shows the four discrete fields consistent
  with Phase 1.1, and the Phase 1 position is still recorded in prose.

**2. [Rule 3 - Blocking] Repaired `milestone:` corruption after EVERY state verb**
- **Found during:** post-task state updates
- **Issue:** `gsd-tools` derives the milestone version by scraping a version string out of
  STATE.md *body prose*. STATE.md's blockers section discusses Foundry `v1.8.0`, so `init`
  reported `milestone_version: v1.8` and **every** state-writing verb (`record-metric`,
  `add-decision`, `record-session`) rewrote the frontmatter key `milestone: v1.0` → `v1.8`.
  Reproduced three separate times in this plan.
- **Fix:** Re-checked `grep '^milestone:' .planning/STATE.md` after each verb and restored `v1.0`.
- **Files modified:** `.planning/STATE.md`
- **Verification:** final `grep '^milestone:' .planning/STATE.md` → `milestone: v1.0`.

**3. [Rule 3 - Blocking] Moved `record-metric`'s row into the Per-plan table**
- **Found during:** post-task state updates
- **Issue:** `state record-metric` appended `| Phase 01.1 P01 | … |` *after* the
  `*Updated after each plan completion*` line rather than into the table — the same defect Phase 1
  plan 01 recorded. It also does not maintain the Velocity or By-Phase aggregates.
- **Fix:** Moved the row under `| Phase 01 P02 |`, and updated Velocity (2→3 plans, avg 2.7 min),
  the By-Phase table (added `| Phase 1.1 | 1/6 | 2 min | 2 min |`) and Recent Trend by hand.
- **Files modified:** `.planning/STATE.md`
- **Verification:** the Per-plan table now has three contiguous rows and nothing trails the
  `*Updated after each plan completion*` line.

**4. [Rule 3 - Blocking] Moved `add-decision`'s three entries above the "Open by design" block**
- **Found during:** post-task state updates
- **Issue:** `state add-decision` appended all three `[Phase 01.1]` decisions *below* the
  `**Open by design — owned by phase planning, do not pre-resolve:**` block, filing resolved
  decisions under unresolved ones. Same defect Phase 1 plan 01 recorded.
- **Fix:** Moved all three to sit directly after the `[Phase 01]` decisions, above the
  "Open by design" block.
- **Files modified:** `.planning/STATE.md`
- **Verification:** the three `[Phase 01.1]` lines now precede the `**Open by design`  heading.

**5. [Rule 3 - Blocking] `state update-progress` reported `percent: 25` and wrote neither the frontmatter key nor the body bar**
- **Found during:** post-task state updates
- **Issue:** The documented structural defect. The command returned
  `{"updated": true, "percent": 25, "completed": 3, "total": 12, "bar": "[███░░░░░░░] 25%"}` —
  and a byte diff of STATE.md before and after showed the **only** change was the `last_updated`
  timestamp. `progress.percent` stayed `33` and the body bar stayed `33%`.
  `state record-session` additionally set `status: paused` when execution is ongoing, and left the
  stale `Next:` line pointing at Phase 1's `01-02-PLAN.md`.
- **Fix:** Set `percent: 25`, `status: executing`, the body bar to
  `[███░░░░░░░] 25%  (3 of 12 plans across the two open phases: 1 and 1.1)`, and rewrote the
  `Next:` line to point at `01.1-02-PLAN.md` in the `feat/ci-feedback-loop` worktree while
  recording where Phase 1 resumes.
- **Files modified:** `.planning/STATE.md`
- **Verification:** `diff` against a pre-command snapshot isolated the no-op; the repaired values
  are visible in the frontmatter and on line 36.

**6. [Rule 3 - Blocking] `roadmap update-plan-progress 01.1` reported success and changed nothing**
- **Found during:** post-task ROADMAP update
- **Issue:** The documented unconditional-`output({updated: true})` defect, reproduced verbatim:
  the command returned `{"updated": true, "phase": "01.1", "plan_count": 6, "summary_count": 1,
  "status": "In Progress"}` while `diff` against a pre-command snapshot of ROADMAP.md showed
  **zero** changes.
- **Fix:** Set the row `| 1.1 CI Feedback Loop (INSERTED) | 1/6 | In Progress | - |` and ticked
  the `01.1-01-PLAN.md` checkbox in the Phase 1.1 Plans list, by hand.
- **Files modified:** `.planning/ROADMAP.md`
- **Verification:** `git diff .planning/ROADMAP.md` shows exactly the two intended hunks — the
  proof the tool's return value could not provide.

### Deliberately NOT done

**`requirements mark-complete CI-07` was NOT run, and REQUIREMENTS.md is unchanged.**
This plan's frontmatter carries `requirements: [CI-07]`, and the standard bookkeeping step would
tick CI-07's box. That would be a **false claim**: CI-07 reads "A push to any branch other than
`develop` **immediately** triggers a build that initializes submodules, installs dependencies,
builds the Plank toolchain, and runs `forge build` + `forge test` — **without manual approval**".
No such build exists yet — `.github/workflows/push-build.yml` is plan 03's output and the run that
demonstrates it is plan 04's. This plan delivers only CI-07's *named demonstration subject*: the
branch `feat/ci-feedback-loop` on `origin`. CI-07 stays `[ ]` / `Pending` and is marked complete in
plan 01.1-06, after the gate run proves it. Marking it now would put the requirements ledger in
exactly the "green means nothing" state this milestone exists to prevent.

---

**Total deviations:** 6 auto-fixed (all Rule 3 - blocking), plus 1 deliberate omission.
**Impact on plan:** Zero deviations inside the executed tasks. All six fixes are repairs to
`.planning/` bookkeeping written incorrectly by `gsd-tools`; three of them (metric placement,
decision placement, `update-plan-progress` lying) reproduce defects Phase 1 plan 01 already
recorded, and the `milestone:` scrape corruption is newly confirmed as firing on *every* state
verb rather than occasionally. No scope creep.

### Note on the plan's context block

Two documented differences from the plan's *context* block (facts recorded at planning time, not
deviations in execution):

1. The plan states `develop` HEAD is `ee701f5`. By execution time it was `c3888ea` (two later
   `docs:` commits — the Phase 1.1 plan set itself, and the `update-plan-progress` root-cause
   note). Task 1 committed on top of `c3888ea`, giving `90dacaa`; the worktree branched from that
   pushed tip as the plan requires. No criterion changed.
2. The plan's Task 1 step 3 stages `.planning/phases/01.1-ci-feedback-loop/`. Those six PLAN files
   were already tracked from `c3888ea`, so that `git add` was a no-op and the commit contains only
   the two FEATURES paths. Same no-op occurred in Phase 1 plan 01 for the same reason.

## Issues Encountered

- The push to `origin develop` reported `Bypassed rule violations … Required status check "gate"
  is expected.` The branch protection rule on `develop` expects a `gate` check that a
  `.planning/`-only push does not trigger, because `develop-gate` runs on `pull_request` only.
  The push succeeded via the bypass allowance. **This is the exact gap Phase 1.1 exists to close
  for code branches** — note that it is not closed for `develop` itself and must not be: decision
  3 on issue #58 is `branches-ignore: [develop]`, keeping merges the PR gate's job.
- `gsd-tools init` reported `milestone_version: v1.8`, scraped from the `v1.8.0` mentions in
  STATE.md's blockers prose rather than read from the `milestone: v1.0` frontmatter key. The key
  itself was re-checked after every state write and still reads `v1.0`.

## User Setup Required

None — no external service configuration required.

## Next Phase Readiness

**Ready.** Plan 01.1-02 can begin immediately:

- Write all CI/YAML/script artifacts inside
  `/home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop` (branch `feat/ci-feedback-loop`),
  never in the main worktree.
- Keep planning artifacts (`PLAN`/`SUMMARY`/`EVIDENCE`/`STATE`/`ROADMAP`) in
  `/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets` on `develop`.
- Reference issue **#58** in the plan 01.1-06 PR body (`Closes #58`).
- Do not run `forge`, `make`, `npm ci` or `cabal` in the feature worktree, and do not run
  `scripts/wt-setup.sh`. All 13 submodules are intentionally uninitialized.
- The first workflow run on this branch arrives in plan 01.1-03, when the push carries
  `.github/workflows/push-build.yml` for the first time.
- `gh` in this repo requires an explicit `--repo JMSBPP/cfmm-vol-markets` (or `-R`): with both
  `origin` (fork) and `upstream` (canonical) remotes, bare `gh issue view N` does not resolve.

---
*Phase: 01.1-ci-feedback-loop*
*Completed: 2026-08-27*

## Self-Check: PASSED

All claimed artifacts verified to exist:
- `.planning/phases/FEATURES/README.md` — FOUND (roster row `1.1 (INSERTED)` present)
- `.planning/phases/FEATURES/feat-ci-feedback-loop` — FOUND (symlink, mode 120000 in index)
- `.planning/phases/01.1-ci-feedback-loop/01.1-01-SUMMARY.md` — FOUND
- `/home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop` — FOUND (worktree on `feat/ci-feedback-loop`)
- Commit `90dacaa` — FOUND (on `develop`, `origin/develop` and `origin/feat/ci-feedback-loop`)
- Issue JMSBPP/cfmm-vol-markets#58 — FOUND (OPEN)
