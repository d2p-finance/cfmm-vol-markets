---
phase: 01.1-ci-feedback-loop
plan: 02
type: execute
wave: 2
depends_on: ["01.1-01"]
files_modified:
  - .github/foundry-version
  - notes/TOOLCHAIN_PINS.md
autonomous: true
requirements: [CI-05]

must_haves:
  truths:
    - "The Foundry version CI must use is declared in a tracked, reviewable file, not only inside workflow YAML"
    - "The declared pin names both the release tag and the commit SHA, so a same-tag rebuild cannot pass unnoticed"
    - "A reader can find WHY this version is pinned, with the measured findings that depend on it, without reading any workflow"
    - "The file is directly consumable by a workflow step (shell-sourceable KEY=value), so the two workflows cannot pin different versions"
  artifacts:
    - path: ".github/foundry-version"
      provides: "The machine-readable pin: FOUNDRY_VERSION, FOUNDRY_COMMIT, FOUNDRYUP_INSTALLER_COMMIT"
      contains: "FOUNDRY_COMMIT=b0a9dd9ceda36f63e2326ce530c10e6916f4b8a2"
    - path: "notes/TOOLCHAIN_PINS.md"
      provides: "The human-readable rationale: what breaks if the pin moves, and the procedure for moving it"
      contains: "1.5.1-stable"
  key_links:
    - from: ".github/foundry-version"
      to: "notes/TOOLCHAIN_PINS.md"
      via: "in-file pointer comment in the header block"
      pattern: "notes/TOOLCHAIN_PINS.md"
---

<objective>
Record the Foundry pin **in the repository**, as a tracked file that both CI workflows read, with
its rationale written down next to the measured findings that depend on it.

Purpose: CI-05 and its success criterion 5 — "The pinned version is recorded in the repo — not
only in workflow YAML — with the reason ... Changing the pin is a visible, reviewable diff; it
cannot happen by someone running `foundryup` on the runner." A version literal buried in two
workflow files satisfies neither the single-source-of-truth half nor the reviewability half.
Output: `.github/foundry-version` (the machine-readable pin, sourced by plans 03 and 05) and
`notes/TOOLCHAIN_PINS.md` (the reason).

This plan writes **only files**. It changes no workflow, triggers no CI run, and builds nothing.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01.1-ci-feedback-loop/01.1-01-SUMMARY.md

**Work in the FEATURE worktree:** `/home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop`
(branch `feat/ci-feedback-loop`). Not MAIN.

Facts established at planning time, quoted here so the executor does not have to re-derive them:

- **The pin target is `forge 1.5.1-stable`, commit `b0a9dd9ceda36f63e2326ce530c10e6916f4b8a2`** —
  the version every recorded transport finding in `STATE.md` was measured at, and the version on
  the developer machine.
- **Foundry is currently pinned nowhere.** Verified: `foundry.toml` has no version key,
  `develop-gate.yml` has no `foundry-toolchain` action and no `foundryup` step, and there is no
  `.foundryrc` or `.tool-versions`. `foundry.toml` is not a candidate location — Foundry has no
  toolchain-version key in it.
- **The foundryup installer commit `a27902ef04dcb43061fabf343365cb5afc95fc48`** is not invented:
  it is the value `foundry-rs/foundry-toolchain@v1` itself pins in `src/index.ts`
  (`const FOUNDRYUP_INSTALLER_COMMIT = "a27902ef..."`), read from that repository at planning time.
- **`foundryup` takes `--install <version>`, not `--version`.** From foundry-toolchain@v1's
  `action.yml`: the `version` input is documented as "Passed to `foundryup --install <version>`",
  and `src/index.ts` strips a leading `v` before passing it. Plan 03 uses that exact form.
- **`forge --version` on a 1.x release prints a multi-line block** whose lines are
  `forge Version: <ver>`, `Commit SHA: <sha>`, `Build Timestamp: ...`, `Build Profile: ...` — which
  is precisely the "version, commit SHA, build timestamp" CI-06 asks a run to show. Plans 03 and 05
  nonetheless assert with a format-agnostic `grep -F "$FOUNDRY_COMMIT"` over the whole output
  rather than parsing a named line, so an output-format change is not a false red.

The measured findings that hold at this exact commit (from `STATE.md` Blockers/Concerns) are the
content of `notes/TOOLCHAIN_PINS.md`, not decoration. Quote their numbers faithfully.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write .github/foundry-version — the machine-readable pin</name>
  <files>/home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop/.github/foundry-version</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop/.github/workflows/develop-gate.yml`
      — the whole file, and specifically the `forge` job (lines 19-66). This is the job plan 05
      will teach to source this file, and its comment style is the house style being matched.
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/.planning/STATE.md` — the
      Blockers/Concerns section, for the exact `1.5.1-stable` / `b0a9dd9` strings.
    - Run `ls -a /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop/.github/` to confirm the
      directory currently holds only `workflows/`.
  </read_first>
  <action>
In the FEATURE worktree, create `.github/foundry-version` with EXACTLY this content:

    # THE Foundry toolchain both CI workflows resolve. Requirement CI-05.
    #
    # This file exists so that changing what CI compiles with is a REVIEWABLE DIFF. The
    # self-hosted `cfmm-build` runner is persistent: anyone running `foundryup` on that box
    # silently changes what every future run measures, with no commit, no PR and no trace. A
    # version literal living only inside workflow YAML would also let the push workflow and the
    # PR gate drift apart. Both workflows source THIS file, so there is exactly one pin and
    # moving it costs a commit.
    #
    # WHY THIS VERSION -- and what breaks if it moves: notes/TOOLCHAIN_PINS.md.
    #
    # Format: shell-sourceable KEY=value. Consumed by
    #   .github/workflows/push-build.yml    (unattended push build)
    #   .github/workflows/develop-gate.yml  (the PR/merge gate, forge job)
    # via `. ./.github/foundry-version`. Keep it sourceable: no spaces around `=`, no command
    # substitution, comments only on their own lines.

    # The release tag. Passed to `foundryup --install "${FOUNDRY_VERSION#v}"`.
    FOUNDRY_VERSION=v1.5.1

    # The commit the pinned release was built from. THIS is the assertion the workflows make --
    # a tag can in principle be re-cut, and "1.5.1" alone would not notice. `forge --version`
    # prints this SHA, so a run either matches it or goes red.
    FOUNDRY_COMMIT=b0a9dd9ceda36f63e2326ce530c10e6916f4b8a2

    # The foundryup INSTALLER is pinned too: an unpinned installer is an unpinned supply chain
    # for the pinned toolchain. This is the same commit foundry-rs/foundry-toolchain@v1 pins in
    # its own src/index.ts (FOUNDRYUP_INSTALLER_COMMIT).
    FOUNDRYUP_INSTALLER_COMMIT=a27902ef04dcb43061fabf343365cb5afc95fc48

(Write the lines flush-left in the file; the indentation above is only this plan's code block.)

Do NOT add a `FOUNDRY_DIR` key here — the install location is a per-workflow concern (plan 03
explains at length why it must NOT be the shared `$HOME/.foundry`), not part of the pin.

Do not commit yet. Task 2 commits both files together.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop &amp;&amp; sh -c '. ./.github/foundry-version; test "$FOUNDRY_VERSION" = v1.5.1 &amp;&amp; test "$FOUNDRY_COMMIT" = b0a9dd9ceda36f63e2326ce530c10e6916f4b8a2 &amp;&amp; test -n "$FOUNDRYUP_INSTALLER_COMMIT"' &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The file is sourceable by POSIX `sh` without error:
      `sh -c '. ./.github/foundry-version && echo "$FOUNDRY_VERSION $FOUNDRY_COMMIT"'` prints
      `v1.5.1 b0a9dd9ceda36f63e2326ce530c10e6916f4b8a2`.
    - `grep -c '^FOUNDRY_VERSION=v1\.5\.1$' .github/foundry-version` returns `1`.
    - `grep -c '^FOUNDRY_COMMIT=b0a9dd9ceda36f63e2326ce530c10e6916f4b8a2$' .github/foundry-version`
      returns `1`.
    - `grep -c '^FOUNDRYUP_INSTALLER_COMMIT=a27902ef04dcb43061fabf343365cb5afc95fc48$' .github/foundry-version`
      returns `1`.
    - `grep -q 'notes/TOOLCHAIN_PINS.md' .github/foundry-version` succeeds — the pin points at its
      own rationale.
    - `grep -cE '^[A-Z_]+=' .github/foundry-version` returns exactly `3` — no stray assignments.
    - `grep -cE '^[A-Z_]+ *= | *=.*\$\(' .github/foundry-version` returns `0` — no spaces around
      `=` and no command substitution (either would break `.` sourcing under `set -u`).
    - `git -C /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop status --porcelain .github/foundry-version`
      prints a line (the file is new and not yet committed).
  </acceptance_criteria>
  <done>`.github/foundry-version` is a three-key, POSIX-sourceable pin naming the tag, the commit
  SHA and the installer commit, and pointing at its rationale document.</done>
</task>

<task type="auto">
  <name>Task 2: Write notes/TOOLCHAIN_PINS.md and commit the pin record</name>
  <files>
    /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop/notes/TOOLCHAIN_PINS.md
    /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop/.github/foundry-version
  </files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop/notes/DATA_CONTRACT.md` — the first
      ~40 lines only, for the house tone of `notes/` documents (per `CLAUDE.md` these are binding
      spec cited from `src/*.plk`, not README prose).
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/.planning/STATE.md` — the
      Blockers/Concerns bullets quoted into section 2 below. Quote faithfully; do not paraphrase
      away the measured numbers (`b.length == 96`, the two `tokenId` coercion shapes, `45.00 s`).
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop/.github/foundry-version` — Task 1's
      file, which this document explains.
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop/CLAUDE.md` — the "`notes/` docs are
      binding spec" paragraph, which is why this rationale lives in `notes/` and not in `docs/`.
  </read_first>
  <action>
In the FEATURE worktree, create `notes/TOOLCHAIN_PINS.md` with this content:

    # Toolchain pins

    Binding. The values live in `.github/foundry-version`; this file is the reason they are
    those values and the procedure for changing them.

    ## 1. Foundry — `v1.5.1` / `b0a9dd9ceda36f63e2326ce530c10e6916f4b8a2`

    CI does not use "whatever `forge` is on the box". Both workflows install the pinned release,
    put it on `PATH`, then assert `forge --version` contains `b0a9dd9...` and fail the run if it
    does not.

    This matters more here than in a normal repo for one structural reason: **the `cfmm-build`
    runner is persistent, not a fresh container.** Its `forge` survives between runs, so a single
    `foundryup` typed on that box by anyone, ever, silently redefines what every subsequent gate
    run measures — retroactively invalidating conclusions already recorded as evidence, with no
    diff to point at. A pin turns that from an invisible event into a red run.

    ## 2. What is measured at this commit, and would have to be re-measured

    Recorded findings, not preferences. Each was measured on `forge 1.5.1-stable` `b0a9dd9` and
    each is load-bearing for the Haskell-Plank differential milestone.

    1. **`vm.rpc` result coercion is value-dependent.** The same JSON record decodes to different
       ABI types by number *magnitude*: `tokenId "0"` gives `tuple(string,string,string)`;
       `tokenId "18446744073709551616"` gives `tuple(string,string,uint256)`. A fuzz campaign hits
       both, so a decoder correct for one is intermittently wrong for the other — and would
       present as a Plank divergence. This is the finding that forces the "one even-length
       `0x`-hex string with a tag byte" wire design.
    2. **`vm.rpc` return encoding.** `bytes memory b = vm.rpc(...)` is unsafe: static-coerced
       results give a bare un-messaged `EvmError: Revert`, and object results silently return
       garbage with `b.length == 96`. Explicitly version-dependent — Foundry master encodes
       returns differently from 1.5.1.
    3. **`[rpc_endpoints]` `${...}` resolves lazily, at alias *use*.** With the variable unset,
       `forge build` exits 0 and `forge config` stores the literal `"${EVM_SPEC_BRIDGE_URL}"`;
       only a test that uses the alias fails, with a self-describing message, and the failure does
       not spread across suites. Every "the alias is safe to add before anything can answer on it"
       conclusion rests on this. Measured on 1.5.1-stable only.
    4. **Whether `try`/`catch` catches a cheatcode revert** — still OPEN, and any measurement of
       it is a claim about this binary and no other (`try`/`catch` does an `extcodesize` check
       against the cheatcode address).

    Related, same binary: a server that accepts a connection and never answers costs **45.00 s per
    call and the test PASSED**, because the call site ignored `success`. That is the founding
    failure mode of the transport work. `vm.rpc`'s retry and timeout constants are hardcoded in
    the binary (`max_retry` 8, `initial_backoff` 800 ms, `REQUEST_TIMEOUT` 45 s) and unreachable
    from config, so they are a property of the pinned version rather than of any setting.

    ## 3. Why not v1.8.0

    `vm.rpcJson` (merged 2026-06-05, PR #15076) ships only in v1.8.0, published **2026-08-27** —
    the same day this pin was written. Adopting a same-day release to obtain one convenience
    cheatcode, in exchange for re-measuring all four findings above against an encoding that is
    *known* to have changed, is not a trade this project makes. `vm.rpc` is sufficient.

    A standing rule that produced this note: **Foundry docs track `master`, not the shipped
    binary.** A researcher recommended `vm.rpcJson` at high confidence from real documentation,
    for a cheatcode absent from every binary anyone here runs. Only `cheatcodes.json` at a release
    tag is a claim about what ships.

    ## 4. Why the installer is pinned too

    `.github/foundry-version` also pins `FOUNDRYUP_INSTALLER_COMMIT`. Fetching `foundryup-init.sh`
    from a moving branch would leave the toolchain's *installer* unpinned, which makes the
    toolchain pin conditional on a third party's default branch. The pinned commit is the same one
    `foundry-rs/foundry-toolchain@v1` pins in its own `src/index.ts`.

    ## 5. Where CI installs it, and why not `~/.foundry`

    Both workflows install into `$HOME/.foundry-pins/$FOUNDRY_VERSION` and prepend that `bin` to
    the job's `PATH`. They deliberately do NOT install into `$HOME/.foundry`, which is where a
    bare `foundryup` — and where `foundry-rs/foundry-toolchain@v1`, whose `src/index.ts` sets
    `FOUNDRY_DIR = path.join(os.homedir(), ".foundry")` — would put it. On a persistent runner
    that shared directory IS the box's default `forge`, so installing there would have CI rewrite
    the machine for every other job and every human on it. A per-pin directory cannot collide,
    and `$GITHUB_PATH` is prepended, so the pinned binary wins for the job and nothing outside the
    job is mutated.

    ## 6. Changing the pin

    1. Edit `.github/foundry-version`. That diff is the change; there is no other place to edit.
    2. Re-measure every finding in section 2 against the new binary and update this file with the
       new results. A bump whose PR does not touch section 2 has not been reviewed, only merged.
    3. Expect the coercion-conformance fixture (assigned to Phase 5, RPC-03) to go red with a
       named cause. That is the third drift layer working: **the pin prevents, the version stamp
       reveals, the fixture fails.** Turning any of the three off is how a green run comes to mean
       nothing.

(Write the document flush-left; the indentation above is only this plan's code block.)

Then commit both files together, staging BY PATH:

```bash
cd /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop
git add .github/foundry-version notes/TOOLCHAIN_PINS.md
git status --porcelain --untracked-files=no
git commit -m "ci(01.1): record the Foundry pin in-repo (CI-05)"
```

**Do NOT push.** Plan 03 pushes, and that push is deliberately the first one carrying
`push-build.yml` so it is also the first that triggers it. Pushing here spends a
zero-information push and muddies the demonstration.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop &amp;&amp; git show --name-only --format= HEAD | grep -qx 'notes/TOOLCHAIN_PINS.md' &amp;&amp; git show --name-only --format= HEAD | grep -qx '.github/foundry-version' &amp;&amp; grep -q '1.5.1-stable' notes/TOOLCHAIN_PINS.md &amp;&amp; test -z "$(git status --porcelain .github notes)" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git show --name-only --format= HEAD` lists exactly two paths: `.github/foundry-version`
      and `notes/TOOLCHAIN_PINS.md` — nothing else was swept in.
    - `grep -c '^## ' notes/TOOLCHAIN_PINS.md` returns `6` (sections 1 through 6 all present).
    - `grep -q 'b0a9dd9ceda36f63e2326ce530c10e6916f4b8a2' notes/TOOLCHAIN_PINS.md` succeeds.
    - `grep -q '1.5.1-stable' notes/TOOLCHAIN_PINS.md` succeeds.
    - All four measured findings are present:
      `grep -c 'b.length == 96\|18446744073709551616\|rpc_endpoints\|extcodesize' notes/TOOLCHAIN_PINS.md`
      returns at least `4`.
    - `grep -q '45.00 s' notes/TOOLCHAIN_PINS.md` succeeds — the founding failure mode is recorded
      with its measured number, not paraphrased.
    - `grep -q 'foundry-pins' notes/TOOLCHAIN_PINS.md` succeeds — section 5 states the install
      location decision that plan 03 implements.
    - `grep -q 'the pin prevents, the version stamp reveals, the fixture fails' notes/TOOLCHAIN_PINS.md`
      succeeds (case-insensitively) — the three-layer drift model is written down.
    - `git status --porcelain .github notes` prints nothing (both files committed, clean).
    - `git log origin/feat/ci-feedback-loop..HEAD --oneline | wc -l` returns `1` — exactly one
      unpushed commit; nothing was pushed by this plan.
    - `gh run list --repo JMSBPP/cfmm-vol-markets --branch feat/ci-feedback-loop --limit 5` still
      shows NO workflow run (this plan triggered nothing).
  </acceptance_criteria>
  <done>The pin and its rationale are one local commit on `feat/ci-feedback-loop`, unpushed, with
  the four measured findings and the install-location decision written into `notes/`.</done>
</task>

</tasks>

<verification>
- `sh -c '. ./.github/foundry-version && echo $FOUNDRY_COMMIT'` prints the pinned SHA — the file
  is machine-consumable, which is what makes "both workflows resolve the same pin" structural
  rather than aspirational.
- `notes/TOOLCHAIN_PINS.md` names the version, the commit, the four findings, why not v1.8.0, the
  install-location decision, and the bump procedure.
- Exactly one commit exists on `feat/ci-feedback-loop` beyond `origin/develop`, touching exactly
  two files.
- No `forge`, `make`, `npm ci` or `cabal` was run. No CI run was triggered.
</verification>

<success_criteria>
- CI-05's "recorded in the repo — not only in workflow YAML — with the reason" half is satisfied
  by artifacts that exist and are committed. (The "workflows resolve it" half is plans 03 and 05.)
- A future pin bump is a two-file diff whose review is forced to confront the measured findings.
</success_criteria>

<output>
After completion, create
`.planning/phases/01.1-ci-feedback-loop/01.1-02-SUMMARY.md`
recording: the commit SHA on `feat/ci-feedback-loop`, the three pinned values, and confirmation
that nothing was pushed and no run was triggered.
</output>
