---
phase: 01.1-ci-feedback-loop
plan: 05
subsystem: infra
tags: [ci, github-actions, develop-gate, foundry-pin, skip-ledger, additive-proof, self-hosted-runner]

# Dependency graph
requires:
  - phase: 01.1-ci-feedback-loop
    provides: "`.github/foundry-version` (plan 01.1-02, `dddb26b`) — the single pin both workflows `.`-source"
  - phase: 01.1-ci-feedback-loop
    provides: "`.github/workflows/push-build.yml` (plan 01.1-03, `bdeecb3`) — the install/stamp step bodies copied verbatim into the gate"
  - phase: 01.1-ci-feedback-loop
    provides: "`01.1-CI-EVIDENCE.md` (plan 01.1-04, `79602e2`) — the two-run proof that the mechanism works on THIS persistent runner, which is the whole reason plan 05 runs after plan 04"
  - phase: 01.1-ci-feedback-loop
    provides: "The maintainer's approval of plan 04's blocking human-verify checkpoint — the authorisation for this plan to touch the sole required check on `develop`"
provides:
  - "`.github/workflows/develop-gate.yml` — the PR/merge gate's `forge` job now resolves the tracked pin and stamps the resolved `forge --version`, with an install/stamp body byte-identical to the push build's"
  - "A STATIC, seven-block proof that the edit is PURELY ADDITIVE: `84` lines added, `0` deleted against `origin/develop`, trigger/approval/gate-contract/ledger/seed all provably unmoved"
  - "Commit `7591034` on `feat/ci-feedback-loop` — NOT pushed; plan 06 owns the PR and the first gate run"
  - "`01.1-04-SUMMARY.md`'s checkpoint section filled with the maintainer's verbatim approval, and plan 01.1-04 marked complete"
affects: [01.1-06]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "One pin, two workflows, ONE body: the install/stamp `run:` blocks are copied byte-for-byte (inline comments included) so a `diff` of the extracted regions is empty — divergence is mechanically detectable, not a matter of reading two files carefully"
    - "The additive proof is a diff SHAPE assertion, not a review: `git diff --numstat` deleted-field == 0 and `grep '^-'` empty. A reflowed comment fails it as loudly as a deleted job."
    - "Toolchain resolution is placed BEFORE the expensive setup (submodules / plankc cargo build / npm ci) so a drifted pin reddens in seconds instead of after ~20 minutes"

key-files:
  created:
    - .planning/phases/01.1-ci-feedback-loop/01.1-05-SUMMARY.md
  modified:
    - .github/workflows/develop-gate.yml
    - .planning/phases/01.1-ci-feedback-loop/01.1-04-SUMMARY.md
    - .planning/ROADMAP.md
    - .planning/STATE.md

key-decisions:
  - "Used push-build.yml's step bodies VERBATIM — including their inline comments — rather than the plan's re-worded copies. The plan's literal text contradicted its own acceptance criterion (`diff` of the extracted regions must print nothing) and its own mandate that the two workflows must not diverge."
  - "The `--skip` ledger was NOT touched, NOT re-made and NOT reverted. The 4→3 change is `fb546e8`/`12e1fb9`'s, made outside any plan on maintainer instruction; this plan's diff baseline is the post-cleanup `origin/develop`, so the ledger correctly shows as unchanged."
  - "CI-05 and CI-06 remain UNTICKED. CI-06's sentence is 'every GATE run emits the resolved forge --version'; no develop-gate run exists yet. The file contents are necessary but not sufficient — plan 06's gate run is the evidence."
  - "The gate did NOT get a copy of `scripts/check-ci-skip-ledger.sh`. It would be harmless and it is still declined: every push already runs it, and a second copy widens the diff on the one workflow that must stay reviewable at a glance."
  - "Nothing was pushed. Plan 06 owns the PR, the gate run and the merge."

patterns-established:
  - "Verbatim-body copying between workflows, verified by an extracted-region `diff`, is the cheap enforcement of 'one mechanism, two consumers' when the shared thing cannot be factored into an action"
  - "When a plan's literal instruction conflicts with its own acceptance criterion, satisfy the criterion and the stated intent, and record the divergence"

requirements-completed: []  # CI-05 and CI-06 deliberately NOT ticked — see "Deliberately NOT done"

# Metrics
duration: 7min
completed: 2026-08-27
---

# Phase 1.1 Plan 05: Pin + Stamp in develop-gate — Summary

**`develop-gate`'s `forge` job now resolves the tracked Foundry pin and stamps the resolved
`forge --version`, using an install/stamp body byte-identical to `push-build.yml`'s, inserted before
the ~20 minutes of submodule/plankc/npm setup — and the edit is proven `84` lines added / `0` deleted
against `origin/develop`, with the PR-only trigger, the single `environment:` approval, the `gate`
job's incident-hardened contract, the three-entry `--skip` ledger and `--fuzz-seed 4880` all
demonstrably unmoved.**

## Performance

- **Duration:** ~7 min
- **Started:** 2026-08-27T20:04:59Z
- **Completed:** 2026-08-27T20:12Z
- **Tasks:** 2 of 2 (task 2 is assertion-only and produced no commit of its own)
- **Files modified:** 1 workflow + 4 planning files
- **Local builds run:** **zero.** No `forge`, `make`, `npm ci`, `cabal` or `foundryup`. Verification
  was `git`, `grep`, `diff`, `sed`, `python3`+PyYAML and the repo's own grep-only ledger guard.

## Accomplishments

- **The gate resolves the same pin the push build does, from the same file.** Both workflows now
  contain `. ./.github/foundry-version` twice (install + stamp) and neither carries a version
  literal. Moving the toolchain costs one commit to one tracked file, and it moves both consumers
  at once — which is exactly what `.github/foundry-version`'s own header says it exists to do.

- **The two step bodies are byte-identical between the workflows, and that is *mechanically*
  checked, not asserted.** Both extracted regions `diff` empty:

      diff <(sed -n '/Install the pinned Foundry toolchain/,/GITHUB_PATH/p' push-build.yml   | sed 's/^ *//') \
           <(sed -n '/Install the pinned Foundry toolchain/,/GITHUB_PATH/p' develop-gate.yml | sed 's/^ *//')
      → (install body identical)
      → (stamp body identical)

  This is the difference between "we copied it carefully" and "a divergence is detectable". The
  inline comments were copied too, precisely so the comparison has no exceptions to reason about.

- **Placement is load-bearing and was verified as an ordering assertion.** `Install the pinned
  Foundry toolchain` is at line **37**; the `forge` job's `git submodule sync --recursive` is at line
  **123**. A drifted or unavailable toolchain now reddens the gate in seconds, before the submodule
  init, the plankc `cargo build --release` and `npm ci` that plan 04 measured as the expensive part
  of the critical path. Nothing before that point needs `forge`.

- **The additive proof is a shape assertion, and it passed cleanly on the first attempt.**
  `git diff --numstat origin/develop` reports `84  0` — eighty-four insertions, **zero** deletions —
  and `git diff | grep -E '^-' | grep -v '^---'` is empty. Not one pre-existing line was removed,
  reflowed or re-indented. Every one of `develop-gate.yml`'s long comments — the `gate` job's
  skipped-jobs-green incident narrative, the submodule-recursion note, the ledger rationale — is
  bit-for-bit what it was.

- **The four non-refactorable properties of the sole required check are each proven unmoved, by
  parse rather than by eye.** `on:` parses to exactly `{'pull_request': {'branches': ['develop']}}`
  (no `push`, no `workflow_dispatch`, no `schedule`); `environment:` appears exactly once, at line
  14, inside `approve`; `gate` has no `name:` key (so the branch-protection context string is
  intact) and still declares `needs: [approve, forge, plank]`; and neither `forge` nor `plank`
  gained an `if:`, which is the specific thing that would reopen the "every heavy job skipped →
  gate green → zero tests ran" incident the `gate` job's comment records.

- **The ledger-parity guard still agrees across the two workflows:**
  `skip-ledger parity OK: 3 patterns, seed 4880`. The extracted `--skip` pattern set and the
  `--fuzz-seed` are identical between `origin/develop:<gate>` and `HEAD:<gate>` — both `diff`
  invocations print nothing. The guard's comment-stripping contract was respected: none of the
  84 added lines contains a quoted `"*…*"` glob in a comment (checked explicitly).

## Task Commits

1. **Task 1: Add the pin resolution and version stamp to develop-gate's forge job** — `7591034`
   (`ci(01.1): resolve the pinned Foundry toolchain in develop-gate too (CI-05, CI-06)`).
   `git show --name-only --format= HEAD` prints exactly `.github/workflows/develop-gate.yml` and
   nothing else. **Not pushed** — plan 06 opens the PR.
2. **Task 2: Prove the gate edit is purely additive** — no code, no commit. Seven static assertion
   blocks, output reproduced verbatim below.

## Files Created/Modified

- `.github/workflows/develop-gate.yml` — +84 / −0. Two steps inserted into the `forge` job between
  `actions/checkout@v4` and the submodule block.
- `.planning/phases/01.1-ci-feedback-loop/01.1-04-SUMMARY.md` — checkpoint section filled with the
  maintainer's verbatim approval; `status:` `AWAITING-CHECKPOINT` → `complete`.
- `.planning/phases/01.1-ci-feedback-loop/01.1-05-SUMMARY.md` — this file.
- `.planning/ROADMAP.md` — plans 04 and 05 ticked; criteria 2 and 4 annotated with what is now
  statically established and what still awaits a gate run.
- `.planning/STATE.md` — repositioned to plan 5 of 6 by hand.

## Task 2 — the seven assertion blocks, verbatim

```
== 1. purely additive: deleted-line count must be 0 ==
84	0	.github/workflows/develop-gate.yml

== 2. no removed line anywhere in the diff ==
(none — good)

== 3. trigger untouched: pull_request only, no push, targeting develop ==
on: {'pull_request': {'branches': ['develop']}}

== 4. exactly one environment reference, still on approve ==
14:    environment: develop-gate

== 5. gate job still unnamed and still needs all three ==
gate OK: ['approve', 'forge', 'plank']

== 6. skip ledger and seed identical to origin/develop ==
(ledger identical)
(seed identical)

== 7. the push build still agrees with the gate's ledger ==
skip-ledger parity OK: 3 patterns, seed 4880
```

Block 4's `14:` is inside `approve` (lines 11–17), i.e. the environment reference did not move.
Block 5's python asserts `'name' not in gate`, `gate['needs'] == ['approve','forge','plank']`,
`gate['if'] == 'always()'`, and `'if' not in forge` / `'if' not in plank`; it prints only on success.

### Task 1's acceptance checks, also verbatim

```
--- yaml parse + job set ---
jobs OK ['approve', 'forge', 'gate', 'plank']
--- numstat vs origin/develop ---
84	0	.github/workflows/develop-gate.yml
--- source-count (want 2) ---
2
--- foundry-pins present ---
yes
--- HOME/.foundry" (want 0) ---
0
--- ordering ---
37:      - name: Install the pinned Foundry toolchain
123:          git submodule sync --recursive
163:      - run: git submodule sync --recursive
--- which -a forge (want 1) ---
1
--- GITHUB_STEP_SUMMARY (want 1) ---
1
--- body diff vs push-build ---
(install body identical)
(stamp body identical)
```

(Line 163's `git submodule sync --recursive` is the **`plank`** job's, which this plan did not touch;
the `forge` job's is line 123. Both are after line 37.)

## Deviations from Plan

### Auto-fixed / handled

**1. [Rule 1 — Bug] The plan's literal step text contradicted its own acceptance criterion; the criterion and the stated intent won**

- **Found during:** Task 1
- **Issue:** The plan's `<action>` block gives *re-worded* inline comments inside the two `run:`
  bodies (e.g. `# NOT $HOME/.foundry: on this persistent runner…` vs push-build's
  `# NOT $HOME/.foundry. On this PERSISTENT runner…`). But the same task's acceptance criteria
  require `diff <(sed -n '/Install the pinned Foundry toolchain/,/GITHUB_PATH/p' push-build.yml …)
  <(… develop-gate.yml …)` to print **nothing**, and the plan's own `<read_first>` says "Copy their
  `run:` bodies verbatim; the two workflows must not diverge." Typing the plan's re-worded text
  would have failed the criterion and, more importantly, created the exact divergence the single
  pin file exists to prevent.
- **Fix:** Extracted `push-build.yml` lines 74–125 (install) and 133–151 (stamp) programmatically
  and spliced them **unmodified** — indentation is already 6 spaces in both files, so no
  re-indentation was needed or performed. The plan's distinctive *header* comments (the ones ABOVE
  each `- name:`, carrying the gate-specific rationale about placement before the expensive setup)
  were kept, since they sit outside the compared region and are the part that genuinely differs
  between the two workflows.
- **Files modified:** `.github/workflows/develop-gate.yml`
- **Commit:** `7591034`

**2. [Rule 1 — Bug] The plan's stamp-step header comment would have broken its own `grep -c 'which -a forge'` == 1 criterion**

- **Found during:** Task 1
- **Issue:** The plan's header comment reads "`which -a forge` records the collision with the
  runner's pre-existing forge", and the copied body contains `which -a forge || true`. That is two
  occurrences; the acceptance criterion demands one. (`push-build.yml` itself has this collision —
  it contains the string twice — so the criterion was written against an imagined file.)
- **Fix:** The header comment says "`which -a` records the collision with the runner's pre-existing
  forge". Same meaning, criterion satisfied at `1`.
- **Files modified:** `.github/workflows/develop-gate.yml`
- **Commit:** `7591034`

### Recorded, not fixed — the plan's stale ledger expectation

**Task 2's acceptance criterion says `scripts/check-ci-skip-ledger.sh` should print
`skip-ledger parity OK: 4 patterns, seed 4880`. It prints `3 patterns, seed 4880`, and that is
correct.** The `*PriceSetterHook*` entry was retired between the plan being written and this plan
running, by `fb546e8` on `feat/ci-feedback-loop` and the identical `12e1fb9` on `develop` — both
outside any plan, on direct maintainer instruction. **This plan did not make that change, does not
claim it, and did not revert it.** The seed is unchanged at 4880, which is the half of the criterion
that is actually about this plan.

The consequence the plan could not anticipate, and which plan 04's Summary flagged in advance: the
"purely additive" proof had to be diffed against the **post-cleanup** state. It was —
`origin/develop` is `12e1fb9`, which already carries the three-entry ledger, and
`git diff origin/develop -- .github/workflows/develop-gate.yml` was **empty** before Task 1's edit
(verified). So the `84 / 0` numstat measures this plan's insertion and nothing else. Diffing against
the pre-phase state would have attributed someone else's ledger edit to this plan.

### Deliberately NOT done

**`requirements mark-complete` was NOT run. `REQUIREMENTS.md` is unchanged; CI-05 and CI-06 both
remain `Pending`** — even though this plan's `requirements:` frontmatter lists them.

- **CI-06** says "Every **gate** run emits the resolved `forge --version`". There has never been a
  `develop-gate` run on this branch (`gh run list --workflow develop-gate.yml --branch
  feat/ci-feedback-loop` was empty at plan 04, and nothing was pushed since). A workflow file that
  *would* stamp is not a run that *did*. Judged on the requirement's own sentence, this is not
  satisfied.
- **CI-05** says both workflows resolve an explicit, declared version rather than the ambient one.
  The *file contents* now say so for both. The *resolution* — that on this persistent runner the
  gate's job actually gets `b0a9dd9…` and not the box's `/home/jmsbpp/.foundry/bin/forge` — is
  proven for the push path only. It is highly likely to transfer (same runner label, byte-identical
  body, and plan 04 measured the prepend beating the system forge twice), but "highly likely" is not
  the standard plans 01–04 held.

Both close at plan **06**, whose PR produces the first `develop-gate` run. This keeps the standard
set by plan 01 (refused CI-07), plan 02 (refused CI-05), plan 03 (refused both) and plan 04 (refused
CI-07 despite its sentence being factually true).

**The ledger-parity guard was NOT added to `develop-gate`.** Deliberate, per the plan's own context:
drift arrives via an edit, an edit arrives via a push, and every push already runs the guard. A
second copy buys nothing and widens the diff on the workflow that must stay reviewable at a glance.

**The `plank` job was NOT touched.** It never invokes `forge`; installing a toolchain there would be
dead weight on every PR's critical path.

**Nothing was pushed**, and `state advance-plan` / `state update-progress` /
`roadmap update-plan-progress` were not used — see below.

## Checkpoint bookkeeping (plan 04)

Plan 04's blocking `checkpoint:human-verify` was **APPROVED**. The maintainer's verbatim response:

> remove the PriceSetterHook skip from develop since is no longer there but do not create a worktree
> for such fix but make it under the current tree. Ther approve

`01.1-04-SUMMARY.md`'s `## Checkpoint (Task 2)` section — deliberately left unfilled by that plan's
agent — now carries this response plus three judgements:

1. **The unattended-execution tradeoff is ACCEPTED, unmodified.** No change to `push-build.yml` was
   asked for; it ships with no `environment:`, no secrets, no approval, exactly as measured.
2. **The one requested change was already done.** "remove the PriceSetterHook skip from develop" was
   `12e1fb9`, applied on `develop` in the `vol-markets` worktree — honouring "do not create a
   worktree for such fix but make it under the current tree".
3. **The `timeout-minutes: 30` question was NOT answered and is not treated as answered.** The
   checkpoint asked about cold-run headroom; the response is silent on it. The evidence file's
   position stands verbatim: 30 minutes is comfortable *as observed* (3 min 11 s) and **unproven
   cold**, because submodules, the plankc cargo build and the forge compile cache were warm in both
   runs. Only the pin install was measured cold, at 19 s. Silence is not assent.

Plan 04 is now `status: complete` with its metrics filled and its ROADMAP line ticked.

## Verification Performed

All static. **No `forge`, `make`, `npm ci`, `cabal` or `foundryup` was executed.**

| Criterion (from the plan) | Result |
|---|---|
| `develop-gate.yml` parses as YAML | PASS (PyYAML 6.0.3) |
| Job set unchanged: `['approve','forge','gate','plank']` | PASS |
| `grep -c '\. \./\.github/foundry-version'` == 2 | PASS |
| `grep -q 'foundry-pins'` succeeds; `grep -c 'HOME/\.foundry"'` == 0 | PASS / PASS (`0`) |
| Install step precedes `git submodule sync --recursive` | PASS (37 < 123) |
| `grep -c 'which -a forge'` == 1; `grep -c 'GITHUB_STEP_SUMMARY'` == 1 | PASS / PASS |
| Install + stamp bodies identical to push-build's | PASS (both `diff`s empty) |
| Exactly one commit, touching only that file | PASS (`7591034`) |
| Deleted-line count vs `origin/develop` == 0 | PASS (`84  0`) |
| No `^-` line in the diff | PASS (empty) |
| `on:` is exactly `{'pull_request': {'branches': ['develop']}}` | PASS |
| `grep -c 'environment:'` == 1, inside `approve` | PASS (line 14) |
| `gate` has no `name:`, `needs` unchanged, no `if:` on `forge`/`plank` | PASS |
| `--skip` set and `--fuzz-seed` identical to `origin/develop` | PASS (both `diff`s empty) |
| `bash scripts/check-ci-skip-ledger.sh` exits 0 | PASS — prints `3 patterns` (plan said `4`; see deviations) |
| No comment among the added lines carries a quoted `"*…*"` glob | PASS (guard's contract respected) |
| Nothing pushed | PASS (`7591034` is local to `feat/ci-feedback-loop`) |
| Protected files untouched | PASS — `AGENTS.md`, `TODO.md`, `offchain`, `spec`, `.planning/config.json`, `src/modules/premium/DynamicFeeMod.plk`, `docs/superpowers/specs/2026-08-26-*.md` all exactly as found |
| `milestone:` is `v1.0` | PASS (checked before and after every STATE.md write) |
| ROADMAP change proven by `git diff` | PASS |

## Issues Encountered

- **The plan's own acceptance criteria contained two internal contradictions** (the re-worded step
  bodies vs. the byte-identity `diff`; the header comment vs. the `which -a forge` count) and one
  stale fact (`4 patterns`). All three are recorded above rather than silently smoothed. None
  required a checkpoint: each had an unambiguously correct resolution given the plan's stated
  intent.

- **`git diff --numstat` is the whole additive proof, and it is only as good as its baseline.** It
  was empty *before* Task 1, which is what makes `84 / 0` attributable. Anyone re-running this proof
  after `origin/develop` moves must re-establish that precondition first.

- **Assertion 4 (`grep -n 'environment:'`) is weaker than it reads.** It proves the string occurs
  once and reports its line number; that the line is *inside* `approve` is established by reading
  the job boundaries (11–17), not by the grep. The PyYAML parse in block 5 covers the `gate` job's
  contract properly; there is no equivalent structural assertion that `approve` still owns the
  environment. Recorded as a limitation of the plan's assertion set, not a finding.

- **The 30-minute cold-run headroom remains unmeasured** and is now *also* a question about the
  gate's `timeout-minutes: 25`, which is tighter. Plan 06's gate run will produce the first data
  point on the gate path — though on this persistent runner it will very likely be warm again, so
  the cold bound may simply stay open.

- **Issue #16's items 2 and 3 are still untracked loose ends** (the seed-dependent width-type fuzz
  bug behind `*VolRangeWidth*`/`*SpreadTickAssimetryHelper*`, and the gamsdiff `.gms.gms` runner
  environment). Carried forward from plan 04; still no `.planning/` artifact. Not this plan's scope.

## User Setup Required

None.

## Next Phase Readiness

**Plan 06 is unblocked.** It opens the PR from `feat/ci-feedback-loop` → `develop`, which triggers
the first `develop-gate` run in this phase, and that run is the evidence CI-05 and CI-06 have been
waiting on. What it inherits:

- `feat/ci-feedback-loop` is at `7591034`, **4 commits ahead of `origin/develop`** (`dddb26b`,
  `bdeecb3`, `fb546e8`, `7591034`) and **not pushed**. Plan 06 pushes.
- The gate will require an `environment: develop-gate` approval before any heavy job runs — plan 06
  should expect `pending_deployments` to be non-empty, unlike every push-build run so far. That is
  the design, not a fault.
- The install step should be a **0-second cache hit** on the gate too: the stamp
  `$HOME/.foundry-pins/v1.5.1/.installed-b0a9dd9…` already exists on the runner from plan 03's run,
  and it is keyed by pin, not by workflow. The *stamp* step is where the pin gets re-asserted, so a
  silent 0-second install step is correct behaviour and must not be read as a step that did not run.
- Expected gate `forge test` result, from the push path: `271 passed, 0 failed, 1 skipped` across 74
  suites, with `VolOrderToPanopticTokenId.t.sol` at 10/10. The gate runs `forge test` only (no bare
  `forge build`), so it measures a subset of what the push build measured.
- Both workflows carry `3 patterns, seed 4880`. If plan 06 changes either, the push build's parity
  guard fails first.
- **Still no local builds.** All submodules remain uninitialized; CI is the only build environment.

---
*Phase: 01.1-ci-feedback-loop*
*Status: COMPLETE — 2026-08-27*

## Self-Check: PASSED

Every artifact and commit claimed above verified to exist:

- `.github/workflows/develop-gate.yml` (in the `ci-feedback-loop` worktree) — FOUND, parses as YAML,
  four jobs, `+84 / −0` vs `origin/develop`
- `.planning/phases/01.1-ci-feedback-loop/01.1-05-SUMMARY.md` — FOUND (this file)
- `.planning/phases/01.1-ci-feedback-loop/01.1-04-SUMMARY.md` — FOUND, checkpoint section filled with
  the maintainer's verbatim approval, `status: complete`
- `.planning/ROADMAP.md` — FOUND, modified, change proven by `git diff` (`32` insertions / `8`
  deletions): plans 04 and 05 ticked, criteria 2 and 4 annotated, unattended-tradeoff open question
  resolved, progress row `3/6 (04 at checkpoint)` → `5/6`
- `.planning/STATE.md` — FOUND, modified by hand, `milestone: v1.0` verified **before and after** the
  write, `current_plan: 5`, `status: executing`, Phase-1-PAUSED-at-2-of-6 record preserved verbatim
- `.planning/REQUIREMENTS.md` — verified **UNCHANGED** (`git diff --quiet` clean); CI-05, CI-06 and
  CI-07 all still `Pending`, as intended
- Commit `7591034` — FOUND on `feat/ci-feedback-loop`, touching only
  `.github/workflows/develop-gate.yml`
- Commits `fb546e8`, `bdeecb3`, `dddb26b`, `79602e2`, `12e1fb9` — all FOUND
- No gsd-tools state verb was invoked: `state advance-plan`, `state update-progress`,
  `state record-metric`, `state add-decision`, `state record-session` and
  `roadmap update-plan-progress` were **all** skipped, per the standing constraints (phase-agnostic
  counter while Phase 1 is independently paused; `milestone:` corruption; the
  `update-plan-progress` / `update-progress` unconditional-success defect). Every STATE.md and
  ROADMAP.md change was hand-written and proven with `git diff`.
- Protected files untouched: `AGENTS.md`, `TODO.md`, `offchain`, `spec`, `.planning/config.json`,
  `src/modules/premium/DynamicFeeMod.plk`, `docs/superpowers/specs/2026-08-26-*.md` all still show
  exactly their pre-existing `git status` entries
