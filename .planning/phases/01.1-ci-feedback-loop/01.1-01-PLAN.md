---
phase: 01.1-ci-feedback-loop
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/FEATURES/README.md
  - .planning/phases/FEATURES/feat-ci-feedback-loop
autonomous: true
requirements: [CI-07]

must_haves:
  truths:
    - "A tracking issue for Phase 1.1 exists on JMSBPP/cfmm-vol-markets and names the branch feat/ci-feedback-loop"
    - "A git worktree for branch feat/ci-feedback-loop exists on disk, branched from the develop tip, and the branch exists on origin"
    - "The path .planning/phases/FEATURES/feat-ci-feedback-loop/ resolves to this phase's PLAN files on develop"
    - "No submodule was initialized and no build artifact was produced in the new worktree"
  artifacts:
    - path: ".planning/phases/FEATURES/feat-ci-feedback-loop"
      provides: "Symlink making the FEATURES layout path real on develop for the inserted phase"
    - path: ".planning/phases/FEATURES/README.md"
      provides: "Roster row for the inserted Phase 1.1"
      contains: "feat-ci-feedback-loop"
  key_links:
    - from: ".planning/phases/FEATURES/feat-ci-feedback-loop"
      to: ".planning/phases/01.1-ci-feedback-loop"
      via: "git-tracked symlink (mode 120000)"
      pattern: "01.1-ci-feedback-loop"
---

<objective>
Open Phase 1.1 the way the ROADMAP's binding conventions require every phase to open: a tracking
issue on the JMSBPP fork, a dedicated git worktree for `feat/ci-feedback-loop`, and the
`.planning/phases/FEATURES/feat-ci-feedback-loop` path committed to `develop`.

Purpose: CI-07's success criterion is worded "Demonstrated by an actual run on
`feat/ci-feedback-loop`" — the branch is not incidental, it is the named subject of the proof.
Plans 02 and 03 write into the worktree this plan creates, and plan 03's first push to this exact
branch is what makes the push build observable at all.
Output: a tracking issue, a worktree at `/home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop`
on branch `feat/ci-feedback-loop` pushed to `origin`, and a FEATURES roster/symlink on `develop`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md

Two worktrees are in play for the whole of Phase 1.1. Never confuse them:

| Role | Absolute path | Branch |
|------|---------------|--------|
| MAIN — planning artifacts (PLAN/SUMMARY/EVIDENCE), `.planning/` commits | `/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets` | `develop` |
| FEATURE — all CI/YAML/script artifacts for this phase | `/home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop` | `feat/ci-feedback-loop` |

Verified facts (checked at planning time, in MAIN):
- `origin` = `https://github.com/JMSBPP/cfmm-vol-markets.git` (the JMSBPP fork — everything lands
  here). `upstream` = `https://github.com/d2p-finance/cfmm-vol-markets.git` (canonical, PR-only).
  This phase pushes to `origin` only.
- `gh` is authenticated as `JMSBPP`.
- Phase 1's tracking issue is `#57`. There is no Phase 1.1 issue yet.
- `develop` HEAD is `ee701f5` ("docs: Phase 1.1 becomes CI Feedback Loop …").
- `.planning/phases/FEATURES/feat-ci-feedback-loop` ALREADY EXISTS ON DISK as an untracked symlink
  to `../01.1-ci-feedback-loop` (`git status --porcelain .planning/phases/` prints exactly
  `?? .planning/phases/FEATURES/feat-ci-feedback-loop`). This plan commits it; it does not
  recreate it.
- `.planning/phases/FEATURES/README.md` is tracked (blob `51f8135`) and its Roster table lists
  phases 1–11 but NOT the inserted 1.1.
- Phase 1's `feat/red-diff-scaffold` worktree lives at
  `/home/jmsbpp/cfmms-playground/cfmm-wt/red-diff-scaffold` — the `cfmm-wt/<slug>` naming pattern
  this plan follows.

Pre-existing dirty working-tree entries in MAIN that this plan MUST NOT touch or stage:
`AGENTS.md`, `offchain`, `spec`, `src/modules/premium/DynamicFeeMod.plk`, `TODO.md`,
`docs/superpowers/specs/2026-08-26-*.md`, `.planning/config.json`.
Never use `git add -A` or `git commit -a` anywhere in this phase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the Phase 1.1 FEATURES symlink and roster row to develop</name>
  <files>
    /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/.planning/phases/FEATURES/README.md
    /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/.planning/phases/FEATURES/feat-ci-feedback-loop
  </files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/.planning/phases/FEATURES/README.md` —
      the whole file. The Roster table at the bottom is the only part being edited.
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/.planning/ROADMAP.md` — the "Conventions
      (binding for every phase)" section (phase directory layout) and the "Phase Numbering" note
      that decimal phases are INSERTED phases.
    - Run `ls -la /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/.planning/phases/FEATURES/`
      and `readlink .planning/phases/FEATURES/feat-ci-feedback-loop` BEFORE touching anything —
      the symlink already exists on disk and must not be recreated as a directory copy.
  </read_first>
  <action>
Work in the MAIN worktree `/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets`, on `develop`.

1. Confirm the on-disk symlink is correct (do not recreate it if it is):

```bash
cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets
readlink .planning/phases/FEATURES/feat-ci-feedback-loop
# must print exactly: ../01.1-ci-feedback-loop
```

If — and only if — `readlink` disagrees or prints nothing:

```bash
rm -f .planning/phases/FEATURES/feat-ci-feedback-loop
ln -s ../01.1-ci-feedback-loop .planning/phases/FEATURES/feat-ci-feedback-loop
```

2. Edit `.planning/phases/FEATURES/README.md`. Insert ONE row into the Roster table, between the
   `| 1 |` row and the `| 2 |` row, so the table reads:

```markdown
| Phase | FEATURES path | Branch |
|-------|---------------|--------|
| 1 | `feat-red-diff-scaffold` | `feat/red-diff-scaffold` |
| 1.1 (INSERTED) | `feat-ci-feedback-loop` | `feat/ci-feedback-loop` |
| 2 | `feat-volorder-t-minimal` | `feat/volorder-t-minimal` |
```

   Then append this paragraph immediately after the Roster table (it explains the one thing a
   reader will otherwise get wrong about a decimal phase directory):

```markdown
Inserted phases (decimal numbers, per the ROADMAP's "Phase Numbering") follow the identical
convention: the numbered tool-facing directory carries the decimal (`01.1-ci-feedback-loop`) and
the FEATURES entry is the same kind of tracked symlink into it.
```

   Change nothing else in the file — not the heading, not the convention block, not the symlink
   rationale, not the V2-05 deferral line.

3. Stage the symlink BY PATH (never `git add -A`) so git records mode `120000` instead of
   following the link and copying the directory, and commit:

```bash
cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets
git add .planning/phases/FEATURES/README.md .planning/phases/FEATURES/feat-ci-feedback-loop
git add .planning/phases/01.1-ci-feedback-loop/
git status --porcelain --untracked-files=no   # eyeball: ONLY the three paths above are staged
git commit -m "docs(01.1): open CI Feedback Loop phase — FEATURES layout for the inserted phase"
git push origin develop
```

Do NOT stage `AGENTS.md`, `offchain`, `spec`, `src/modules/premium/DynamicFeeMod.plk`, `TODO.md`,
`docs/superpowers/specs/2026-08-26-*.md` or `.planning/config.json`.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets &amp;&amp; git ls-files -s .planning/phases/FEATURES/feat-ci-feedback-loop | grep -q '^120000' &amp;&amp; test -f .planning/phases/FEATURES/feat-ci-feedback-loop/01.1-01-PLAN.md &amp;&amp; grep -q 'feat-ci-feedback-loop' .planning/phases/FEATURES/README.md &amp;&amp; git status --porcelain .planning/phases/FEATURES | grep -q . &amp;&amp; exit 1 || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files -s .planning/phases/FEATURES/feat-ci-feedback-loop` prints a line beginning
      with `120000` (tracked as a symlink, not a copied tree).
    - `readlink .planning/phases/FEATURES/feat-ci-feedback-loop` prints exactly
      `../01.1-ci-feedback-loop`.
    - `test -f .planning/phases/FEATURES/feat-ci-feedback-loop/01.1-01-PLAN.md` succeeds — the
      FEATURES path resolves to this phase's real plan files.
    - `grep -c '^| 1.1 (INSERTED) | `feat-ci-feedback-loop` |' .planning/phases/FEATURES/README.md`
      returns `1`.
    - `git diff HEAD~1 --numstat -- .planning/phases/FEATURES/README.md` shows at most `5` deleted
      lines (a one-row insert plus the appended paragraph — a larger deletion count means the file
      was rewritten rather than edited).
    - `git status --porcelain .planning/phases/FEATURES` prints nothing.
    - `git log origin/develop -1 --name-only` lists both
      `.planning/phases/FEATURES/README.md` and `.planning/phases/FEATURES/feat-ci-feedback-loop`.
    - `git status --porcelain` still shows `AGENTS.md`, `TODO.md`, `offchain`, `spec` and
      `.planning/config.json` as dirty/untouched (nothing unrelated was swept into the commit).
  </acceptance_criteria>
  <done>`.planning/phases/FEATURES/feat-ci-feedback-loop` is a tracked mode-120000 symlink on
  `origin/develop` that resolves to this phase's plans, and the README roster names the inserted
  phase.</done>
</task>

<task type="auto">
  <name>Task 2: Open the Phase 1.1 tracking issue</name>
  <files>(no repo files — a GitHub issue on JMSBPP/cfmm-vol-markets)</files>
  <read_first>
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/.planning/ROADMAP.md` — the
      "Phase 1.1: CI Feedback Loop (INSERTED)" section in full. Its Goal, the "Why this was
      inserted" (a) and (b) paragraphs, and its six Success Criteria are quoted into the issue
      body and must match.
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/.planning/REQUIREMENTS.md` — the exact
      wording of CI-05, CI-06, CI-07 (lines 67–69).
    - Run `gh issue list --repo JMSBPP/cfmm-vol-markets --state all --limit 20` first — do not
      open a duplicate. Issue `#57` is Phase 1's; there should be no Phase 1.1 issue.
  </read_first>
  <action>
If `gh issue list --repo JMSBPP/cfmm-vol-markets --state all --search "CI Feedback Loop"` already
returns a matching issue, record its number and SKIP creation.

Otherwise create it. Write the body to a temp file first (it contains backticks and newlines that
do not survive inline quoting cleanly):

```bash
cat > /tmp/phase-1-1-issue.md <<'MARKER'
Tracking issue for **Phase 1.1: CI Feedback Loop (INSERTED)** of the Haskell↔Plank Differential
Conformance milestone.

- **Branch:** `feat/ci-feedback-loop`
- **Worktree:** `/home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop`
- **Plans:** `.planning/phases/FEATURES/feat-ci-feedback-loop/`
- **Requirements:** CI-05, CI-06, CI-07
- **Runs BEFORE** Phase 1 wave 3 (#57), so Phase 1's remaining waves get compile feedback on
  every push instead of discovering breakage at the wave-5 PR.

## Goal

Close the hole in the "CI is the only build environment" doctrine — make every push compile
immediately and unattended, and make every result attributable to a known toolchain.

## Why this was inserted

**(a) The doctrine had no feedback path.** `develop-gate` is `on: pull_request` only —
deliberately, per its own comment. Combined with dependencies being intentionally uninitialized
locally, code can be written, committed and pushed with **zero compile feedback** until someone
opens a PR. `SpecHelper.sol` is on the remote right now and nothing has ever compiled it.

**(b) Foundry is pinned nowhere** — no version key in `foundry.toml`, no `foundry-toolchain`
action or `foundryup` step, no `.foundryrc`/`.tool-versions`. The self-hosted runner is
*persistent*, so its `forge` drifts silently on any `foundryup`. Four recorded findings rest on
`1.5.1-stable` `b0a9dd9` specifically: the `json_value_to_token` coercion behaviour, the `vm.rpc`
return encoding, `${...}` alias resolution being lazy, and `try`/`catch` catching a cheatcode
revert. v1.8.0 (published 2026-08-27) encodes returns differently.

## Success criteria

- [ ] A push to a non-`develop` branch triggers a build with **no manual approval**, which
      initializes submodules, builds the Plank toolchain, runs `npm ci`, and runs `forge build` +
      `forge test`. Demonstrated by an actual run on `feat/ci-feedback-loop`.
- [ ] That workflow is **separate** from `develop-gate`, which keeps its `environment` approval
      and its role as the PR/merge authority — no double-approval, and no push path that can
      approve a merge.
- [ ] Pushes to `develop` do **not** trigger the push build.
- [ ] Both workflows resolve an explicit, declared Foundry version rather than the ambient one,
      and each run's log shows the resolved `forge --version` including version and commit SHA.
- [ ] The pinned version is recorded in the repo — not only in workflow YAML — with the reason,
      citing that the transport findings hold at `1.5.1-stable` `b0a9dd9`.
- [ ] The existing suite is unaffected: a run still reports `VolOrderToPanopticTokenId.t.sol`
      green and the `--skip` ledger line is untouched.

## Decisions already taken (do not re-open)

1. A **separate** workflow file (`.github/workflows/push-build.yml`), NOT a push trigger added to
   `develop-gate.yml`.
2. **No manual approval** on the push build — it runs immediately and unattended. Approval stays
   on the PR path only. Accepted tradeoff: pushed code runs on the self-hosted runner without a
   human in the loop, so the push workflow is given no secrets it does not need.
3. Triggers on all branches EXCEPT `develop` (`branches-ignore: [develop]`).

## Not in this phase

No change to `develop-gate`'s triggers, its `environment` approval, or its `--skip` ledger line.
No new tests. No spec/oracle work.
MARKER

gh issue create --repo JMSBPP/cfmm-vol-markets \
  --title "Phase 1.1: CI Feedback Loop (feat/ci-feedback-loop)" \
  --body-file /tmp/phase-1-1-issue.md
```

Record the resulting issue number — plan 06's PR body references it with `Closes #<N>`. Put the
number in this plan's SUMMARY.
  </action>
  <verify>
    <automated>gh issue list --repo JMSBPP/cfmm-vol-markets --state open --json number,title -q '.[].title' | grep -cx 'Phase 1.1: CI Feedback Loop (feat/ci-feedback-loop)' | grep -qx 1 &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `gh issue list --repo JMSBPP/cfmm-vol-markets --state open --json title -q '.[].title'`
      contains exactly ONE line equal to
      `Phase 1.1: CI Feedback Loop (feat/ci-feedback-loop)` (no duplicate).
    - `gh issue view <N> --repo JMSBPP/cfmm-vol-markets --json body -q .body | grep -c 'CI-0'`
      returns at least `1` — the requirement IDs CI-05/CI-06/CI-07 are in the body.
    - The body contains the literal strings `feat/ci-feedback-loop`, `branches-ignore`,
      `push-build.yml` and `b0a9dd9`.
    - The body's "Success criteria" section has exactly `6` unchecked boxes:
      `gh issue view <N> --json body -q .body | grep -c '^- \[ \]'` returns `6`.
    - The body's "Decisions already taken" section is present:
      `gh issue view <N> --json body -q .body | grep -q 'do not re-open'` succeeds.
  </acceptance_criteria>
  <done>Exactly one open tracking issue on the JMSBPP fork names the branch, the plans directory,
  the three requirement IDs, the six success criteria, and the three locked decisions.</done>
</task>

<task type="auto">
  <name>Task 3: Create the feat/ci-feedback-loop worktree</name>
  <files>(no repo files — creates the worktree at /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop)</files>
  <read_first>
    - Run `git -C /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets worktree list` — confirms the
      `cfmm-wt/<slug>` layout and that `feat/ci-feedback-loop` is not already checked out.
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/scripts/wt-setup.sh` — READ IT SO YOU DO
      NOT RUN IT. It is peers.tsv-driven and its pass 2 does
      `git submodule update --init --recursive`, which violates this milestone's
      no-local-build/uninitialized-submodules rule. Use plain `git worktree add` instead.
    - `/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/.planning/ROADMAP.md` — "Conventions",
      the "Worktree + issue per phase" bullet and the "CI is the validation gate — and the only
      build environment" bullet.
  </read_first>
  <action>
Branch from the `develop` commit Task 1 pushed, so the FEATURES layout for this phase is an
ancestor of the feature branch.

```bash
cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets
git fetch origin develop
git worktree add /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop \
  -b feat/ci-feedback-loop develop
git -C /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop \
  push -u origin feat/ci-feedback-loop
```

Pushing now also proves the branch name is free on `origin` before plan 03 depends on it.

**Note what this push does and does not do:** it lands a branch that does NOT yet contain
`.github/workflows/push-build.yml`. GitHub selects the workflow files for a `push` event from the
pushed ref, so no push build runs here — correctly, since the workflow does not exist yet. Plan
03's push is the first one that contains the file and therefore the first that triggers it.

**CRITICAL — DO NOT run `forge`, `make`, `npm ci`, `cabal`, `git submodule update`, or
`scripts/wt-setup.sh` in the new worktree.** Submodules are deliberately left uninitialized
locally; CI is the only build environment. The new worktree will show most of `lib/` empty and
that is the CORRECT state, not a problem to fix.

Record the worktree absolute path in the SUMMARY. Plans 02, 03 and 05 write their files inside it.
  </action>
  <verify>
    <automated>git -C /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets worktree list | grep -q 'ci-feedback-loop.*\[feat/ci-feedback-loop\]' &amp;&amp; git -C /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop rev-parse --abbrev-ref HEAD | grep -qx 'feat/ci-feedback-loop' &amp;&amp; test ! -d /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop/out &amp;&amp; test ! -d /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop/node_modules &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets worktree list` contains a line
      matching both `ci-feedback-loop` and `[feat/ci-feedback-loop]`.
    - `git -C /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop rev-parse --abbrev-ref HEAD`
      prints exactly `feat/ci-feedback-loop`.
    - `git -C /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop rev-parse HEAD` equals
      `git -C /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets rev-parse origin/develop`
      (branched from the pushed develop tip, zero divergence).
    - `git ls-remote --heads origin feat/ci-feedback-loop` prints exactly one ref.
    - `test -f /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop/.github/workflows/develop-gate.yml`
      succeeds (full checkout).
    - `test -f /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop/.planning/phases/FEATURES/feat-ci-feedback-loop/01.1-01-PLAN.md`
      succeeds (Task 1's commit is an ancestor of the branch).
    - `test ! -d /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop/out` and
      `test ! -d /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop/node_modules` — nothing
      was built locally.
    - `git -C /home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop submodule status | grep -c '^-'`
      equals the total submodule count (every submodule still uninitialized; `-` prefix means
      not checked out).
    - `gh run list --repo JMSBPP/cfmm-vol-markets --branch feat/ci-feedback-loop --limit 5` shows
      NO workflow run (the branch has no push workflow yet, and `develop-gate` is PR-only).
  </acceptance_criteria>
  <done>A worktree on `feat/ci-feedback-loop`, branched from the pushed `develop` tip and pushed
  to `origin`, exists at `/home/jmsbpp/cfmms-playground/cfmm-wt/ci-feedback-loop` with every
  submodule uninitialized and no build artifacts.</done>
</task>

</tasks>

<verification>
- `.planning/phases/FEATURES/feat-ci-feedback-loop/01.1-01-PLAN.md` is readable through the
  FEATURES path AND the entry is a mode-120000 symlink on `origin/develop`.
- Exactly one open tracking issue matches the Phase 1.1 title on `JMSBPP/cfmm-vol-markets`.
- `feat/ci-feedback-loop` exists locally as a worktree and on `origin`, at the `develop` tip.
- No `out/`, `cache/`, `node_modules/` or initialized submodule appears in the new worktree.
- `git status --porcelain` in MAIN still lists the pre-existing dirty entries — none were
  swept into this plan's commit.
</verification>

<success_criteria>
- The ROADMAP's binding "worktree + issue per phase" convention is satisfied for Phase 1.1.
- The FEATURES layout covers the inserted decimal phase and is documented as covering it.
- Plans 02, 03 and 05 have a worktree to write into, and CI-07's named demonstration branch
  exists on `origin`.
</success_criteria>

<output>
After completion, create
`.planning/phases/01.1-ci-feedback-loop/01.1-01-SUMMARY.md`
recording: the tracking issue number, the worktree absolute path, the `develop` commit SHA that
carries the FEATURES roster/symlink, and the `feat/ci-feedback-loop` SHA on `origin`.
</output>
