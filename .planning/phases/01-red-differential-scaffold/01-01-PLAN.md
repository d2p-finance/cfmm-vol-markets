---
phase: 01-red-differential-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/FEATURES/README.md
  - .planning/phases/FEATURES/feat-red-diff-scaffold
autonomous: true
requirements: [PROC-01]

must_haves:
  truths:
    - "A tracking issue for Phase 1 exists on JMSBPP/cfmm-vol-markets and names the branch feat/red-diff-scaffold"
    - "A git worktree for branch feat/red-diff-scaffold exists on disk, branched from develop"
    - "The path .planning/phases/FEATURES/feat-red-diff-scaffold/ resolves to the phase's PLAN files on develop"
  artifacts:
    - path: ".planning/phases/FEATURES/README.md"
      provides: "Written statement of the FEATURES phase-directory layout (PROC-01 evidence)"
      contains: "feat-<slug>"
    - path: ".planning/phases/FEATURES/feat-red-diff-scaffold"
      provides: "Symlink making the FEATURES layout path real on develop"
  key_links:
    - from: ".planning/phases/FEATURES/feat-red-diff-scaffold"
      to: ".planning/phases/01-red-differential-scaffold"
      via: "git-tracked symlink (mode 120000)"
      pattern: "01-red-differential-scaffold"
---

<objective>
Open Phase 1 the way every phase in this milestone opens: a tracking issue on `develop`, a
dedicated git worktree for `feat/red-diff-scaffold`, and the `.planning/phases/FEATURES/`
layout committed to `develop` so the directory convention this milestone adopts is a real,
tracked path rather than an aspiration.

Purpose: PROC-01, plus the ROADMAP's binding "worktree + issue per phase" convention. Every
later plan in this phase writes code inside the worktree this plan creates.
Output: a tracking issue, a worktree at `/home/jmsbpp/cfmms-playground/cfmm-wt/red-diff-scaffold`
on branch `feat/red-diff-scaffold`, and a `FEATURES/` layout committed to `develop`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Two worktrees are in play for the whole of Phase 1. Never confuse them:

| Role | Absolute path | Branch |
|------|---------------|--------|
| MAIN — planning artifacts (PLAN/SUMMARY), `.planning/` commits | `/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets` | `develop` |
| FEATURE — all code artifacts for this phase | `/home/jmsbpp/cfmms-playground/cfmm-wt/red-diff-scaffold` | `feat/red-diff-scaffold` |

Verified facts:
- `git remote -v` in MAIN: `origin` = `https://github.com/JMSBPP/cfmm-vol-markets.git` (the JMSBPP
  fork, where all work lands), `upstream` = `https://github.com/d2p-finance/cfmm-vol-markets.git`
  (canonical, reachable only via PR). Phase 1 pushes to `origin` only.
- `gh` is authenticated as `JMSBPP`.
- `.planning/phases/` is currently UNTRACKED (`git status --porcelain .planning` prints
  `?? .planning/phases/`). The symlink `.planning/phases/FEATURES/feat-red-diff-scaffold ->
  ../01-red-differential-scaffold` already exists on disk but is not in git.
- The real phase directory must stay at `.planning/phases/01-red-differential-scaffold/`
  because gsd-tools' directory scan does not follow symlinks. The FEATURES path is the
  human-facing layout; the numbered path is the tool-facing one.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Commit the FEATURES layout to develop</name>
  <files>
    /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/.planning/phases/FEATURES/README.md
    /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/.planning/phases/FEATURES/feat-red-diff-scaffold
  </files>
  <read_first>
    - /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/.planning/ROADMAP.md — the "Conventions
      (binding for every phase)" section, which defines the layout being committed here.
    - /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/.planning/REQUIREMENTS.md — PROC-01.
    - Run `ls -la /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/.planning/phases/FEATURES/`
      to see the existing on-disk symlink before touching it.
  </read_first>
  <action>
Work in the MAIN worktree `/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets`, on `develop`.

1. Create `.planning/phases/FEATURES/README.md` with exactly this content:

```markdown
# FEATURES phase layout

This milestone (Haskell↔Plank Differential Conformance) adopts a FEATURES-oriented phase
directory layout. Requirement PROC-01.

## The convention

    .planning/phases/FEATURES/feat-<slug>/
        <NN>-<NN>-PLAN.md
        <NN>-<NN>-SUMMARY.md

Every phase is a FEATURE, named after the `feat/<slug>` git branch it is built on, and every
phase starts with a git worktree on that branch plus a tracking issue on `develop`.

## Why the entries here are symlinks

GSD's own tooling (`gsd-tools.cjs`) scans `.planning/phases/` for `NN-<name>` directories and
its scan does NOT follow symlinks. The real phase directory therefore lives at the numbered
path and the FEATURES entry is a git-tracked symlink into it:

    .planning/phases/FEATURES/feat-red-diff-scaffold -> ../01-red-differential-scaffold

Both paths address the same files. Read or write either; nothing is duplicated.

Making FEATURES phases a first-class GSD structure is deferred to v2 (requirement V2-05).

## Roster

| Phase | FEATURES path | Branch |
|-------|---------------|--------|
| 1 | `feat-red-diff-scaffold` | `feat/red-diff-scaffold` |
| 2 | `feat-volorder-t-minimal` | `feat/volorder-t-minimal` |
| 3 | `feat-volorder-t-rich` | `feat/volorder-t-rich` |
| 4 | `feat-volorder-t-wire-format` | `feat/volorder-t-wire-format` |
| 5 | `feat-rpc-design` | `feat/rpc-design` |
| 6 | `feat-spec-oracle-entrypoint` | `feat/spec-oracle-entrypoint` |
| 7 | `feat-spec-transport` | `feat/spec-transport` |
| 8 | `feat-plank-guards` | `feat/plank-guards` |
| 9 | `feat-guard-parity` | `feat/guard-parity` |
| 10 | `feat-diff-test-green` | `feat/diff-test-green` |
| 11 | `feat-ci-enforcement` | `feat/ci-enforcement` |
```

2. Ensure the symlink exists and points at the numbered directory (it already does; recreate
   only if `readlink` disagrees):

```bash
cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets
readlink .planning/phases/FEATURES/feat-red-diff-scaffold
# must print: ../01-red-differential-scaffold
```

3. Stage and commit BOTH the symlink and the README on `develop`. Use `git add` on the symlink
   path itself so git records mode `120000` rather than following it:

```bash
cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets
git add .planning/phases/FEATURES/README.md .planning/phases/FEATURES/feat-red-diff-scaffold
git add .planning/phases/01-red-differential-scaffold/
git commit -m "docs(01): adopt .planning/phases/FEATURES/ layout (PROC-01)"
git push origin develop
```

Do NOT `git add -A` — `.planning/` may contain other untracked scratch.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets &amp;&amp; git ls-files -s .planning/phases/FEATURES/feat-red-diff-scaffold | grep -q '^120000' &amp;&amp; test -f .planning/phases/FEATURES/feat-red-diff-scaffold/01-01-PLAN.md &amp;&amp; grep -q 'PROC-01' .planning/phases/FEATURES/README.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-files -s .planning/phases/FEATURES/feat-red-diff-scaffold` prints a line beginning
      with `120000` (git tracks it as a symlink, not as a copied directory).
    - `readlink .planning/phases/FEATURES/feat-red-diff-scaffold` prints exactly
      `../01-red-differential-scaffold`.
    - `test -f .planning/phases/FEATURES/feat-red-diff-scaffold/01-01-PLAN.md` succeeds — the
      FEATURES path resolves to real plan files.
    - `grep -c 'feat-' .planning/phases/FEATURES/README.md` returns at least 11 (the roster).
    - `grep -q 'PROC-01' .planning/phases/FEATURES/README.md` succeeds.
    - `git log origin/develop -1 --name-only` lists `.planning/phases/FEATURES/README.md`.
    - `git status --porcelain .planning/phases/FEATURES` prints nothing (all committed).
  </acceptance_criteria>
  <done>The FEATURES layout is a tracked, pushed path on `develop` whose `feat-red-diff-scaffold`
  entry resolves to this phase's plan files.</done>
</task>

<task type="auto">
  <name>Task 2: Open the Phase 1 tracking issue</name>
  <files>(no files — GitHub issue on JMSBPP/cfmm-vol-markets)</files>
  <read_first>
    - /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/.planning/ROADMAP.md — "Phase 1: RED
      Differential Scaffold" section, whose Goal and Success Criteria are quoted into the issue.
    - Run `gh issue list --repo JMSBPP/cfmm-vol-markets --limit 10 --state all` first to check
      no Phase 1 tracking issue already exists (do not open a duplicate).
  </read_first>
  <action>
If `gh issue list --repo JMSBPP/cfmm-vol-markets --state all --search "RED Differential Scaffold"`
already returns a matching issue, record its number and skip creation.

Otherwise create it:

```bash
gh issue create --repo JMSBPP/cfmm-vol-markets \
  --title "Phase 1: RED Differential Scaffold (feat/red-diff-scaffold)" \
  --body '<body below>'
```

Issue body, verbatim:

```markdown
Tracking issue for **Phase 1: RED Differential Scaffold** of the Haskell↔Plank Differential
Conformance milestone.

- **Branch:** `feat/red-diff-scaffold`
- **Worktree:** `/home/jmsbpp/cfmms-playground/cfmm-wt/red-diff-scaffold`
- **Plans:** `.planning/phases/FEATURES/feat-red-diff-scaffold/`
- **Requirements:** RED-01, RED-02, RED-03, RED-04, RED-05, RED-06, PROC-01

## Goal

The differential test exists as a real, compiling, pushed artifact — written against the
assertion it will eventually make, and guarded so it costs the gate nothing until an oracle
exists.

## Success criteria

- [ ] `develop-gate` green on `feat/red-diff-scaffold`, forge job compiling
      `test/protocol_integrations/VolOrderToPanopticTokenId.diff.t.sol` under `--via-ir`.
- [ ] The same run reports the differential test **skipped via the wiring probe**, with **no
      edit to the gate `--skip` ledger** in `.github/workflows/develop-gate.yml`.
- [ ] The same run still reports `VolOrderToPanopticTokenId.t.sol` green (regression floor).
- [ ] The merged tree carries the doctrine header (neither side sacrosanct), the differential
      discipline (bound-constructed corpora, non-fuzz anchor, asserted non-vacuity), and a
      written description of layout, naming and the Solidity↔spec transport boundary.
- [ ] `.planning/phases/FEATURES/feat-red-diff-scaffold/` exists on `develop`.

## Not in this phase

No oracle. No transport. `SpecHelper.readTokenId` reverts by design; the test skips by design.
Wiring is Phase 7; enforcement is Phase 11.
```

Record the resulting issue number (e.g. `#57`) — later plans reference it in the PR body.
Append the number to the plan's SUMMARY.
  </action>
  <verify>
    <automated>gh issue list --repo JMSBPP/cfmm-vol-markets --state open --search "RED Differential Scaffold" --json number,title | grep -q "RED Differential Scaffold" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `gh issue list --repo JMSBPP/cfmm-vol-markets --state open --json number,title` contains
      an entry whose title is exactly
      `Phase 1: RED Differential Scaffold (feat/red-diff-scaffold)`.
    - `gh issue view <N> --repo JMSBPP/cfmm-vol-markets --json body -q .body | grep -c 'RED-0'`
      returns at least 1 (requirement IDs are in the body).
    - The issue body contains the literal string `feat/red-diff-scaffold`.
    - Exactly ONE open issue matches that title (no duplicate was created).
  </acceptance_criteria>
  <done>A single open tracking issue exists on the JMSBPP fork naming the branch, the plans
  directory, the seven requirement IDs and the five success criteria.</done>
</task>

<task type="auto">
  <name>Task 3: Create the feat/red-diff-scaffold worktree</name>
  <files>(no repo files — creates the worktree at /home/jmsbpp/cfmms-playground/cfmm-wt/red-diff-scaffold)</files>
  <read_first>
    - Run `git -C /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets worktree list` to see the
      existing worktree layout and confirm the naming pattern `cfmm-wt/<name>`.
    - /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/.planning/PROJECT.md — the "Constraints
      → Workflow" bullet that makes worktree-per-feature binding.
  </read_first>
  <action>
Branch from the `develop` commit that Task 1 just pushed, so the FEATURES layout is an
ancestor of the feature branch.

```bash
cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets
git fetch origin develop
git worktree add /home/jmsbpp/cfmms-playground/cfmm-wt/red-diff-scaffold -b feat/red-diff-scaffold develop
```

Then push the branch so it exists on `origin` (the PR in plan 01-05 needs it; pushing now also
proves the branch name is not already taken upstream):

```bash
git -C /home/jmsbpp/cfmms-playground/cfmm-wt/red-diff-scaffold push -u origin feat/red-diff-scaffold
```

CRITICAL — DO NOT run `forge`, `make`, `npm ci` or `cabal` in the new worktree. Submodules are
deliberately left uninitialized locally; CI is the only build environment. The new worktree will
show most of `lib/` unchecked-out and that is the correct state. Do not `git submodule update`.

Record the worktree path in the SUMMARY. Every subsequent plan in Phase 1 writes its code
inside this worktree.
  </action>
  <verify>
    <automated>git -C /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets worktree list | grep -q 'red-diff-scaffold.*\[feat/red-diff-scaffold\]' &amp;&amp; git -C /home/jmsbpp/cfmms-playground/cfmm-wt/red-diff-scaffold rev-parse --abbrev-ref HEAD | grep -qx 'feat/red-diff-scaffold' &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git -C /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets worktree list` contains a line
      matching `red-diff-scaffold` and `[feat/red-diff-scaffold]`.
    - `git -C /home/jmsbpp/cfmms-playground/cfmm-wt/red-diff-scaffold rev-parse --abbrev-ref HEAD`
      prints exactly `feat/red-diff-scaffold`.
    - `git -C /home/jmsbpp/cfmms-playground/cfmm-wt/red-diff-scaffold rev-parse HEAD` equals
      `git -C /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets rev-parse origin/develop`
      (branched from the pushed develop tip, no divergence).
    - `git ls-remote --heads origin feat/red-diff-scaffold` prints one ref.
    - `test -f /home/jmsbpp/cfmms-playground/cfmm-wt/red-diff-scaffold/test/protocol_integrations/VolOrderToPanopticTokenId.t.sol`
      succeeds (the worktree is a full checkout of the repo).
    - `test -d /home/jmsbpp/cfmms-playground/cfmm-wt/red-diff-scaffold/out` is FALSE — nothing
      was built locally.
  </acceptance_criteria>
  <done>A worktree on `feat/red-diff-scaffold`, branched from the pushed `develop` tip and
  pushed to `origin`, exists at `/home/jmsbpp/cfmms-playground/cfmm-wt/red-diff-scaffold` with
  no local build artifacts.</done>
</task>

</tasks>

<verification>
- `.planning/phases/FEATURES/feat-red-diff-scaffold/01-01-PLAN.md` is readable through the
  FEATURES path AND tracked as a symlink on `origin/develop`.
- Exactly one open tracking issue matches the Phase 1 title on `JMSBPP/cfmm-vol-markets`.
- `feat/red-diff-scaffold` exists locally as a worktree and on `origin`, at the `develop` tip.
- No `out/`, `cache/`, or initialized submodule appears in the new worktree.
</verification>

<success_criteria>
- PROC-01 is satisfied on `develop`: the FEATURES path is real, tracked, documented and
  resolves to this phase's plans.
- The ROADMAP's "worktree + issue per phase" convention is satisfied for Phase 1.
- Plans 01-02 through 01-06 have a worktree to write into.
</success_criteria>

<output>
After completion, create
`.planning/phases/01-red-differential-scaffold/01-01-SUMMARY.md`
recording: the tracking issue number, the worktree absolute path, and the `develop` commit SHA
that carries the FEATURES layout.
</output>
