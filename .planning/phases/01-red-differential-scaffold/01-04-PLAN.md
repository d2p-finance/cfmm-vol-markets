---
phase: 01-red-differential-scaffold
plan: 04
type: execute
wave: 4
depends_on: ["01-02", "01-03"]
files_modified:
  - notes/DIFFERENTIAL_LAYOUT.md
  - Makefile
autonomous: true
requirements: [RED-06]

must_haves:
  truths:
    - "A reader of Phases 6-11 can find, in one document, where differential files live, what they are named, and where the Solidity<->spec boundary is"
    - "The transport is recorded as RESOLVED - JSON-RPC, decided at evm-spec-bridge initialization, outside Phase 5 - with the override visible rather than smoothed over"
    - "The generated-interface boundary is recorded: the bridge generates the Solidity library from the same schema as its Haskell protocol types, and Phase 1's SpecHelper.sol is a provisional stand-in for it"
    - "The three-way outcome contract (spec success / spec rejection / transport failure) is written down before Phase 7 implements it, together with the channel rule that makes it achievable"
    - "The measured transport constraints are recorded WITH their measurements, so a later phase cannot simplify them away"
    - "The wiring probe's lifecycle - introduced Phase 1, implemented Phase 7, removed Phase 11 - is recorded, including probe-once-and-cache"
    - "Wire format (Phase 4) and oracle packaging (Phase 6) are still recorded as OPEN and are not pre-resolved"
    - "The new differential has a Makefile target following the same convention as every other .diff.t.sol in the repo"
  artifacts:
    - path: "notes/DIFFERENTIAL_LAYOUT.md"
      provides: "RED-06: file layout, naming, the generated-interface boundary, the measured transport constraints, and the Solidity<->spec seam that Phases 6-11 extend rather than redesign"
      contains: "Solidity"
      min_lines: 140
    - path: "Makefile"
      provides: "test-vol-order-tokenid-diff target, matching the repo's per-diff-test target convention"
      contains: "test-vol-order-tokenid-diff"
  key_links:
    - from: "notes/DIFFERENTIAL_LAYOUT.md"
      to: "test/protocol_integrations/SpecHelper.sol"
      via: "the document names the seam file as the single transport boundary"
      pattern: "SpecHelper\\.sol"
    - from: "test/protocol_integrations/SpecHelper.sol"
      to: "notes/DIFFERENTIAL_LAYOUT.md"
      via: "natspec cross-reference already written in plan 01-02"
      pattern: "notes/DIFFERENTIAL_LAYOUT\\.md"
---

<objective>
Write down the organization — file layout, naming, the generated-interface boundary and the
Solidity↔spec transport boundary — so Phases 6 through 11 extend it instead of redesigning it,
and give the new differential the same Makefile entry every other `.diff.t.sol` in this repo has.

Purpose: RED-06. The transport decision is now RESOLVED and arrived from outside this roadmap's
sequencing; two decisions (wire format, oracle packaging) remain owned by Phases 4 and 6 and must
NOT be pre-resolved here. What this document fixes is the *shape* the remaining answers plug
into — which file, which function, which outcomes must stay distinguishable, and which transport
behaviours have been MEASURED and must not be re-litigated by guess.
Output: `notes/DIFFERENTIAL_LAYOUT.md` and one new `Makefile` target on `feat/red-diff-scaffold`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-red-differential-scaffold/01-02-SUMMARY.md
@.planning/phases/01-red-differential-scaffold/01-03-SUMMARY.md

**Work directory: `/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets` (branch
`feat/red-diff-scaffold`).**

**NO LOCAL COMPILATION.** Do not run `forge`, `make`, `npm ci` or `cabal`. The Makefile target
added here is a convenience entry point for later, gate-independent use; it is NOT executed as
part of verifying this plan, and it is NOT how any Phase 1 claim is verified.

`notes/` is the repo's binding-spec directory (`DATA_CONTRACT.md`, `UNITS_AND_SCALES.md`,
`STOCHASTIC_MODEL.md`), cited from `src/*.plk` comments. Putting the layout document there is
deliberate: `SpecHelper.sol` already cites it by path.

**WHAT IS NOW DECIDED, AND WHAT IS STILL OPEN.**

RESOLVED, and this document RECORDS it rather than making it:
- **Spec transport: JSON-RPC.** Decided by the user at `evm-spec-bridge` initialization,
  **outside Phase 5**, knowingly overriding this project's "open by design, do not pre-resolve"
  instruction. Record the override VISIBLY — Phase 5's success criterion 1 and RPC-01 both
  require the override be shown, not smoothed over.
- **`evm-spec-bridge` enters this repo as a submodule**, and **GENERATES the Solidity interface
  from the same schema as its Haskell protocol types**, so the two sides cannot drift silently.

STILL OPEN — record with owning phase and constraints, and pick NONE of them:
- `VolOrder(T)` wire format (Shock-style tagged vs per-variant) → **Phase 4**
- Oracle packaging (new cabal exe vs a mode on `cfmm-scratchpad-exe`) → **Phase 6**
- **RPC-02, the responsibility split** between the Haskell spec service and the Foundry test
  process → **Phase 5**, which still owns it

**THE TRANSPORT MEASUREMENTS THIS DOCUMENT MUST CARRY.** All measured against
`forge 1.5.1-stable` (`b0a9dd9`), the local toolchain, by the bridge author. They are recorded
here — not in `SpecHelper.sol`, not in the test — because this is the document Phase 7 reads
before implementing. **A rule with its evidence attached survives; a bare rule gets "simplified"
away**, and each of these, simplified away, reintroduces a false green.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write notes/DIFFERENTIAL_LAYOUT.md</name>
  <files>/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/notes/DIFFERENTIAL_LAYOUT.md</files>
  <read_first>
    - `notes/DATA_CONTRACT.md` and `notes/UNITS_AND_SCALES.md` — match their register and
      heading style; these are the sibling binding-spec documents.
    - `test/protocol_integrations/SpecHelper.sol` (plan 01-02) — the seam being documented,
      including its `EXTENSION POINTS` natspec block, which this document must agree with.
    - `test/protocol_integrations/VolOrderToPanopticTokenId.diff.t.sol` (plan 01-03) — the
      doctrine and discipline this document points at rather than restates, and the
      probe-once / assert-on-success / full-envelope patterns it obeys.
    - `.planning/PROJECT.md` Key Decisions — the RESOLVED transport row, the `evm-spec-bridge`
      submodule row, and the "spec version must have ONE authority" row. Quote their substance;
      do not invent a different rationale.
    - `.planning/ROADMAP.md` — Phase 4, 5, 6, 7 and 11 sections, for the exact owning-phase
      attributions, and the Sequencing Notes for the CI tension and the spec-skew hazard.
    - Run `find test -name '*.diff.t.sol' | sort` to confirm the four existing family members
      whose naming convention the document records.
  </read_first>
  <action>
Create `notes/DIFFERENTIAL_LAYOUT.md`. It must contain these exact H2 headings, in this order:

1. `## Scope`
2. `## File layout and naming`
3. `## The Solidity<->spec transport boundary`
4. `## The generated interface boundary`
5. `## Transport mechanics: measured constraints`
6. `## Outcome contract: three states, never conflated`
7. `## The wiring probe and its lifecycle`
8. `## Doctrine and discipline`
9. `## Open decisions, open questions and open risks`
10. `## Extension points, phase by phase`

Content requirements, section by section:

**`## Scope`** — one paragraph. This document fixes the *organization* of the Haskell↔Plank
differential: where files live, what they are called, where the process boundary is, and which
transport behaviours have been measured rather than assumed. It does not choose the wire format
or the oracle packaging.

**`## File layout and naming`** — a table with the exact paths, plus the convention statement:

| Path | Role |
|------|------|
| `test/protocol_integrations/VolOrderToPanopticTokenId.diff.t.sol` | the differential itself |
| `test/protocol_integrations/SpecHelper.sol` | the ONLY Solidity↔spec seam |
| `test/protocol_integrations/VolOrderToPanopticTokenIdHarness.plk` | the impl-side entrypoint |
| `test/protocol_integrations/VolOrderToPanopticTokenId.t.sol` | the structural suite; the REGRESSION FLOOR |
| `notes/DIFFERENTIAL_LAYOUT.md` | this document |

State the naming rules explicitly:
- `*.diff.t.sol` is the repo's established differential-test suffix. Existing family members
  are `test/pos_spec/VolOrderManager.diff.t.sol`,
  `test/market_state_measurements/RealizedVolatility.diff.t.sol`,
  `test/market_state_measurements/GetAverageVolatility.diff.t.sol`,
  `test/exposure/VegaIssuance.diff.t.sol`. New differentials join it; they do not invent a
  suffix.
- A `.diff.t.sol` sits in the SAME directory as the subject it diffs and as that subject's
  `*Harness.plk`, so the three are read together.
- The differential test contract is `<Subject>DiffTest`; the structural suite is `<Subject>Test`.
- Each `.diff.t.sol` gets a `make` target named `test-<subject>-diff`, matching the existing
  `test-vol-order-diff` / `test-realized-vol` convention.

**`## The Solidity<->spec transport boundary`** — state, as rules:
- `SpecHelper.sol` is the ONLY place a Foundry test may reach the Haskell spec. No test spawns a
  process, reads a spec-produced file, or calls a cheatcode against the spec directly.
- The seam is the library `SpecOracle`. Its two methods are
  `health() -> Health` and `volOrderToTokenId(bytes volOrderWire, uint64 poolId) -> TokenIdResult`.
  Record that the Phase 1 draft's `readTokenId(uint256,uint256,uint256[4])` and its separate
  `isWired()` probe were REPLACED by these: the names follow the interface the bridge generates,
  and `isWired()` COLLAPSED INTO `health()` because two wiring mechanisms are two things that can
  drift. RED-04 is satisfied in substance — a stub that reverts when called, plus a wiring
  predicate the test queries first — under the generated names.
- `SpecHelperProbe` is the external call boundary, and it is HAND-WRITTEN, not generated. It
  exists because a Solidity library's `internal` functions are inlined, so a revert inside the
  seam would abort the calling test rather than being observable. Phase 7 keeps it for a sharper
  reason recorded in the next-but-one section: a transport fault arrives as a REVERT, and a
  revert must be catchable across a real call boundary before it can be converted into
  `Status.TransportFailure`.
- Callers reach the boundary with a LOW-LEVEL `address(probe).call(...)`, not `try`/`catch` —
  see the open questions.
- The input crossing the boundary is Plank-originated: Plank is the fuzz source, the serialized
  `VolOrder(T)` is TRANSPORTED, never reconstructed independently on the Haskell side. Two
  independently constructed corpora would drift, which defeats the test.
- `volOrderWire` is OPAQUE at this boundary. Phase 4 chooses the format; nothing else about the
  seam's shape changes when it does.
- `TokenIdResult.detail` is diagnostics only and is NEVER asserted on, in any phase.

**`## The generated interface boundary`** — the section RED-06 gained when the transport was
resolved. State:
- `evm-spec-bridge` (canonical `d2p-finance/evm-spec-bridge`, `JMSBPP` fork, PR-only) enters this
  repo as a submodule. It is a Haskell library plus a JSON-RPC server exe, depends on
  `cfmm-vol-markets-spec`, and **generates the Solidity interface from the same schema as its
  Haskell protocol types**. Drift between the two sides is prevented BY CONSTRUCTION, not by
  review.
- Phase 1's `SpecOracle` in `SpecHelper.sol` is a **PROVISIONAL HAND-WRITTEN STAND-IN** for that
  generated artifact — the shape, and nothing else. Phase 7 replaces it with the generated file
  and adopts whatever the generator emits. Where the two differ, the generator wins and the
  difference is recorded here.
- Two arrangements in Phase 1 are explicitly **provisional-pending-generation**: `Status` living
  as a field on `Health` (rather than `health()` returning a `(Status, Health)` pair), and the
  Phase 1 stub REVERTING rather than returning a `TransportFailure` struct. The second is a
  property of the STUB only, and is deliberate: a struct-returning stub is FAIL-OPEN, and a test
  that forgot to check `status` would proceed with `tokenId == 0` — precisely the false-green
  class this milestone exists to eliminate. The revert is FAIL-SAFE, and it is the backstop for
  anyone who bypasses the wiring predicate.
- **Spec version must have ONE authority.** The bridge depends on `cfmm-vol-markets-spec` and
  this repo pins `spec/` directly — two paths to the oracle. Divergence breaks nothing and fails
  nothing, so the differential would compare Plank against a spec version nobody believes is the
  oracle and stay green. Mitigation is mandatory: `Health.specCommit` reports the SHA the RUNNING
  BINARY was built from, and Phase 5 (RPC-03) asserts it equals the `spec/` pin, failing loudly.
  Preferred topology if the bridge supports it: pin only the bridge.
- Record as EXPECTED FUTURE METHODS, not implemented in Phase 1: `spec_fixtureRejection`
  (returns a canned rejection) and `spec_fixtureTransportFault` (forces a transport error).
  Naming them now is what lets Phase 7 extend rather than redesign — the three-way outcome
  contract is untestable without a way to provoke each of the three.

**`## Transport mechanics: measured constraints`** — the section that must carry its evidence.
Open it with the scoping sentence: **all findings below are measured against
`forge 1.5.1-stable` (`b0a9dd9`); Foundry is unpinned in this repo (see open risks), and v1.8.0
encodes cheatcode returns differently.** Then, as numbered binding constraints:

1. **Every call site MUST bind its success flag and check it before touching the returned
   bytes.** MEASURED: a server that accepts the connection and never answers costs **45.00 s per
   call**, and **the test PASSED** — because the call site ignored `success`. At
   `fuzz.runs = 256` that is **3.2 hours of green CI meaning nothing**: this project's founding
   failure mode, reproduced against the transport we chose. Connection-refused fails fast; a
   half-dead server is far worse than a dead one, and there is **no timeout knob** (see 5).
2. **Spec rejection MUST ride the HTTP-200 `result` channel as a tagged value. The JSON-RPC
   `error` field is reserved exclusively for transport and protocol faults** (unknown method, bad
   params), which revert. MEASURED: `fork.rs:599-604` maps any provider error to `Err`;
   `inspector.rs:1443-1453` turns a cheatcode `Err` into `InstructionResult::Revert`;
   `error.rs:137-142` encodes it as `Vm::CheatcodeError { message: string }` — **untyped free
   text**. A JSON-RPC error object, HTTP 500, connection-refused, timeout and calling a cheatcode
   the binary lacks ALL revert with the same selector `0xeeaa9e6f`, differing only in unstable
   English. **The three-way distinction can therefore NEVER be recovered by matching selectors or
   parsing revert messages.** It exists only because rejection rides `result` with a tag. This is
   the mechanism XPORT-02 and Phase 9 depend on.
3. **The result MUST travel as ONE `"0x"`-prefixed, even-nibble hex string carrying a tag byte
   plus our own ABI encoding. This is the single highest-value constraint in the design.**
   MEASURED, three ways: `vm.rpc` runs the JSON `result` through `json_value_to_token`, where
   objects become tuples in **alphabetical key order**, numbers round-trip through `f64`, and
   `null` becomes 32 zero bytes; and the coercion is **VALUE-DEPENDENT** — the same record shape
   decodes to `tuple(string,string,string)` for `tokenId "0"` but `tuple(string,string,uint256)`
   for `tokenId "18446744073709551616"`, so a decoder correct for one is wrong for the other,
   intermittently, by input magnitude, across a fuzz campaign. `"0x<even-nibble hex>"` is the
   only branch with no coercion, no reordering and no magnitude sensitivity. **A later phase must
   not "simplify" this into returning a JSON object.**
4. **NEVER write `bytes memory b = vm.rpc(...)` then `abi.decode(b, ...)`.** MEASURED on 1.5.1:
   static-coerced results give a bare un-messaged `EvmError: Revert`, and **object results
   silently return garbage with `b.length == 96`**. Silent garbage into `abi.decode` is the worst
   possible shape for this project. It is also version-dependent, so it is not a stable contract
   even where it appears to work.
5. **Timeouts and retries are HARDCODED and unconfigurable.** MEASURED: `rpc_result` builds its
   provider via `ProviderBuilder::new`, **not** `from_config`, so the 45 s `REQUEST_TIMEOUT`,
   `max_retry` of 8 and 800 ms `initial_backoff` cannot be tuned — neither `eth_rpc_timeout` nor
   a per-endpoint `retries` key reaches it. **Do not record a tuning knob; there is none.**
   Retries fire only on HTTP 429/503.
6. **Probe the wiring ONCE per test contract, in `setUp`, and cache it.** Every skip reads the
   cache; no test body calls `health()`. Rationale, stated accurately: (a) hygiene — one wiring
   decision per contract rather than N identical ones; (b) insurance against the residual risk in
   (1), a hung server at a hardcoded 45 s per call. **It is NOT justified by a retry storm** —
   that justification is known to be false, since connection-refused fails fast.
7. **`health()` returns the SAME envelope as a domain method.** MEASURED: `"ok"` is a `string`,
   one of the few shapes that decodes cleanly — so a bare-string health check would round-trip
   green while the domain payload path was broken. "Wired" must mean **the real envelope
   round-trips**, so `health()` carries the full `Health` struct through the same tagged hex
   envelope as `TokenIdResult`.
8. **Address the oracle as `http://127.0.0.1:PORT`.** Alloy's `guess_local_url` recognises only
   `localhost`, `127.0.0.1` and `::1`; any other host form (a container name, a `.local` alias)
   is not treated as local and `HTTP_PROXY` is honoured — a bizarre failure mode on a proxied
   self-hosted runner.
9. **The cheatcode is `vm.rpc`, the three-argument form `vm.rpc(url, method, params)`. NOT
   `vm.rpcJson`.** VERIFIED FROM SOURCE: `rpc_result` calls
   `ProviderBuilder::<AnyNetwork>::new(url).build()` then `provider.raw_request(method, params)` —
   **no allowlist, no `eth_*` check, no namespace filter, no node handshake**; `rpc_endpoint`
   accepts any string beginning `http`/`ws`; and `rpc_1Call` is `apply`, not `apply_stateful`, so
   the three-arg form needs **no fork, no anvil and no `[rpc_endpoints]` entry** — a literal URL
   works in plain `forge test`. `vm.rpcJson` was merged 2026-06-05 (PR #15076) and appears only
   in v1.8.0; the toolchain here is 1.5.1-stable, so it does not exist and pinning to a same-day
   release to obtain it would be reckless.

**`## Outcome contract: three states, never conflated`** — a table Phase 7 implements against:

| Outcome | Carried as | May it ever look like agreement? |
|---------|-----------|----------------------------------|
| spec success | `Status.Ok` + `tokenId`, in the `result` channel | it IS the comparison |
| spec rejection | `Status.Rejected` + `Guard`, in the `result` channel | no — it is compared against Plank's revert-vs-return (Phase 9, GUARD-05) |
| transport failure | `Status.TransportFailure`, produced by catching the cheatcode revert at the boundary | no — and it must never be reported as a rejection either |

Add the sentence: an unreachable oracle reported as a rejection, or a rejection reported as
agreement, is how a differential test goes silently green; distinguishing the three is therefore
a correctness requirement, not ergonomics. Then state that the distinction is only ACHIEVABLE
because of constraint 2 above — record the dependency explicitly, so nobody moves rejections onto
the `error` channel later and destroys it. Record the stable `Guard` ids and their requirement
mapping: `None` (zero value), `OptionRatioRange` (GUARD-01), `LegSpanBelowSpacing` (GUARD-02),
`TickOutOfBounds` (GUARD-03); the shared pair is GUARD-04.

**`## The wiring probe and its lifecycle`** — record all three states:
- Phase 1: `SpecOracle.health()` reports `Status.TransportFailure`; the test probes it ONCE in
  `setUp` through `SpecHelperProbe` with a low-level call, decodes the full `Health` struct,
  caches the status, and the differential tests `vm.skip` on the CACHED value. The reverting stub
  is never reached on that path; it is the backstop for anyone who bypasses the predicate.
- Phase 7: `health()` becomes a live transport health check. **The predicate is unchanged** —
  `status == Status.TransportFailure` — which is the concrete meaning of "extend, not redesign".
  The skip still governs, because the spec is not on the CI runner until Phase 11.
- Phase 11 (CI-04): the probe and every `vm.skip` guarded by it are REMOVED. A deliberately
  unreachable oracle must then make `develop-gate` RED, not green-with-a-skip.

State the standing rule explicitly: **the skip is computed by the test, never configured in the
gate.** Adding `*VolOrderToPanopticTokenId.diff.t.sol*` to the `--skip` ledger in
`.github/workflows/develop-gate.yml` would exclude the file from compilation and execution
entirely, and is forbidden for this file in every phase. (`*PanopticVegaLens.t.sol*` is on that
ledger as a drafted RED test — that is the mechanism this milestone deliberately does not use.)

**`## Doctrine and discipline`** — do not restate the doctrine; point at it. Say: the doctrine
("neither side sacrosanct") and the discipline (corpora CONSTRUCTED with `bound` and never
filtered with `vm.assume`; every fuzz names a non-fuzz anchor; non-vacuity ASSERTED via a live
counter) live in the header of
`test/protocol_integrations/VolOrderToPanopticTokenId.diff.t.sol` and are binding on every
differential added to this family. Summarise the "neither side sacrosanct" orientation in two
sentences and note that it is where this file departs from
`test/pos_spec/VolOrderManager.diff.t.sol`, whose oracle is a disposable restatement.
Add one rule this document owns rather than points at: **spec rejections are NEVER routed through
`vm.assume`.** `max_test_rejects` is 65536 and SHARED across the run, so a rejection filtered
that way burns a global budget and is HIDDEN rather than observed — and rejection parity
(GUARD-05) needs rejections visible.

**`## Open decisions, open questions and open risks`** — three tables. Record them as OPEN with
constraints, and pick none.

Open DECISIONS (owned by a phase):

| Decision | Owner | Constraint any answer must satisfy |
|----------|-------|-------------------------------------|
| `VolOrder(T)` wire format — Shock-style tagged vs per-variant | Phase 4 | a consumer must recover WHICH `T` it received from the bytes alone |
| Oracle packaging — new cabal exe vs a mode on `cfmm-scratchpad-exe` | Phase 6 | must be buildable on the self-hosted runner; GHC/cabal availability there is UNVERIFIED |
| RPC-02 — the responsibility split for wire encode/decode, input validation, guard evaluation and error classification | Phase 5 | no responsibility unassigned or shared by default; **the Foundry test process owns none of them** — any semantics it holds is a re-implementation of the spec, which is the failure this milestone exists to eliminate |

Record separately, in a sentence above that table, that **spec transport is RESOLVED: JSON-RPC**,
decided at `evm-spec-bridge` initialization **outside Phase 5**, by the user, knowingly overriding
this project's "open by design" instruction. Rationale for JSON-RPC over `vm.ffi`: a warm service
avoids per-case process spawn across a 256-run fuzz and generalizes to the whole spec surface.
RPC-01 records the decision; it no longer makes it. State the override plainly — Phase 5's
criterion 1 requires it visible, not smoothed over.

Open QUESTIONS (no owner yet; answer before Phase 7 relies on them):

| Question | Why it matters | Status |
|----------|----------------|--------|
| Does `try`/`catch` work against a cheatcode-originated revert? | it performs an `extcodesize` check against the cheatcode address. This is the LOWEST-confidence item in the transport analysis, and the entire three-way distinction rests on catching that revert | OPEN — mitigated by using a low-level `address(...).call(...)` at the boundary instead. Do not assert either way |
| Does an end-to-end round trip against a NON-Ethereum JSON-RPC server actually work? | `vm.rpc` accepting an arbitrary method string is verified from source, but a full round trip against a non-Ethereum server has **no known prior art** | OPEN — Phase 5's RPC-03 skeleton is what answers it |

Open RISKS:

| Risk | Evidence | Disposition |
|------|----------|-------------|
| **Foundry is UNPINNED in this repo** | no version key in `foundry.toml`, no `foundry-toolchain` action or `foundryup` step in `develop-gate`, no `.foundryrc`, no `.tool-versions`. The runner is persistent, so its `forge` drifts on any `foundryup`. Every measurement in this document is scoped to `1.5.1-stable` `b0a9dd9`; v1.8.0 encodes cheatcode returns differently | An unpinned floating toolchain makes wire behaviour non-deterministic across runs — a silent-divergence source of exactly the kind this milestone exists to eliminate. **NOT in Phase 1's scope** (it is in none of RED-01..RED-06); recorded here as an explicit open risk and a candidate for `/gsd:insert-phase` |
| Spec-version skew via two paths to the oracle | see `## The generated interface boundary` | mitigated by `Health.specCommit` asserted against the `spec/` pin (RPC-03); mitigation is mandatory, not optional |

**`## Extension points, phase by phase`** — a table matching `SpecHelper.sol`'s natspec exactly:

| Phase | What changes | What does NOT change |
|-------|--------------|----------------------|
| 4 | `volOrderWire` becomes the `VolOrder(T)` wire bytes; the test's one placeholder encoder is replaced | the seam file, the seam signature, the probe, the outcome contract |
| 5 | the responsibility split is fixed (RPC-02) and `health()` is proven end to end | the transport choice, the seam's shape |
| 6 | the Haskell gains an out-of-process entrypoint | nothing on the Solidity side |
| 7 | the generated library replaces the provisional stand-in; both bodies become real; the three outcomes become observable | the file, the probe boundary, the skip predicate, the doctrine |
| 8-9 | Plank gains guards; revert-vs-return parity is asserted over `TokenIdResult.guard` | the corpus discipline, the `Guard` ids |
| 10 | the fuzz corpus widens to the divergence-prone geometry | the anchor, the non-vacuity counter |
| 11 | the probe and every `vm.skip` are removed; the gate builds the spec | the layout in this document |

Close with a line stating that a phase which finds this organization inadequate should amend
THIS document in its own branch and say why, rather than diverging from it silently.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets && F=notes/DIFFERENTIAL_LAYOUT.md && for h in '## Scope' '## File layout and naming' '## The Solidity<->spec transport boundary' '## The generated interface boundary' '## Transport mechanics: measured constraints' '## Outcome contract: three states, never conflated' '## The wiring probe and its lifecycle' '## Doctrine and discipline' '## Open decisions, open questions and open risks' '## Extension points, phase by phase'; do grep -qF "$h" "$F" || { echo "MISSING: $h"; exit 1; }; done && grep -qF 'SpecHelper.sol' "$F" && grep -qF 'develop-gate.yml' "$F" && grep -qF 'JSON-RPC' "$F" && grep -qF '1.5.1-stable' "$F" && [ "$(wc -l < "$F")" -ge 140 ] && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `notes/DIFFERENTIAL_LAYOUT.md` exists and is at least 140 lines.
    - All ten H2 headings listed in the action are present, verbatim, in the given order
      (`grep -n '^## ' notes/DIFFERENTIAL_LAYOUT.md` returns them in that sequence).
    - The document contains all five canonical paths verbatim:
      `test/protocol_integrations/VolOrderToPanopticTokenId.diff.t.sol`,
      `test/protocol_integrations/SpecHelper.sol`,
      `test/protocol_integrations/VolOrderToPanopticTokenIdHarness.plk`,
      `test/protocol_integrations/VolOrderToPanopticTokenId.t.sol`,
      `notes/DIFFERENTIAL_LAYOUT.md`.
    - It names all four existing family members: `VolOrderManager.diff.t.sol`,
      `RealizedVolatility.diff.t.sol`, `GetAverageVolatility.diff.t.sol`,
      `VegaIssuance.diff.t.sol`.
    - **Transport recorded as RESOLVED, with the override visible:** the document contains
      `RESOLVED`, `JSON-RPC`, `evm-spec-bridge`, and a phrase placing the decision outside
      Phase 5 (`grep -qi 'outside Phase 5' "$F"` succeeds), and `RPC-01`.
    - **Generated-interface boundary recorded:** the document contains
      `generates the Solidity interface`, `PROVISIONAL`, `spec_fixtureRejection` and
      `spec_fixtureTransportFault`.
    - **The seam's current names are recorded and the superseded ones are named as superseded:**
      the document contains `SpecOracle`, `volOrderToTokenId(bytes volOrderWire, uint64 poolId)`,
      `SpecHelperProbe`, `health()`, and states that `readTokenId` / `isWired` were replaced
      (`grep -q 'isWired' "$F"` succeeds and appears in the sentence recording the collapse).
    - **Every measured constraint carries its measurement.** All of these literal strings are
      present: `45.00 s`, `3.2 hours`, `0xeeaa9e6f`, `CheatcodeError`, `alphabetical key order`,
      `b.length == 96`, `ProviderBuilder::new`, `max_retry`, `127.0.0.1`, `vm.rpcJson`,
      `raw_request`, `1.5.1-stable`, `b0a9dd9`.
    - The document states that `vm.rpc` is the cheatcode and `vm.rpcJson` is NOT available on
      this toolchain: `grep -q 'NOT `vm.rpcJson`' "$F"` or equivalent phrasing succeeds, and
      `grep -qi 'eth_rpc_timeout' "$F"` FAILS — no non-existent tuning knob is recorded.
    - It contains the three outcome names `spec success`, `spec rejection`,
      `transport failure`, and the words `never conflated`, and states that the distinction
      depends on rejection riding the `result` channel.
    - The four `Guard` ids appear with their requirement mapping: `OptionRatioRange`,
      `LegSpanBelowSpacing`, `TickOutOfBounds`, and `GUARD-01`, `GUARD-02`, `GUARD-03`.
    - It contains `.github/workflows/develop-gate.yml` and a sentence stating that adding this
      file to the `--skip` ledger is forbidden.
    - `grep -c 'Phase 11' notes/DIFFERENTIAL_LAYOUT.md` >= 2 and the document contains `CI-04`.
    - **Still-open items are still open:** the open-decisions table contains `Phase 4`,
      `Phase 6` and `RPC-02` with `Phase 5`; the document does NOT pick a wire format
      (`grep -qiE 'wire format.*(is|will be) (tagged|per-variant)' "$F"` returns nothing) and
      does NOT pick an oracle packaging
      (`grep -qiE 'packaging.*(is|will be) (a new cabal exe|a mode on)' "$F"` returns nothing).
    - The open-questions table contains `try`/`catch` and `no known prior art`, and the
      open-risks table contains `UNPINNED` and `insert-phase`.
    - `grep -qF 'max_test_rejects' notes/DIFFERENTIAL_LAYOUT.md` succeeds — the no-`vm.assume`
      rule carries its reason.
    - `grep -qF 'REGRESSION FLOOR' notes/DIFFERENTIAL_LAYOUT.md` succeeds.
    - `grep -qF 'notes/DIFFERENTIAL_LAYOUT.md' test/protocol_integrations/SpecHelper.sol`
      succeeds — the cross-reference written in plan 01-02 now resolves to a real file.
    - `grep -qF 'notes/DIFFERENTIAL_LAYOUT.md' test/protocol_integrations/VolOrderToPanopticTokenId.diff.t.sol`
      succeeds — same, from the test header.
  </acceptance_criteria>
  <done>`notes/DIFFERENTIAL_LAYOUT.md` records layout, naming, the generated-interface boundary,
  the measured transport constraints with their measurements attached, the three-outcome contract
  and the channel rule that makes it achievable, the probe lifecycle including probe-once, and
  the per-phase extension points — while leaving the wire format, the oracle packaging and
  RPC-02 open with their owners, and recording the try/catch question, the no-prior-art round
  trip and the unpinned-Foundry risk as open.</done>
</task>

<task type="auto">
  <name>Task 2: Add the test-vol-order-tokenid-diff Makefile target</name>
  <files>/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets/Makefile</files>
  <read_first>
    - `Makefile` lines 185-225 — the `test-vol-order-return`, `test-vol-order-diff` and
      `test-vol-order-fixture` targets and the `.PHONY` line at 224. Copy their comment style
      (a paragraph saying what the target covers and what it does NOT) and their recipe form
      exactly.
    - `Makefile` lines 110-150 — `test-realized-vol` and the VegaIssuance target, the two other
      `--match-path '...diff.t.sol'` recipes.
  </read_first>
  <action>
Append a target to `Makefile`, immediately after the existing `test-vol-order-diff` target
block, following that block's exact style:

```make
# test-vol-order-tokenid-diff: the Haskell<->Plank volOrderToTokenId DIFFERENTIAL (RED-01..RED-06).
# One input driven into BOTH spec/src/Panoptic/NId.hs::volOrderToTokenId and the Plank map
# vol_order_to_panoptic_token_id (via VolOrderToPanopticTokenIdHarness.plk), asserted equal at
# tolerance 0. Distinct from test-vol-order-diff, which is the pos_spec MODULE sequence
# differential against a Solidity mock -- different subject, different oracle, different doctrine
# (there the mock is disposable; here NEITHER SIDE IS SACROSANCT).
# UNTIL PHASE 7 THIS IS SKIP-GUARDED and reports SKIP: SpecOracle.volOrderToTokenId is a stub
# that reverts, SpecOracle.health() reports TransportFailure, setUp caches that once, and the two
# differential tests self-skip on it. That is the intended RED state, not a failure. The two
# evidence tests (test_specHelper_stubRevertsAndProbeReportsNotWired, test_implSide_answersOnAnchor)
# DO run and must PASS -- without them, "everything skipped" and "the file is inert" would look
# identical in the log.
# NOTE: develop-gate is the only build environment for this repo; this target is a convenience
# entry point, not the validation path.
test-vol-order-tokenid-diff:
	forge test --match-path 'test/protocol_integrations/VolOrderToPanopticTokenId.diff.t.sol' --via-ir --optimize
```

Then add `test-vol-order-tokenid-diff` to the `.PHONY` list on the line that currently ends with
`test-vol-order-fixture test-vol-order-acceptance` (Makefile line 224). Append it to that same
line; do not create a second `.PHONY` line.

Recipe lines MUST be indented with a literal TAB, not spaces — a Make recipe indented with
spaces fails with "missing separator". Verify with `grep -P '^\t' Makefile` after editing.

DO NOT RUN `make`. Adding the target is the deliverable; executing it is not, and cannot be —
submodules are uninitialized locally by design.

Then commit both files:

```bash
cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets
git add notes/DIFFERENTIAL_LAYOUT.md Makefile
git commit -m "docs(01-04): differential layout, generated-interface boundary and measured transport constraints, plus a make target (RED-06)"
```

Do NOT push — plan 01-05 owns the push and the PR.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets && grep -q '^test-vol-order-tokenid-diff:' Makefile && grep -A1 '^test-vol-order-tokenid-diff:' Makefile | tail -1 | grep -qP '^\tforge test --match-path' && grep '^\.PHONY' Makefile | grep -q 'test-vol-order-tokenid-diff' && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^test-vol-order-tokenid-diff:' Makefile` returns exactly `1`.
    - The recipe line immediately below it begins with a literal TAB character and contains
      `forge test --match-path 'test/protocol_integrations/VolOrderToPanopticTokenId.diff.t.sol' --via-ir --optimize`
      (check with `grep -A1 '^test-vol-order-tokenid-diff:' Makefile | tail -1 | cat -A | head -c 40`
      showing a leading `^I`).
    - `grep '^\.PHONY' Makefile | grep -c 'test-vol-order-tokenid-diff'` returns `1`, and
      `grep -c '^\.PHONY' Makefile` is unchanged from `develop`
      (`git show develop:Makefile | grep -c '^\.PHONY'` gives the same number).
    - The comment block above the target contains `NEITHER SIDE IS SACROSANCT`,
      `develop-gate is the only build environment`, `SpecOracle.health()` and both evidence test
      names.
    - `git diff develop -- Makefile` shows ONLY additions (no `^-` lines other than the
      rewritten `.PHONY` line): `git diff develop -- Makefile | grep -c '^-[^-]'` returns `1`.
    - `git -C /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets diff develop --name-only`
      now returns exactly four paths: `Makefile`, `notes/DIFFERENTIAL_LAYOUT.md`,
      `test/protocol_integrations/SpecHelper.sol`,
      `test/protocol_integrations/VolOrderToPanopticTokenId.diff.t.sol`.
    - `git diff develop -- .github/` is still EMPTY.
  </acceptance_criteria>
  <done>`make test-vol-order-tokenid-diff` exists as a tab-indented target with a comment
  distinguishing it from `test-vol-order-diff`, is declared `.PHONY`, and the branch diff is
  exactly four files with `.github/` untouched.</done>
</task>

</tasks>

<verification>
- All ten required headings present in `notes/DIFFERENTIAL_LAYOUT.md`, in order.
- Both source files' `notes/DIFFERENTIAL_LAYOUT.md` cross-references resolve to a real file.
- The transport is recorded as RESOLVED with the override visible; the generated-interface
  boundary is recorded; wire format, oracle packaging and RPC-02 remain OPEN with their owners.
- Every measured transport constraint carries its measurement, and the measurement scope
  (`forge 1.5.1-stable` `b0a9dd9`, unpinned in CI) is stated once at the top of that section.
- The Makefile target exists, is tab-indented, and is in `.PHONY`.
- Branch diff vs `develop` is exactly four files; `.github/` diff is empty.
</verification>

<success_criteria>
- RED-06: the organization — layout, naming, the generated-interface boundary and the
  Solidity↔spec transport boundary — is written down in a location both source files already
  cite, in enough detail that Phases 6-11 extend it rather than redesign it.
- No open decision belonging to Phase 4, 5 or 6 has been pre-resolved; the one decision that WAS
  taken elsewhere is recorded as such, with the override visible.
- No transport constraint appears without the measurement that justifies it.
</success_criteria>

<output>
After completion, create
`.planning/phases/01-red-differential-scaffold/01-04-SUMMARY.md`
recording the commit SHA, the final heading list of `notes/DIFFERENTIAL_LAYOUT.md`, the exact
four-file branch diff that plan 01-05 will open a PR for, and the open questions and risks the
document leaves for later phases (try/catch vs cheatcode, the non-Ethereum round trip, and the
unpinned Foundry toolchain).
</output>
