---
phase: 01-red-differential-scaffold
plan: 06
type: execute
wave: 6
depends_on: ["01-05"]
files_modified: []
autonomous: true
requirements: [RED-02, RED-03, RED-06, PROC-01]

must_haves:
  truths:
    - "develop's tree contains the doctrine header, the differential discipline, and the written layout/naming/transport-boundary description"
    - "develop's tree contains .planning/phases/FEATURES/feat-red-diff-scaffold/ resolving to the phase's plans"
    - "Phase 1 did not touch the gate's --skip ledger (CORRECTED: it is NOT byte-identical to the pre-phase state — Phase 1.1 retired *PriceSetterHook* in `12e1fb9`, taking it from four patterns to three. The claim to verify is that THIS phase's diff against develop for .github/ is empty, not that the ledger never changed.)"
    - "The regression floor file on develop is byte-identical to what it was before this phase"
    - "The Phase 1 tracking issue is closed and names the merge commit"
  artifacts:
    - path: "test/protocol_integrations/VolOrderToPanopticTokenId.diff.t.sol"
      provides: "On develop: the doctrine header and the differential discipline (criterion 4)"
      contains: "NEITHER SIDE SACROSANCT"
    - path: "notes/DIFFERENTIAL_LAYOUT.md"
      provides: "On develop: the written layout, naming and transport-boundary description (criterion 4)"
      contains: "transport boundary"
    - path: ".planning/phases/FEATURES/feat-red-diff-scaffold"
      provides: "On develop: the FEATURES layout (criterion 5)"
  key_links:
    - from: "develop"
      to: "feat/red-diff-scaffold"
      via: "merged pull request"
      pattern: "Merge pull request"
---

<objective>
Land the branch on `develop` and close the phase out. Phase 1's criterion 4 is a statement about
**the merged tree**, and criterion 5 about **`develop`** — neither is true while the work sits on
a branch.

Purpose: turn a green PR into the merged state the roadmap actually asked for, and verify the
claim against `develop` itself rather than against the branch that produced it.
Output: `develop` carrying all four files, the tracking issue closed, the feature worktree
retired.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-red-differential-scaffold/01-01-SUMMARY.md
@.planning/phases/01-red-differential-scaffold/01-05-SUMMARY.md
@.planning/phases/01-red-differential-scaffold/01-05-GATE-EVIDENCE.md

**Precondition:** plan 01-05's checkpoint returned "approved — merge". If it did not, this plan
does not run.

**Work directory for git operations: the MAIN worktree
`/home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets`, on `develop`.**

**NO LOCAL COMPILATION.** Do not run `forge`, `make`, `npm ci` or `cabal`. Every check below is
`git`, `grep` or `gh`.

**Merge style.** Recent history uses merge commits from PRs (`git log --oneline` shows
`Merge pull request #56 from JMSBPP/chore/consolidate-scripts-notes`). Preserve that: use
`gh pr merge --merge`, not squash or rebase.

**Fork → PR direction.** This merge is `feat/red-diff-scaffold` → `develop` **on the JMSBPP
fork**. Do NOT open or merge anything against `d2p-finance/cfmm-vol-markets` in this phase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge the PR into develop</name>
  <files>(no repo files — merges the PR on JMSBPP/cfmm-vol-markets)</files>
  <read_first>
    - `.planning/phases/01-red-differential-scaffold/01-05-SUMMARY.md` — the PR URL/number and
      the user's recorded resume signal. The maintainer answered the checkpoint with the single word "approved" — an unambiguous affirmative to a prompt that read "Type 'approved — merge' to authorize plan 01-06 to merge PR #60 into develop and close #57". Do NOT block on exact wording; record the response verbatim. Confirm an affirmative before doing
      anything; if it does not, stop.
    - `.planning/phases/01-red-differential-scaffold/01-05-GATE-EVIDENCE.md` — the run whose
      green result authorises this merge.
    - Run `git -C /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets log --oneline -6` to confirm
      the merge-commit style used on this repo.
  </read_first>
  <action>
1. Re-confirm the check is still green immediately before merging (a later push could have
   invalidated it):

```bash
gh pr checks --repo JMSBPP/cfmm-vol-markets feat/red-diff-scaffold
```

The `gate` check must read `pass`. If it does not, STOP and report.

2. Merge with a merge commit, preserving the repo's history style:

```bash
gh pr merge --repo JMSBPP/cfmm-vol-markets feat/red-diff-scaffold --merge --delete-branch=false
```

Keep the remote branch (`--delete-branch=false`): the branch name is the phase's identifier in
the roadmap and the tracking issue, and Phase 2 branches from `develop`, not from it.

3. Update the main worktree:

```bash
cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets
git fetch origin
git checkout develop
git pull --ff-only origin develop
```

Record the merge commit SHA.
  </action>
  <verify>
    <automated>gh pr view --repo JMSBPP/cfmm-vol-markets feat/red-diff-scaffold --json state,mergedAt -q '.state' | grep -qx MERGED && git -C /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets fetch origin -q && git -C /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets cat-file -e origin/develop:test/protocol_integrations/VolOrderToPanopticTokenId.diff.t.sol && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `gh pr view --repo JMSBPP/cfmm-vol-markets feat/red-diff-scaffold --json state -q .state`
      returns `MERGED`.
    - `git log origin/develop --oneline -1` starts with `Merge pull request` (merge-commit
      style preserved, not squashed).
    - `git -C /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets rev-parse develop` equals
      `git rev-parse origin/develop` (main worktree is up to date, fast-forward only).
    - `git ls-remote --heads origin feat/red-diff-scaffold` still returns one ref (the branch
      was not deleted).
  </acceptance_criteria>
  <done>The PR is merged into `develop` with a merge commit, and the main worktree is
  fast-forwarded to it.</done>
</task>

<task type="auto">
  <name>Task 2: Verify the merged tree against criteria 4 and 5, close the issue, retire the worktree</name>
  <files>(no repo files — verification, issue closure, worktree removal)</files>
  <read_first>
    - `.planning/ROADMAP.md` Phase 1 Success Criteria 4 and 5 — verbatim; this task checks the
      merged tree against exactly those words, not against a paraphrase.
    - `.planning/phases/01-red-differential-scaffold/01-01-SUMMARY.md` — the tracking issue
      number to close.
    - Re-read `notes/DIFFERENTIAL_LAYOUT.md` **as it now stands on develop** (via
      `git show origin/develop:notes/DIFFERENTIAL_LAYOUT.md`) before asserting criterion 4 —
      verify the merged content, not the branch content you remember writing.
  </read_first>
  <action>
1. Verify criterion 4 against `origin/develop` directly, using `git show` so nothing is read
   from a stale working tree:

```bash
cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets
D=test/protocol_integrations/VolOrderToPanopticTokenId.diff.t.sol

git show origin/develop:$D | grep -c 'NEITHER SIDE SACROSANCT'          # >= 1
git show origin/develop:$D | grep -c 'adjudicated case by case'          # >= 1
git show origin/develop:$D | grep -c 'never filtered with vm.assume'     # >= 1
git show origin/develop:$D | grep -c 'every fuzz names a non-fuzz anchor'# >= 1
git show origin/develop:$D | grep -c 'non-vacuity is ASSERTED via a live counter'  # >= 1
git show origin/develop:$D | grep -c 'vm.assume'                         # == 0
git show origin/develop:notes/DIFFERENTIAL_LAYOUT.md | grep -c '^## '    # == 10
git show origin/develop:test/protocol_integrations/SpecHelper.sol | grep -c 'SpecOracleNotWired'  # >= 2
```

2. Verify criterion 5 against `origin/develop`:

```bash
git ls-tree origin/develop .planning/phases/FEATURES/     # lists README.md and the symlink
git ls-tree origin/develop .planning/phases/FEATURES/feat-red-diff-scaffold | grep 120000
git ls-tree -r origin/develop --name-only | grep '01-red-differential-scaffold/01-0.*-PLAN.md' | wc -l   # >= 6
```

3. Verify the two must-not-change files are byte-identical on `develop` to their pre-phase state.
   Take the pre-phase SHA as the parent of the merge on the `develop` side:

```bash
BASE=$(git rev-parse origin/develop^1)
git diff "$BASE" origin/develop -- .github/workflows/develop-gate.yml    # MUST be empty
git diff "$BASE" origin/develop -- test/protocol_integrations/VolOrderToPanopticTokenId.t.sol  # MUST be empty
git show origin/develop:.github/workflows/develop-gate.yml | grep -c 'PanopticVegaLens'  # == 1, ledger intact
git show origin/develop:.github/workflows/develop-gate.yml | grep -c 'diff.t.sol'        # == 0, our file is NOT on the ledger
```

If ANY of these disagrees, STOP and report — do not close the issue on a tree that does not
match the criteria.

4. Close the tracking issue with the merge SHA:

```bash
gh issue close <ISSUE_NUMBER> --repo JMSBPP/cfmm-vol-markets \
  --comment "Merged as <MERGE_SHA>. Gate evidence: .planning/phases/01-red-differential-scaffold/01-05-GATE-EVIDENCE.md. Criteria 1-3 from the gate run, 4-5 verified against origin/develop. The differential is compiled and reported SKIP by its own wiring probe; the gate --skip ledger was not touched. Wiring: Phase 7. Enforcement: Phase 11 (CI-04)."
```

5. Retire the branch — **CORRECTED: there is no feature worktree to remove.** Phases went inline
   after this plan was written; the `red-diff-scaffold` worktree was collapsed into the main tree,
   which is itself checked out on `feat/red-diff-scaffold`. `git worktree list` shows ONE entry.
   The inline teardown is: return the tree to `develop`, then delete the merged branch.

```bash
cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets
git checkout develop && git pull --ff-only origin develop
# only after confirming the branch is fully merged:
git branch --merged origin/develop | grep -q 'feat/red-diff-scaffold' \
  && git branch -d feat/red-diff-scaffold \
  && git push origin --delete feat/red-diff-scaffold
```

If `git branch -d` refuses (unmerged commits), do NOT use `-D` — inspect and report. Unmerged
commits mean something did not reach `develop`.

NOTE the working tree carries pre-existing dirt that is NOT yours and must survive the checkout:
seven drifted `lib/*` submodule pins (left by a local `forge test`), plus `offchain`, `spec`,
`src/modules/premium/DynamicFeeMod.plk`, `TODO.md`, `.planning/config.json`. Do not stash, reset,
clean or commit any of them.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets && git fetch origin -q && D=test/protocol_integrations/VolOrderToPanopticTokenId.diff.t.sol && git show origin/develop:$D | grep -q 'NEITHER SIDE SACROSANCT' && [ "$(git show origin/develop:$D | grep -c 'vm.assume')" = "0" ] && [ "$(git show origin/develop:.github/workflows/develop-gate.yml | grep -c 'diff.t.sol')" = "0" ] && git ls-tree origin/develop .planning/phases/FEATURES/feat-red-diff-scaffold | grep -q 120000 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git show origin/develop:test/protocol_integrations/VolOrderToPanopticTokenId.diff.t.sol`
      contains all five doctrine/discipline strings: `NEITHER SIDE SACROSANCT`,
      `adjudicated case by case`, `never filtered with vm.assume`,
      `every fuzz names a non-fuzz anchor`, `non-vacuity is ASSERTED via a live counter`.
    - That same file, on `origin/develop`, has `grep -c 'vm.assume'` == `0`.
    - `git show origin/develop:notes/DIFFERENTIAL_LAYOUT.md | grep -c '^## '` returns `10`
      (the eight original sections plus `## The generated interface boundary` and
      `## Transport mechanics: measured constraints`, added when the transport was resolved).
    - `git show origin/develop:test/protocol_integrations/SpecHelper.sol | grep -c 'SpecOracleNotWired'`
      is at least `2` (declaration + revert).
    - `git ls-tree origin/develop .planning/phases/FEATURES/feat-red-diff-scaffold` shows mode
      `120000`, and `git ls-tree -r origin/develop --name-only | grep -c '01-red-differential-scaffold/01-0.*-PLAN.md'`
      is at least `6`.
    - `git diff origin/develop^1 origin/develop -- .github/workflows/develop-gate.yml` produces
      zero bytes.
    - `git diff origin/develop^1 origin/develop -- test/protocol_integrations/VolOrderToPanopticTokenId.t.sol`
      produces zero bytes.
    - `git show origin/develop:.github/workflows/develop-gate.yml | grep -c 'diff.t.sol'`
      returns `0` — the differential is NOT on the gate's `--skip` ledger.
    - `git show origin/develop:.github/workflows/develop-gate.yml | grep -c 'PanopticVegaLens'`
      returns `1` — the pre-existing ledger is intact, not rewritten.
    - `gh issue view <N> --repo JMSBPP/cfmm-vol-markets --json state -q .state` returns `CLOSED`,
      and its last comment contains the merge SHA.
    - `git -C /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets worktree list` no longer lists
      `red-diff-scaffold`.
  </acceptance_criteria>
  <done>`origin/develop` carries the doctrine, the discipline, the layout document and the
  FEATURES path; the gate ledger and the regression floor are provably unchanged; the tracking
  issue is closed naming the merge; the feature worktree is gone.</done>
</task>

</tasks>

<verification>
- `origin/develop` contains all four files with the required content.
- `origin/develop`'s `develop-gate.yml` and `VolOrderToPanopticTokenId.t.sol` are byte-identical
  to their pre-merge state.
- The differential path appears nowhere in the gate's `--skip` ledger.
- `.planning/phases/FEATURES/feat-red-diff-scaffold` is a tracked symlink on `develop` resolving
  to at least six PLAN files.
- The tracking issue is closed; the feature worktree is removed.
</verification>

<success_criteria>
- Phase 1 criteria 4 and 5 are true of `develop` itself, verified with `git show` against the
  merged tree rather than against the branch.
- The phase's process obligations are discharged: issue closed, worktree retired, merge-commit
  history style preserved.
- Phase 2 can branch from a `develop` that already carries the scaffold it must not break.
</success_criteria>

<output>
After completion, create
`.planning/phases/01-red-differential-scaffold/01-06-SUMMARY.md`
recording: the merge commit SHA, the closed issue number, the verified content checks against
`origin/develop`, and a one-line statement of what Phase 2 inherits (a compiling, probe-skipped
differential whose `SpecHelper` seam and `notes/DIFFERENTIAL_LAYOUT.md` boundary it must keep
intact while `VolOrder` becomes `VolOrder(T)`).
</output>
