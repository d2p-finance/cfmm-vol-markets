---
phase: 08-panoptic-vol-claim-lean4-formalization
plan: 03
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md
autonomous: false
requirements:
  - CTX-ECONO   # 08-CONTEXT: econometric blocks (Q_M regression, υ(t) linearization) as a derived model-spec markdown via the structural-econometrics skill — NOT Lean

must_haves:
  truths:
    - "The econometric identification of υ is captured as a structural model-spec markdown, separate from the Lean formalization"
    - "The spec covers the Q_M = Q_M(υ=0) + υ(t)·σ²(t) collateral regression and the υ(t) linearization"
    - "The ATM/OTM null hypothesis (Δυ/Δi maximal at the money, exp-decaying OTM) is stated as the testable hypothesis"
  artifacts:
    - path: "notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md"
      provides: "Reiss-Wolak structural econometric spec for υ identification via the Panoptic subgraph"
      contains: "upsilon"
  key_links:
    - from: "notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md"
      to: "spec/protocol/panoptic.md"
      via: "formalizes the ECONOMETRIC υ-identification section of the pinned spec"
      pattern: "panoptic"
---

<objective>
Produce the derived econometric model specification for υ identification — the econometric blocks
of `spec/protocol/panoptic.md` (the collateral regression Q_M = Q_M(υ=0) + υ(t)·σ²(t), the υ(t) tick
linearization, and the ATM/OTM null hypothesis) — by invoking the `structural-econometrics`
skill. This is a MARKDOWN model-spec artifact, explicitly NOT a Lean object (locked decision: the
econometric track is separate from the Lean analytical formalization).

Purpose: The spec's `# υ IDENTIFICATION / ## ECONOMETRIC` section describes a contract-level
regression on Panoptic-subgraph collateral data. That belongs in a Reiss-Wolak structural
specification (agents, environment, primitives, stochastic model, estimation steps), not in Lean.
Output: One dated spec markdown under `notes/structural-econometrcics/specs/`.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-panoptic-vol-claim-lean4-formalization/08-CONTEXT.md
@spec/protocol/panoptic.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Derive the υ-identification econometric spec via the structural-econometrics skill</name>
  <read_first>
    - spec/protocol/panoptic.md (the `# υ IDENTIFICATION / ## ECONOMETRIC` section — the source of the model)
    - .planning/phases/08-panoptic-vol-claim-lean4-formalization/08-CONTEXT.md (locked: econometric blocks are the skill's job, not Lean; ATM/OTM null hypothesis)
    - /home/jmsbpp/.claude/skills/structural-econometrics/SKILL.md (the skill contract — mandatory interactive questioning)
    - /home/jmsbpp/.claude/skills/structural-econometrics/resources.md (concept → canonical source registry)
  </read_first>
  <files>notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md</files>
  <action>
    Invoke the `structural-econometrics` skill (Skill tool / load
    /home/jmsbpp/.claude/skills/structural-econometrics/SKILL.md) to derive a complete Reiss-Wolak
    (2007) structural econometric specification for υ identification, seeded from the ECONOMETRIC
    section of `spec/protocol/panoptic.md`. The economic question to bring to the skill VERBATIM:

      "Identify the vega-like greek υ ≡ Δπ/Δσ² of a Panoptic vol-claim at the contract level by
       regressing per-position collateral Q_M on realized variance σ²(t) via
       Q_M = Q_M(υ=0) + υ(t)·σ²(t), with the tick linearization
       υ(t) = υ(ī) + (Δυ(t)/Δi(t))·i(t), estimated from the Panoptic subgraph (tokenId per
       Uniswap v3/v4 position). Null hypothesis: (Δυ(t)/Δi(t))(i_K) is maximal at the money and
       exponentially decreasing out of the money."

    The skill is INTERACTIVE — it will ask AskUserQuestion for equilibrium concept, unit of
    observation (per-tokenId vs per-pool), error sources (unobserved heterogeneity vs measurement
    error in subgraph data), functional form, and distributional assumptions. Answer each through
    the user; do NOT auto-decide. The skill writes its output to
    `notes/structural-econometrcics/specs/YYYY-MM-DD-<title>.md` — ensure the dated filename is
    `2026-07-19-panoptic-upsilon-identification.md` (rename if the skill emits a different slug).

    SCOPE GUARD: This task produces the SPEC ONLY. No estimation, no subgraph data pull, no
    regression run (those are deferred — 08-CONTEXT Deferred Ideas). No Lean files are touched.
  </action>
  <verify>
    1. Confirm the artifact exists and is non-trivial:
       `test -f notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md && wc -l notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md`
       (expect a substantial multi-section document, not a stub).
    2. Confirm it covers the three required blocks:
       `grep -iE 'Q_M|collateral' <file>` (the collateral regression),
       `grep -iE 'linear' <file>` (the υ(t) linearization),
       `grep -iE 'at.the.money|ATM|null hypothesis' <file>` (the ATM/OTM null hypothesis).
    3. Confirm NO Lean object was created for the econometrics:
       `! ls lean/vol_markets/*Econom* 2>/dev/null` and the skill produced markdown only.
    4. Read the spec and confirm the three Reiss-Wolak stages (economic model, stochastic model,
       estimation steps) are present and the decisions match what you approved interactively.
  </verify>
  <acceptance_criteria>
    - `test -f notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md` exits 0
    - `grep -iq 'upsilon\|υ\|vega' notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md` matches
    - `grep -iq 'Q_M\|collateral' notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md` matches (collateral regression covered)
    - `grep -iq 'null hypothesis\|at the money\|ATM' notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md` matches (ATM/OTM hypothesis stated)
    - `git ls-files notes/... | wc -l` shows the file is tracked after commit, and `grep -rnE '/home/|\$HOME|~/' notes/structural-econometrcics/specs/` returns nothing (Phase-1 path rule)
    - No Lean file was created for the econometric blocks: `! ls lean/vol_markets/*Upsilon*Econom* 2>/dev/null`
  </acceptance_criteria>
  <done>The dated υ-identification econometric spec exists under notes/structural-econometrcics/specs/, covers the collateral regression, υ(t) linearization, and ATM/OTM null hypothesis across the three Reiss-Wolak stages, contains no home-absolute paths, and encodes nothing in the Lean tree.</done>
  <resume-signal>Type "approved" once the econometric spec covers the collateral regression, the υ(t) linearization, and the ATM/OTM null hypothesis across the three Reiss-Wolak stages — or describe what is missing.</resume-signal>
</task>

</tasks>

<verification>
- The dated econometric spec exists under `notes/structural-econometrcics/specs/` and is tracked
- No `/home/`, `$HOME`, or `~/` path appears in the artifact
- No Lean object encodes the econometric blocks (they remain markdown-only, per the locked decision)
</verification>

<success_criteria>
The econometric identification of υ (collateral regression + tick linearization + ATM/OTM null
hypothesis) is captured as a Reiss-Wolak structural model-spec markdown, produced through the
structural-econometrics skill with user-aligned decisions, and kept strictly out of the Lean tree.
</success_criteria>

<output>
After completion, create `.planning/phases/08-panoptic-vol-claim-lean4-formalization/08-03-SUMMARY.md`
</output>
