---
phase: 08-panoptic-vol-claim-lean4-formalization
plan: 05
type: execute
wave: 4
depends_on: ["08-02", "08-04"]
files_modified:
  - lean/vol_markets/Panoptic.lean
  - scratch/aristotle-panoptic-upsilon/lakefile.toml
  - scratch/aristotle-panoptic-upsilon/lean-toolchain
  - scratch/aristotle-panoptic-upsilon/lake-manifest.json
  - scratch/aristotle-panoptic-upsilon/RequestProject/Panoptic.lean
  - scratch/aristotle-panoptic-upsilon/RequestProject/PosSpec.lean
  - scratch/aristotle-panoptic-upsilon/RequestProject/Flow.lean
  - .gitignore
autonomous: false
requirements:
  - CTX-THETA-PROOF  # 08-CONTEXT: lattice→closed-form θ derivation via a NEW Aristotle project, strictly serial

must_haves:
  truths:
    - "The θ_ATM closed-form derivation is submitted to a NEW Aristotle project (not a continue of a legacy project)"
    - "The submission is strictly serial — no queued/parallel aristotle continue"
    - "The returned proof replaces the sorries in Panoptic.lean and vol_markets builds sorry-free for the θ theorem"
    - "The θ theorem carries no unexpected axiom (#print axioms shows only propext/Classical.choice/Quot.sound)"
  artifacts:
    - path: "scratch/aristotle-panoptic-upsilon/RequestProject/Panoptic.lean"
      provides: "Self-contained submission source carrying the sorry'd θ theorem + central-binomial sub-lemma"
      contains: "theta_atm_closed_form"
    - path: "lean/vol_markets/Panoptic.lean"
      provides: "θ_ATM closed-form theorem with the Aristotle-returned proof integrated (sorry removed)"
      contains: "theta_atm_closed_form"
  key_links:
    - from: "scratch/aristotle-panoptic-upsilon/"
      to: "aristotle submit"
      via: "aristotle submit --project-dir with the reference bundle layout"
      pattern: "aristotle submit"
    - from: "lean/vol_markets/Panoptic.lean"
      to: "Aristotle returned proof"
      via: "integrate downloaded proof, remove sorry, lake build vol_markets green"
      pattern: "theta_atm_closed_form"
---

<objective>
Discharge the phase's single hard proof obligation — the lattice→closed-form derivation
θ_ATM(τ) = kσ/√(8πτ) (and its load-bearing central-binomial asymptotic) — via a NEW Aristotle
project, submitted STRICTLY SERIALLY, then integrate the returned proof into Panoptic.lean and
verify it is sorry-free with no unexpected axiom.

Purpose: Mathlib has Stirling + Wallis but NOT the sharp central-binomial asymptotic
`C(2m,m) ~ 4^m/√(πm)` nor a discrete local-CLT; that gluing step must be assembled by Aristotle.
This is the heaviest obligation (locked: θ closed form is a THEOREM, not a definition).
Output: A sorry-free θ theorem in Panoptic.lean and a full `lake build` green.

CRITICAL SERIAL CONSTRAINT: Aristotle submissions are strictly serial. NEVER queue or run a second
`aristotle continue`/`submit` while a task is in flight — the `--files`/`--project-dir` upload
OVERWRITES the in-flight server-side proof (MEMORY: "Aristotle: no queue"). Submit Stage N+1 only
after Stage N's proof has landed locally and `lake build vol_markets` is green.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-panoptic-vol-claim-lean4-formalization/08-CONTEXT.md
@.planning/phases/08-panoptic-vol-claim-lean4-formalization/08-RESEARCH.md
@lean/vol_markets/Panoptic.lean
@spec/protocol/refs/cfmm-discrete/STREAMING_PREMIUM.md
@spec/protocol/refs/cfmm-discrete/FINANCE.md

<interfaces>
<!-- Reference Aristotle bundle layout (verified from lean/archive/arsitotleTaoCFMM.tar.gz):
<project>/lean-toolchain            → leanprover/lean4:v4.28.0  (copy lean/lean-toolchain)
<project>/lake-manifest.json       → copy lean/lake-manifest.json (Mathlib v4.28.0 pin)
<project>/lakefile.toml            → require mathlib rev v4.28.0; one [[lean_lib]] with globs
<project>/RequestProject/*.lean    → the source subdir (globs = ["RequestProject.+"])
-->

Sorry'd goals to prove (from lean/vol_markets/Panoptic.lean, created in 08-02):
```lean
theorem centralBinom_isEquivalent :
    Filter.Tendsto (fun m : ℕ => (Nat.centralBinom m : ℝ) * Real.sqrt (Real.pi * m) / 4 ^ m)
      Filter.atTop (nhds 1) := by sorry
theorem theta_atm_closed_form (k σ : ℝ) (Θ_ATM : ℝ → ℝ)
    (hΘ : ∀ τ, 0 < τ → Θ_ATM τ = k * σ / Real.sqrt (8 * Real.pi * τ)) :
    Filter.Tendsto (fun τ => Θ_ATM τ * Real.sqrt (8 * Real.pi * τ))
      (nhdsWithin 0 (Set.Ioi 0)) (nhds (k * σ)) := by sorry
```

Mathlib levers Aristotle should use (Pitfall 1): `Stirling.factorial_isEquivalent_stirling`,
`tendsto_stirlingSeq_sqrt_pi`, `Wallis.tendsto_W_nhds_pi_div_two`, `Nat.centralBinom`,
`centralBinom_eq_two_mul_choose`. The √(8π) = 2√(2π) constant comes from the standard-normal peak
φ(0)=1/√(2π); √τ = √(m·Δt).

API key: worktree `.env` (ARISTOTLE_API_KEY, gitignored). Read at call time; pass via
`--api-key "$ARISTOTLE_API_KEY"` or export the env var. NEVER print or commit the key.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the Aristotle submission bundle and submit to a NEW project</name>
  <read_first>
    - lean/vol_markets/Panoptic.lean (the sorry'd θ theorem + central-binomial sub-lemma to submit)
    - lean/vol_markets/PosSpec.lean and lean/vol_markets/Flow.lean (Panoptic's imports — needed in the bundle)
    - lean/lean-toolchain and lean/lake-manifest.json (pins to copy into the bundle verbatim)
    - .planning/phases/08-panoptic-vol-claim-lean4-formalization/08-RESEARCH.md (Pitfall 4: bundle must mirror the reference layout)
  </read_first>
  <files>scratch/aristotle-panoptic-upsilon/lakefile.toml, scratch/aristotle-panoptic-upsilon/lean-toolchain, scratch/aristotle-panoptic-upsilon/lake-manifest.json, scratch/aristotle-panoptic-upsilon/RequestProject/Panoptic.lean, scratch/aristotle-panoptic-upsilon/RequestProject/PosSpec.lean, scratch/aristotle-panoptic-upsilon/RequestProject/Flow.lean</files>
  <action>
    Build a self-contained submission bundle under `scratch/aristotle-panoptic-upsilon/` mirroring
    the verified reference layout (lean/archive/arsitotleTaoCFMM.tar.gz), then submit to a NEW
    Aristotle project. Do NOT `aristotle continue` any legacy project (uploads overwrite server
    files).

    1. **Assemble the bundle directory** `scratch/aristotle-panoptic-upsilon/`:
       - `lean-toolchain` — copy `lean/lean-toolchain` verbatim (must read `leanprover/lean4:v4.28.0`).
       - `lake-manifest.json` — copy `lean/lake-manifest.json` verbatim (Mathlib v4.28.0 pin).
       - `lakefile.toml` — write:
         ```toml
         name = "RequestProject"
         defaultTargets = ["RequestProject"]

         [[require]]
         name = "mathlib"
         git = "https://github.com/leanprover-community/mathlib4.git"
         rev = "v4.28.0"

         [[lean_lib]]
         name = "RequestProject"
         srcDir = "."
         globs = ["RequestProject.+"]
         ```
       - `RequestProject/PosSpec.lean` — copy `lean/vol_markets/PosSpec.lean`, changing the
         `namespace`/import references so the file is self-contained under `RequestProject` (rename
         `import vol_markets.PosSpec` occurrences to `import RequestProject.PosSpec`, keep the
         `namespace PosSpec`). If PosSpec has no vol_markets imports, copy as-is.
       - `RequestProject/Flow.lean` — copy `lean/vol_markets/Flow.lean`, rewriting
         `import vol_markets.PosSpec` → `import RequestProject.PosSpec`.
       - `RequestProject/Panoptic.lean` — copy `lean/vol_markets/Panoptic.lean`, rewriting
         `import vol_markets.PosSpec` → `import RequestProject.PosSpec` and
         `import vol_markets.Flow` → `import RequestProject.Flow`. This file carries the two `sorry`'d
         goals Aristotle must close (`centralBinom_isEquivalent`, `theta_atm_closed_form`).
       Ensure `scratch/` is gitignored or excluded from the doc commit (bundle is a build artifact,
       not tracked source) — verify with `git check-ignore scratch/` or add `scratch/` to
       `.gitignore` if not already covered. NO `~/`/`$HOME`/`/home/` path may appear inside any
       bundled file.

    2. **Submit to a NEW project** (read the key at call time; do not echo it):
       ```bash
       set +o history 2>/dev/null; export ARISTOTLE_API_KEY="$(grep -E '^ARISTOTLE_API_KEY=' .env | cut -d= -f2-)"
       aristotle submit \
         "Prove the two sorry'd theorems in RequestProject/Panoptic.lean: (1) centralBinom_isEquivalent — the sharp central-binomial asymptotic C(2m,m)·√(π m)/4^m → 1, derived from Mathlib's Stirling (factorial_isEquivalent_stirling / tendsto_stirlingSeq_sqrt_pi) and Wallis; (2) theta_atm_closed_form — the ATM lattice-theta closed form Θ_ATM(τ)=kσ/√(8πτ) as the τ→0⁺ limit, where √(8π)=2√(2π) is the standard-normal peak. Do not weaken the statements. Keep all other declarations unchanged." \
         --project-dir scratch/aristotle-panoptic-upsilon \
         --api-key "$ARISTOTLE_API_KEY"
       ```
       Capture the returned project id / task id from stdout into the SUMMARY. Do NOT pass `--wait`
       here if it would block the plan; the next task polls. If `--wait` is used, save the archive
       via `--destination scratch/aristotle-panoptic-upsilon-result.tar.gz`.

    3. Record the project/task id. This is the ONLY in-flight Aristotle task — do not submit again
       until it lands (Task 2 handles polling + integration).
  </action>
  <verify>
    <automated>test -f scratch/aristotle-panoptic-upsilon/lean-toolchain && grep -q 'v4.28.0' scratch/aristotle-panoptic-upsilon/lean-toolchain && test -f scratch/aristotle-panoptic-upsilon/RequestProject/Panoptic.lean && grep -q 'theta_atm_closed_form' scratch/aristotle-panoptic-upsilon/RequestProject/Panoptic.lean && ! grep -rnE '/home/|\$HOME' scratch/aristotle-panoptic-upsilon/</automated>
  </verify>
  <acceptance_criteria>
    - Bundle layout matches the reference: `test -f scratch/aristotle-panoptic-upsilon/{lakefile.toml,lean-toolchain,lake-manifest.json}` all exist
    - `grep -q 'leanprover/lean4:v4.28.0' scratch/aristotle-panoptic-upsilon/lean-toolchain` matches
    - `grep -q 'globs = \["RequestProject' scratch/aristotle-panoptic-upsilon/lakefile.toml` matches
    - `grep -q 'theta_atm_closed_form' scratch/aristotle-panoptic-upsilon/RequestProject/Panoptic.lean` AND `grep -q 'centralBinom_isEquivalent' …` match (both sorry'd goals present in the bundle)
    - Bundle imports are self-contained: `! grep -rn 'import vol_markets' scratch/aristotle-panoptic-upsilon/RequestProject/`
    - No secret leak: the API key does NOT appear in any tracked file or the SUMMARY: `! git grep -I 'ARISTOTLE_API_KEY=' -- ':!.gitignore'` returns nothing beyond the .env reference; `.env` remains gitignored (`git check-ignore .env` exits 0)
    - The submit command returned a project/task id (recorded in the SUMMARY)
  </acceptance_criteria>
  <done>A reference-layout Aristotle bundle carrying both sorry'd θ goals is assembled under scratch/ and submitted to a NEW project; the task id is recorded and it is the sole in-flight submission.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Poll until landed, integrate the returned proof, verify sorry-free and axiom-clean</name>
  <read_first>
    - lean/vol_markets/Panoptic.lean (the file the returned proof is integrated into)
    - .planning/phases/08-panoptic-vol-claim-lean4-formalization/08-05-SUMMARY.md is not yet written; read the Task-1 recorded project/task id from your working notes
    - .planning/phases/08-panoptic-vol-claim-lean4-formalization/08-VALIDATION.md (phase gate: no sorry, #print axioms clean)
  </read_first>
  <files>lean/vol_markets/Panoptic.lean</files>
  <action>
    Poll the in-flight Aristotle task to completion (NO parallel submissions), download the result,
    integrate the returned proof(s) into `lean/vol_markets/Panoptic.lean` replacing the two `sorry`s,
    and verify the θ theorem builds clean and axiom-clean.

    1. Poll (serial): `aristotle show <task-id> --api-key "$ARISTOTLE_API_KEY"` repeatedly until the
       task status is terminal (succeeded/failed). Do NOT submit or continue anything while polling.
    2. Download: `aristotle download <project-id> --api-key "$ARISTOTLE_API_KEY"` (or use the
       `--wait --destination` archive from Task 1). Extract the returned `RequestProject/Panoptic.lean`.
    3. Integrate: transplant the returned proof bodies for `centralBinom_isEquivalent` and
       `theta_atm_closed_form` into `lean/vol_markets/Panoptic.lean`, replacing the `sorry`s.
       Preserve the vol_markets imports (`import vol_markets.PosSpec`/`import vol_markets.Flow`) —
       do NOT keep the `RequestProject.*` import rewrites. If Aristotle introduced helper lemmas,
       bring them in as well (keeping them in Panoptic.lean).
    4. Build: `cd lean && lake build vol_markets` (must exit 0) then full `cd lean && lake build`
       (defaultTargets: exp, vol_markets, tao — the per-wave gate).
    5. Axiom check: confirm the θ theorem has no unexpected axiom. Add a temporary
       `#print axioms Panoptic.theta_atm_closed_form` (and `#print axioms Panoptic.centralBinom_isEquivalent`)
       and confirm output lists ONLY `propext`, `Classical.choice`, `Quot.sound` (no `sorryAx`).
    6. If Aristotle only closed ONE of the two goals (partial result — Open Q1 anticipated this):
       integrate the closed one, leave the other `sorry` with a clear TODO comment citing the
       task id, and record in the SUMMARY that a follow-up serial submission is needed. Do NOT
       auto-resubmit in this task.
  </action>
  <verify>
    1. `cd lean && lake build vol_markets` exits 0.
    2. `cd lean && lake build` exits 0 (full defaultTargets green).
    3. `grep -c 'sorry' lean/vol_markets/Panoptic.lean` returns 0 (both proofs integrated) — OR a
       documented partial state (exactly one sorry with a TODO + task id) is explicitly approved.
    4. `#print axioms Panoptic.theta_atm_closed_form` prints no `sorryAx` and no unexpected axiom.
    5. Read the integrated proof and confirm the theorem statements were NOT weakened from the
       08-02 forms (same signatures).
  </verify>
  <acceptance_criteria>
    - `cd lean && lake build vol_markets` exits 0 with no `error:`
    - `cd lean && lake build` exits 0 (full suite green)
    - `grep -c 'sorry' lean/vol_markets/Panoptic.lean` returns 0 (fully integrated) OR the SUMMARY documents an approved partial (exactly 1 sorry + TODO + Aristotle task id)
    - `#print axioms Panoptic.theta_atm_closed_form` output contains no `sorryAx` (checked via a transient build; remove the `#print` line before final commit if desired)
    - The θ theorem signature is unchanged from 08-02: `grep -q 'theorem theta_atm_closed_form' lean/vol_markets/Panoptic.lean` still matches with the same parameters
    - No API key in tracked files: `git check-ignore .env` exits 0 and `! git grep -I 'sk-\|ARISTOTLE_API_KEY=[^$]'` finds no committed key
  </acceptance_criteria>
  <done>The Aristotle-returned proof is integrated into Panoptic.lean, the θ theorem is sorry-free with a clean #print axioms, and the full lake build is green (or an explicitly-approved documented partial with a follow-up serial submission recorded).</done>
  <resume-signal>Type "approved" once `lake build` is green and the θ theorem is sorry-free with a clean `#print axioms` — or describe the partial/failed Aristotle result and whether a follow-up serial submission is authorized.</resume-signal>
</task>

</tasks>

<verification>
- `cd lean && lake build` exits 0 (full defaultTargets green)
- `grep -c 'sorry' lean/vol_markets/Panoptic.lean` returns 0 (or a documented, approved partial)
- `#print axioms Panoptic.theta_atm_closed_form` shows only propext/Classical.choice/Quot.sound
- The Aristotle project was a NEW submission (not a legacy continue); no parallel/queued submits occurred
</verification>

<success_criteria>
The lattice→closed-form θ_ATM derivation is proved by a new, strictly-serial Aristotle project and
integrated into Panoptic.lean so the θ theorem carries no `sorry` and no unexpected axiom, with the
full `lake build` green — completing the phase's heaviest proof obligation.
</success_criteria>

<output>
After completion, create `.planning/phases/08-panoptic-vol-claim-lean4-formalization/08-05-SUMMARY.md`
</output>
