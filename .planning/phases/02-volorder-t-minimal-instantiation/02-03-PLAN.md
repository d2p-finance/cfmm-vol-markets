---
phase: 02-volorder-t-minimal-instantiation
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - .planning/phases/02-volorder-t-minimal-instantiation/02-REGRESSION-ASSESSMENT.md
autonomous: false
requirements: [VORD-01, VORD-02, VORD-03]

must_haves:
  truths:
    - "A recorded regression assessment enumerates EVERY .plk and .sol file coupled to the concrete VolOrder shape (the 12 .plk / 19 .sol census from CONTEXT.md, re-measured) and classifies each as survives-untouched, mechanical-call-site-update, or elimination-candidate"
    - "The evidence bar is written into the document: survives-untouched requires an empty git diff AND a green run of that file in the phase gate run — both, filled in by plan 02-05"
    - "The two --skip'd files are classified from the 02-02 probe measurement — UNVERIFIED with reason if red, or survives-once-unmasked if green — never counted as evidence by being masked"
    - "The elimination bucket is empty, or the executor has HALTED and brought the maintainer the file, why it is coupled, and what would replace it"
    - "The refactor design (none tag, explicit comptime T, bytes(T) extra, the exact edit list) is written down and the maintainer has approved the classification at a blocking checkpoint before plan 02-04 starts"
  artifacts:
    - path: ".planning/phases/02-volorder-t-minimal-instantiation/02-REGRESSION-ASSESSMENT.md"
      provides: "Criterion 1 — the assessment with classification table, evidence columns, design record and decision log"
      contains: "| survives untouched |"
      min_lines: 120
  key_links:
    - from: "02-REGRESSION-ASSESSMENT.md classification rows"
      to: "test/protocol_integrations/VolOrderToPanopticTokenId.t.sol"
      via: "row classified survives-untouched with evidence = empty diff + green in gate run"
      pattern: "VolOrderToPanopticTokenId\\.t\\.sol"
    - from: "02-REGRESSION-ASSESSMENT.md"
      to: "02-02-SKIP-PROBE.md"
      via: "the UNVERIFIED / green-once-unmasked rows cite the probe run"
      pattern: "02-02-SKIP-PROBE"
---

## ⛔ EXECUTION GATE — READ BEFORE STARTING
This plan is executed by the **superpowers INLINE executor (`/superpowers:executing-plans`) in the maintainer's own session**. `gsd-executor`, `Agent`-spawned subagents, `Workflow` runs, and any other background or autonomous agent are **BARRED** from executing it. If you are a background agent reading this: **STOP and report that you were dispatched against a gated plan.** Reason (maintainer, verbatim intent): this phase is heavy on user intervention across test writing, implementation and design, so the decisions belong in a session the maintainer is present in.

<objective>
Write the regression assessment — Phase 2 criterion 1 — and get the maintainer's approval of it
BEFORE any VolOrder source moves. The assessment enumerates every file coupled to the concrete
`VolOrder` shape, classifies each, states the evidence each classification will need, records the
refactor design that makes the prediction ("elimination bucket EMPTY") true by construction, and
ends in a blocking checkpoint.

Purpose: the ROADMAP is explicit — "the decision is the deliverable"; criterion 4 makes every
eliminated or rewritten test traceable to a decision here; criterion 3's "no edits" is only
meaningful because this document says WHICH files were expected to be untouched, in advance.
Output: `02-REGRESSION-ASSESSMENT.md` on the branch, approved.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-volorder-t-minimal-instantiation/02-CONTEXT.md
@.planning/phases/02-volorder-t-minimal-instantiation/02-01-GATE-EVIDENCE.md
@.planning/phases/02-volorder-t-minimal-instantiation/02-02-ABI-EDGE-BASELINE.md
@.planning/phases/02-volorder-t-minimal-instantiation/02-02-SKIP-PROBE.md
@src/types/pos_spec/VolOrder.plk
@src/lib/protocol_integrations/PanopticTokenIdSetterLib.plk
@src/models/mev_tax_model_one/libraries/Shock.plk
@test/pos_spec/VolOrderDecoder.sol
@test/mocks/VolOrderRefMock.sol

**Census measured at planning time (2026-08-28) — the executor RE-MEASURES it in Task 1, and any
difference is reported, not smoothed.**

`.plk` files whose NON-COMMENT lines mention `VolOrder` as a TYPE (annotation or struct literal) —
these are the mechanical-update candidates:
- `src/types/pos_spec/VolOrder.plk` — the definition (line 17) + 16 type/literal sites (27, 34, 39,
  44, 50, 58, 65, 83, 84, 93, 94, 103, 104, 112, 113, 122).
- `src/lib/protocol_integrations/PanopticTokenIdSetterLib.plk` — import line 2; signatures at
  21 (`vol_order_to_panoptic_token_id`), 99 (`vol_order_leg_split`), 124
  (`position_size_for_target_vega`), 141 (`vol_order_to_mint`), 161 (`induced_leg_liquidities`),
  176 (`average_density_chunks`).
- `src/lib/pos_spec/VolOrderValidationLib.plk` — 69/70 (`build_vol_order`), 86 (`validate_order`),
  93 (`validate_order_strict`).
`.plk` files that import from `VolOrder.plk` but never name the type (they call functions whose
types are inferred) — predicted SURVIVES UNTOUCHED:
- `src/modules/pos_spec/VolOrderManagerMod.plk` (imports `pack_vol_order`, `build_vol_order`)
- `test/protocol_integrations/VolOrderMintSizingHarness.plk` (imports `unpack_vol_order`, calls
  `vol_order_to_mint` / `induced_leg_liquidities` / `average_density_chunks`)
- `test/types/pos_spec/VolOrderHelper.plk`, `test/types/pos_spec/VolOrderValidationHarness.plk`
  (wildcard imports; call setters / `build_vol_order` / `pack`/`unpack`)
- `test/protocol_integrations/VolOrderToPanopticTokenIdHarness.plk` — imports `unpack_vol_order`,
  `tick_bucket_from_vol_order`, `vol_order_to_panoptic_token_id`. **Predicted MECHANICAL** (one
  internal call-site update: the tokenId path gains `comptime T`), with the EXTERNAL surface frozen
  (02-02 stamp).
`.plk` files that mention VolOrder ONLY in comments (predicted survives untouched, trivially):
`src/lib/events/VolEventsLib.plk`, `src/interfaces/pos_spec/VolOrderManagerInterface.plk`,
`src/types/pos_spec/VegaTarget.plk`, `src/lib/exposure/VegaIssuanceLib.plk` (commented-out sketch),
`src/types/protocol_integrations/PanopticTokenId.plk`, `src/types/pricing/TickUtils.plk`,
`src/lib/ldf/LDFLib.plk`, `test/lib/ldf/GeometricWeightsHarness.plk`.
`src/modules/VolOrderManagerMod.plk` (NOT the `pos_spec/` one) defines its OWN unrelated
`const VolOrder = struct { volTarget, rangeWidth, skew }` and does not import `pos_spec::VolOrder` —
out of scope, note it so nobody "fixes" it.

`.sol` files (19) — they reach Plank ONLY through packed `u256` words and `.plk` harnesses (the ABI
edge), so ALL are predicted SURVIVES UNTOUCHED: `test/protocol_integrations/VolOrderToPanopticTokenId.t.sol`
(regression floor, NO EDITS), `…/VolOrderToPanopticTokenId.diff.t.sol` (must still compile+skip),
`…/SpecHelper.sol`, `…/VolOrderMintSizing.t.sol`, `…/DynamicFeeHookE2E.t.sol`,
`test/events/VolOrderCreatedEvent.t.sol`, `test/pos_spec/VolOrderDecoder.sol`,
`test/pos_spec/VolOrderManager.diff.t.sol`, `test/pos_spec/VolOrderTargetVega.t.sol`,
`test/pos_spec/VolOrderManagerBatch.t.sol`, `test/pos_spec/VolOrderManager.t.sol`,
`test/pos_spec/VolOrderManagerFixture.t.sol`, `test/mocks/VolOrderRefMock.sol`,
`test/types/pos_spec/VolOrder.t.sol`, `test/types/pos_spec/VolOrderValidation.t.sol`,
`src/modules/VolOrderManager.s.sol`, `foundry-scripts/VolOrderManager.s.sol`,
`foundry-scripts/deploy/DeployDynamicFeeHook.s.sol`, `foundry-scripts/deploy/DeployVolOrderManagerMod.s.sol`.
Plus the two masked ones: `test/types/pos_spec/VolRangeWidth.t.sol`,
`test/types/pos_spec/SpreadTickAssimetryHelper.t.sol` — classified from the 02-02 probe.

`.sol` → `.plk` harness map (which Solidity suite exercises which Plank entrypoint), from
`grep -rn '\.plk"' test/`: VolOrder.t.sol → VolOrderHelper.plk; VolOrderValidation.t.sol →
VolOrderValidationHarness.plk; VolOrderToPanopticTokenId(.diff).t.sol →
VolOrderToPanopticTokenIdHarness.plk; VolOrderMintSizing.t.sol → VolOrderMintSizingHarness.plk;
VolOrderManager*.t.sol / VolOrderTargetVega.t.sol → src/modules/pos_spec/VolOrderManagerMod.plk;
GeometricWeights.t.sol → GeometricWeightsHarness.plk.

**Design decisions this assessment records (Claude's discretion per CONTEXT.md, exercised here so
the maintainer approves them at the checkpoint together with the classification):**
1. **Local `none` tag, not upstream `ctime`.** `bytes(ctime)` collapses to `cbytes`, a
   comptime-only type; carrying it as a runtime struct field is an unverified corner of the
   compiler. A local `const none = struct {};` in `VolOrder.plk` is structurally identical to how
   `std/regions.plk` declares every region (empty struct), costs nothing, and lets `VolOrder(none)`
   simply OMIT the `extra` field — so reading `.extra` on the minimal instantiation is a
   missing-field COMPILE error with no `@compile_error` plumbing to get wrong. `ctime` is noted as
   the upstream synonym; Phase 4 may map `none` ↔ `ctime` if `@type_index` wants it.
2. **Explicit `comptime T: type` threading, not `$T` inference.** VORD-01 says "following the
   in-repo `Shock(R)` pattern", and `Shock`/`ShockLib` thread `comptime R` explicitly. `$T` on a
   struct-typed argument would infer the WHOLE `VolOrder(none)` type, and recovering `T` from it
   needs the `@field_index`/`@field_type` gymnastics `std/option.plk:unwrap` does — more surface
   for #307-class shadowing bugs, for no gain on a path with exactly one instantiation this phase.
3. **`extra: bytes(T)`** (the `Shock(R)` shape: a `{ptr, length}` view into region `T`) for
   `T ∈ {memory, calldata}`; absent for `none`. Phase 3 defines the payload layout inside the view.
4. **`vol_order_base(comptime T, vo: VolOrder(T)) VolOrder(none)`** — the one region-agnostic
   projection (identity for `none`), so the generic tokenId body reads the four fields through it
   and the ONLY `T`-dependent read in the future is `extra`. Mirrors `shock_load_word` being the
   single region-branching site.
5. **Genericity boundary (locked by CONTEXT.md):** only `vol_order_to_panoptic_token_id` takes
   `comptime T`. `vol_order_to_mint`, `vol_order_leg_split`, `position_size_for_target_vega`,
   `induced_leg_liquidities`, `average_density_chunks`, `pack`/`unpack`, the setters,
   `tick_bucket_from_vol_order`, `VolOrderValidationLib`, `VolOrderManagerMod`, `VolEventsLib`,
   `VegaIssuanceLib` are all `VolOrder(none)`.
6. **The concept, named (maintainer asked):** a phantom type parameter used as a region index —
   region polymorphism supplying a compile-time capability/witness (Haskell: the `s` in `ST s` /
   `runST`). Distinguished from "parse, don't validate": provenance goes into the type, the data
   stays where it lies — which is exactly why `pack`/`unpack` stay agnostic.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Re-measure the census and write 02-REGRESSION-ASSESSMENT.md</name>
  <files>.planning/phases/02-volorder-t-minimal-instantiation/02-REGRESSION-ASSESSMENT.md</files>
  <read_first>
    - `.planning/ROADMAP.md` §"Phase 2" — the "Regression assessment comes first" paragraph and
      criteria 1, 3, 4 (quote them as the document's frame).
    - `.planning/phases/02-volorder-t-minimal-instantiation/02-CONTEXT.md` — §decisions in full
      (evidence bar; UNVERIFIED rule; elimination bucket predicted empty; HARD STOP rule).
    - `.planning/phases/02-volorder-t-minimal-instantiation/02-02-SKIP-PROBE.md` — the two suites'
      measured status.
    - `.planning/phases/02-volorder-t-minimal-instantiation/02-02-ABI-EDGE-BASELINE.md` — the stamp.
    - `src/types/pos_spec/VolOrder.plk` (all 128 lines), `src/lib/protocol_integrations/PanopticTokenIdSetterLib.plk`
      (all), `src/lib/pos_spec/VolOrderValidationLib.plk` lines 60-100,
      `test/protocol_integrations/VolOrderToPanopticTokenIdHarness.plk` (all),
      `test/protocol_integrations/VolOrderMintSizingHarness.plk` (all).
    - `src/models/mev_tax_model_one/libraries/Shock.plk` — the template being followed.
    - `test/pos_spec/VolOrderDecoder.sol` lines 1-32 and `test/mocks/VolOrderRefMock.sol` lines
      1-40 — the two files CONTEXT.md names as coupled to the WORD LAYOUT (unchanged), to justify
      their survives-untouched row from their own NatSpec.
    - `TODO.md` lines 48-75 — the maintainer's design note; quote the two sentences about `T`
      pointing at a region and pack/unpack staying agnostic.
    - The upstream std at the pin (do NOT assume syntax):
      `gh api /repos/plankevm/plank-monorepo/contents/std/regions.plk?ref=00c0a1aa3cb40b63de81c6ca4f92bec392b423c3 -q .content | base64 -d`
      and the same for `std/option.plk` — the `Option(T)` / `Some(value: $T)` precedent.
  </read_first>
  <action>
1. Re-measure the census on the branch (post-bump, pre-refactor) and keep the raw output for the
   appendix:

```bash
cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets
S=/tmp/claude-1000/-home-jmsbpp-cfmms-playground-cfmm-wt-vol-markets/efac9730-d3fe-4dd1-9b4c-7a6c916056fb/scratchpad
git diff origin/develop --stat -- src/ test/ | tail -1      # only the 02-01 std moves so far
# .plk files referencing the type or its functions
grep -rlE 'VolOrder|vol_order_' src/ test/ --include=*.plk | sort > $S/census-plk.txt; wc -l $S/census-plk.txt
# NON-COMMENT type/literal sites (the mechanical-update surface). Comments (lines starting with //)
# are stripped first so a doc comment naming VolOrder cannot inflate the count.
grep -rnE 'VolOrder([^a-zA-Z_(]|$)' src/ test/ --include=*.plk | grep -vE '^[^:]+:[0-9]+:\s*//' | grep -v 'src/modules/VolOrderManagerMod.plk' > $S/census-plk-type-sites.txt; wc -l $S/census-plk-type-sites.txt
# .sol consumers
grep -rlE 'VolOrder|volOrder|vol_order' test/ src/ foundry-scripts/ --include=*.sol | sort > $S/census-sol.txt; wc -l $S/census-sol.txt
# .sol -> .plk harness map
grep -rnE '\.plk"' test/ --include=*.t.sol | sed -E 's/^([^:]+):.*"([^"]+\.plk)".*/\1 -> \2/' | sort -u > $S/census-harness-map.txt
# what the two ledger globs mask
git ls-files | grep -iE 'VolRangeWidth|SpreadTickAssimetryHelper' > $S/census-masked.txt
```

Compare against the planning-time census in <context>: 12 `.plk` files, 19 `.sol` files. If either
count differs, the document's table uses the RE-MEASURED list and the appendix names the delta.

2. Write `.planning/phases/02-volorder-t-minimal-instantiation/02-REGRESSION-ASSESSMENT.md` with
   EXACTLY these sections (`## ` headings, in this order):

```markdown
# Phase 2 regression assessment — VolOrder → VolOrder(T)

**Status:** DRAFT — awaiting maintainer approval (plan 02-03 checkpoint) | APPROVED <date> | FINAL (02-05)
**Tree assessed:** `<HEAD sha>` on `feat/volorder-t-minimal` (plank `00c0a1a…`, forge `b0a9dd9…`)
**Frame (ROADMAP Phase 2, verbatim):** <quote the "Regression assessment comes first" paragraph and criteria 1, 3, 4>

## 1. Evidence bar
- **survives untouched** = `git diff origin/develop -- <file>` is EMPTY **and** the file's suite
  (or the entrypoint that reaches it) ran GREEN in the phase gate run — BOTH. Filled by 02-05
  with the run id and the quoted `Suite result` / `OK` line. A masked or never-executed file
  never counts.
- **mechanical call-site update** = the file's diff consists ONLY of the enumerated edits in §4
  (type annotation `VolOrder` → `VolOrder(none)`, struct literal `VolOrder {` → `VolOrder(none) {`,
  import additions, and the ONE `vol_order_to_panoptic_token_id(none, …)` call-site change) and
  its suite is green in the gate run.
- **elimination candidate** = a test whose assertions depend on the OLD concrete shape in a way
  no mechanical update preserves. **PREDICTED EMPTY.** Any entry here is a HARD STOP: the
  executor halts and brings the maintainer (a) the file, (b) why it is coupled, (c) what would
  replace it. A non-empty bucket also falsifies §4's design prediction and is reported as such.
- **UNVERIFIED (reason)** = cannot meet the bar because the gate never runs it (the two `--skip`
  entries) — classified from the 02-02 probe, never from being masked.

## 2. Classification — `.plk` (<N> files)
| File | Coupling (what it touches) | Predicted class | Evidence (02-05 fills) |
|---|---|---|---|
| `src/types/pos_spec/VolOrder.plk` | definition + 16 type/literal sites | mechanical | diff = §4 edits only; suites: VolOrder.t.sol, VolOrderValidation.t.sol green |
| `src/lib/protocol_integrations/PanopticTokenIdSetterLib.plk` | 6 signatures; tokenId path | mechanical (+ the one generic signature) | … |
| `src/lib/pos_spec/VolOrderValidationLib.plk` | 4 sites | mechanical | … |
| `test/protocol_integrations/VolOrderToPanopticTokenIdHarness.plk` | 1 call site; FROZEN edge | mechanical (internal only; ABI edge stamp equal) | 02-02 stamp vs 02-04 stamp |
| `src/modules/pos_spec/VolOrderManagerMod.plk` | pack_vol_order / build_vol_order, inferred types | survives untouched | … |
| `test/protocol_integrations/VolOrderMintSizingHarness.plk` | unpack + vol_order_to_mint etc., inferred | survives untouched | … |
| `test/types/pos_spec/VolOrderHelper.plk` | wildcard import, setters | survives untouched | … |
| `test/types/pos_spec/VolOrderValidationHarness.plk` | wildcard import, build/validate | survives untouched | … |
| <each comment-only file> | comment mention only | survives untouched (trivial) | diff empty |
| `src/modules/VolOrderManagerMod.plk` | its OWN unrelated `VolOrder` struct | OUT OF SCOPE — do not touch | diff empty |

## 3. Classification — `.sol` (<N> files + 2 masked)
| File | Reaches Plank via | Predicted class | Evidence (02-05 fills) |
|---|---|---|---|
| `test/protocol_integrations/VolOrderToPanopticTokenId.t.sol` | harness ABI (4 selectors) | survives untouched — **NO EDITS (criterion 3)** | diff empty; `Ran 10 tests … Suite result: ok` |
| `test/protocol_integrations/VolOrderToPanopticTokenId.diff.t.sol` | same harness + SpecHelper | survives untouched; must still compile AND skip on `SKIP_REASON` | diff empty; 2× `[SKIP: spec oracle not wired…]` + 2× `[PASS]` |
| `test/protocol_integrations/SpecHelper.sol` | none (seam) | survives untouched | diff empty; compiles |
| `test/pos_spec/VolOrderDecoder.sol` | packed WORD layout (unchanged by design: extra never enters the word) | survives untouched | diff empty; VolOrderManager*.t.sol green |
| `test/mocks/VolOrderRefMock.sol` | independent oracle of the registry SPEC; by charter never mirrors the module's encoding | survives untouched | diff empty; VolOrderManager.diff.t.sol green |
| <every other .sol> | … | survives untouched | … |
| `test/types/pos_spec/VolRangeWidth.t.sol` | masked by `*VolRangeWidth*` | <from 02-02: GREEN-once-unmasked (retirement offered at checkpoint) / UNVERIFIED — red on seed 4880: <n> failing, see #<PROBE_ISSUE>> | probe run <id> |
| `test/types/pos_spec/SpreadTickAssimetryHelper.t.sol` | masked by `*SpreadTickAssimetryHelper*` | <same pattern> | probe run <id> |

## 4. The refactor design that makes §2-3 true by construction
<Decisions 1-6 from the plan context, written out, each with its reason. Then the EXACT edit
list plan 02-04 executes — file:line → new text — for VolOrder.plk (the `none` tag, the
`VolOrder = fn (comptime T: type) type` constructor with the two struct returns and the
`@compile_error` fallthrough, `vol_order_base`, every `VolOrder` → `VolOrder(none)`),
PanopticTokenIdSetterLib.plk (import `{VolOrder, none, vol_order_base, tick_bucket_from_vol_order}`;
`vol_order_to_panoptic_token_id = fn (comptime T: type, vo: VolOrder(T), pool_id: u256)`; body
reads `let base = vol_order_base(T, vo);` and uses `base.` for the four fields; the five other
signatures → `VolOrder(none)`; `vol_order_to_mint` calls `vol_order_to_panoptic_token_id(none, vo, pool_id)`),
VolOrderValidationLib.plk (4 sites → `VolOrder(none)`), the harness (import `none`; the ONE call
`vol_order_to_panoptic_token_id(none, vo, @evm_calldataload(36))`).>
<Quote TODO.md's two governing sentences and name the concept (decision 6).>
<State the upstream facts read from std at the pin: regions are empty structs; `region_ptr_type`
`@compile_error`s on unknown regions; `bytes(ctime)` → `cbytes`; `Option(T)` / `Some($T)`.>

## 5. Elimination candidates
NONE PREDICTED. <If any: the HARD STOP record — file, coupling, replacement, and "HALTED, awaiting maintainer".>

## 6. Decisions for the checkpoint
1. Approve the classification in §2-3 (or name the row that is wrong).
2. Approve design decisions 1-6 in §4 (or name the one to change).
3. `--skip` retirement: <only if both probe results are green> re-apply `<PROBE_SHA>` in plan 02-05
   (retiring both ledger entries + updating the gate's ledger comment) — YES / NO / separate PR.
4. Confirm the HARD STOP rule: any elimination candidate found during 02-04 halts the plan.

## 7. Decision log (maintainer, verbatim)
<filled at the checkpoint — the maintainer's resume-signal text, verbatim, with date>

## Appendix A — census as measured
<the raw outputs of step 1: counts, the type-site list, the harness map, the masked-file list, and
the delta vs the planning-time census (12 .plk / 19 .sol) if any>
```

Rules for the tables: one row per file in the re-measured census — no file omitted; the
"Evidence" column contains the LITERAL string `(02-05 fills)` on every row that is not yet
evidenced, so an unfilled row is greppable later.

3. Commit and push (the document rides the branch to `develop`):

```bash
git add .planning/phases/02-volorder-t-minimal-instantiation/02-REGRESSION-ASSESSMENT.md
git commit -m "docs(02-03): regression assessment for VolOrder(T) — classification, evidence bar, design (DRAFT for checkpoint)"
git push origin feat/volorder-t-minimal
```
  </action>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets &amp;&amp; F=.planning/phases/02-volorder-t-minimal-instantiation/02-REGRESSION-ASSESSMENT.md &amp;&amp; [ "$(wc -l < "$F")" -ge 120 ] &amp;&amp; [ "$(grep -c '^## ' "$F")" -ge 8 ] &amp;&amp; grep -q '| survives untouched' "$F" &amp;&amp; grep -q 'VolOrderToPanopticTokenId.t.sol' "$F" &amp;&amp; grep -q 'VolRangeWidth.t.sol' "$F" &amp;&amp; grep -q 'HARD STOP' "$F" &amp;&amp; grep -q 'vol_order_base' "$F" &amp;&amp; grep -q 'const none = struct' "$F" &amp;&amp; grep -q '02-02-SKIP-PROBE' "$F" &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The file exists, `wc -l` ≥ 120, and `grep -c '^## '` ≥ 8 (sections 1-7 + Appendix A).
    - Every file in the re-measured census appears as a table row: for each path `p` in `$S/census-plk.txt` and `$S/census-sol.txt`, `grep -c "$p" "$F"` ≥ 1 (script it: `while read p; do grep -q "$p" "$F" || echo "MISSING $p"; done < $S/census-plk.txt` prints nothing; same for `.sol`).
    - `grep -c '| survives untouched' "$F"` ≥ 20; `grep -c '| mechanical' "$F"` = 4 (VolOrder.plk, PanopticTokenIdSetterLib.plk, VolOrderValidationLib.plk, the harness); `grep -c 'OUT OF SCOPE' "$F"` = 1 (the unrelated `src/modules/VolOrderManagerMod.plk`).
    - The §5 section contains either the exact string `NONE PREDICTED` with no table rows after it, or the string `HALTED, awaiting maintainer` — in which case the executor STOPS here and does not proceed to Task 2's approval question 1 as a yes.
    - `grep -c 'NO EDITS (criterion 3)' "$F"` = 1 on the `VolOrderToPanopticTokenId.t.sol` row; `grep -c 'still compile AND skip' "$F"` = 1 on the `.diff.t.sol` row.
    - Both masked files' rows cite the probe: `grep -E 'VolRangeWidth\.t\.sol.*(GREEN-once-unmasked|UNVERIFIED)' "$F"` and `grep -E 'SpreadTickAssimetryHelper\.t\.sol.*(GREEN-once-unmasked|UNVERIFIED)' "$F"` each match exactly one line.
    - §4 contains the literal fragments `const none = struct {};`, `fn (comptime T: type) type`, `@compile_error(`, `vol_order_base`, `vol_order_to_panoptic_token_id(none,`, and `VolOrder(none)`; and names the concept: `grep -c 'phantom type' "$F"` ≥ 1 and `grep -c 'ST s' "$F"` ≥ 1.
    - `grep -c '(02-05 fills)' "$F"` ≥ 20 (evidence columns left explicitly unfilled — no fabricated evidence before the run exists).
    - `grep -c '^\*\*Status:\*\* DRAFT' "$F"` = 1 before the checkpoint.
    - `git log origin/feat/volorder-t-minimal -1 --name-only | grep -c 02-REGRESSION-ASSESSMENT.md` = 1 (pushed).
  </acceptance_criteria>
  <done>The assessment exists on the branch with every coupled file classified, the evidence bar
  and design written down, the elimination bucket empty (or a HARD STOP recorded), and every
  evidence cell explicitly awaiting the gate run.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Maintainer approves the classification and the design before any code moves</name>
  <files>.planning/phases/02-volorder-t-minimal-instantiation/02-REGRESSION-ASSESSMENT.md</files>
  <read_first>
    - `.planning/phases/02-volorder-t-minimal-instantiation/02-REGRESSION-ASSESSMENT.md` — in
      full; you are asking the maintainer to approve claims you wrote.
    - `.planning/phases/02-volorder-t-minimal-instantiation/02-02-SKIP-PROBE.md` — so question 3
      is asked from the measurement, not from memory.
  </read_first>
  <action>
Do no further automation. Present to the maintainer, in this order, then HALT:
1. The §2 and §3 tables verbatim (they are the deliverable).
2. §5 (elimination candidates) — say explicitly "NONE" or present the HARD STOP record.
3. §4's design decisions 1-6 as six one-line bullets, and the exact-edit list.
4. The probe result from 02-02 in one line each for the two suites, and whether question 3 is
   open (both green) or moot (any red).
5. The four questions in §6.

On the answer: write the maintainer's resume-signal text VERBATIM (not normalised) with the date
into §7 "Decision log"; flip the Status line to `APPROVED <date>` only if question 1 AND 2 are
approved; if any row/decision is rejected, record the rejection verbatim, fix the document, and
re-present — plan 02-04 does not start until the Status line reads APPROVED. Record the question-3
answer as one of `RETIRE-IN-02-05`, `RETIRE-SEPARATE-PR`, `KEEP-MASKED`, or `MOOT (red)`.

Commit the updated document by name (`docs(02-03): checkpoint decision recorded verbatim`), push.
Then hand-edit STATE.md (add the decision under §Decisions as "[Phase 02, plan 03 checkpoint] …
verbatim …"; update Session Continuity) and ROADMAP.md (tick `02-03-PLAN.md`);
`grep -c '^milestone: v1.0$' .planning/STATE.md` → 1 before and after; commit the two files by
name; push.
  </action>
  <decision>Approve the regression classification (§2-3), the refactor design (§4), the --skip retirement question (§6.3), and the HARD STOP rule — before plan 02-04 edits any VolOrder source.</decision>
  <context>ROADMAP Phase 2: "the decisions are recorded with their rationale before any code moves"; criterion 4 traces every eliminated/rewritten test to a decision here. CONTEXT.md predicts the elimination bucket empty and makes any candidate a hard stop.</context>
  <options>
    <option id="approve">
      <name>Approve as written</name>
      <pros>02-04 starts with the classification and edit list fixed; every later "survives untouched" claim has a row it must satisfy.</pros>
      <cons>Any row later proven wrong is a documented misprediction (which is the point — it is reported, not absorbed).</cons>
    </option>
    <option id="amend">
      <name>Amend a row or a design decision (name it)</name>
      <pros>The document is corrected before code moves.</pros>
      <cons>One more round before 02-04.</cons>
    </option>
    <option id="halt">
      <name>Halt — an elimination candidate or a design objection needs a separate discussion</name>
      <pros>Nothing is refactored on a disputed premise.</pros>
      <cons>Phase 2 pauses.</cons>
    </option>
  </options>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets &amp;&amp; F=.planning/phases/02-volorder-t-minimal-instantiation/02-REGRESSION-ASSESSMENT.md &amp;&amp; grep -q '^\*\*Status:\*\* APPROVED' "$F" &amp;&amp; grep -A6 '^## 7\.' "$F" | grep -qE '(RETIRE-IN-02-05|RETIRE-SEPARATE-PR|KEEP-MASKED|MOOT)' &amp;&amp; [ "$(grep -c '^milestone: v1.0$' .planning/STATE.md)" -eq 1 ] &amp;&amp; grep -q '\[x\] 02-03-PLAN.md' .planning/ROADMAP.md &amp;&amp; echo APPROVED</automated>
  </verify>
  <resume-signal>Reply "approve", or "amend: <row or decision> — <what to change>", or "halt: <reason>". For question 3 (only if both probe results were green) add one of RETIRE-IN-02-05 / RETIRE-SEPARATE-PR / KEEP-MASKED.</resume-signal>
  <acceptance_criteria>
    - `grep -c '^\*\*Status:\*\* APPROVED' "$F"` prints `1` (and `DRAFT` no longer appears on that line).
    - §7 contains the maintainer's reply verbatim and a date: `grep -A8 '^## 7\.' "$F" | grep -cE '20[0-9]{2}-[0-9]{2}-[0-9]{2}'` ≥ 1.
    - §7 records the question-3 outcome as exactly one of `RETIRE-IN-02-05`, `RETIRE-SEPARATE-PR`, `KEEP-MASKED`, `MOOT (red)`.
    - §5 does NOT contain `HALTED, awaiting maintainer` (if it did, this checkpoint cannot be passed with "approve" — the maintainer's resolution must be written into §5 and §7 first).
    - STATE.md §Decisions has a new entry beginning `- **[Phase 02, plan 03 checkpoint]`; `grep -c '^milestone: v1.0$' .planning/STATE.md` prints `1`; ROADMAP has `[x] 02-03-PLAN.md`.
    - All of the above is pushed: `git status --porcelain --untracked-files=no -- .planning/` prints nothing and `git log origin/feat/volorder-t-minimal..HEAD` is empty.
  </acceptance_criteria>
  <done>The maintainer has approved — verbatim, on record — which files may change and how, and
  which may not, before the first VolOrder edit.</done>
</task>

</tasks>

<verification>
- `02-REGRESSION-ASSESSMENT.md` is on the branch, Status APPROVED, every census file classified,
  elimination bucket empty, design decisions 1-6 recorded, §7 verbatim decision log present.
- The two masked suites are classified from the probe measurement, not from being masked.
- STATE.md carries the checkpoint decision; `milestone: v1.0` intact.
</verification>

<success_criteria>
- Phase 2 criterion 1 is satisfied in DRAFT form (evidence columns to be filled by 02-05 from the
  gate run), and the maintainer has agreed to the classification before code moves.
- Plan 02-04 has an exact edit list and a hard stop rule.
</success_criteria>

<output>
After completion, create
`.planning/phases/02-volorder-t-minimal-instantiation/02-03-SUMMARY.md`
recording: the census counts as re-measured (and any delta vs 12/19), the classification counts
per bucket, the six design decisions in one line each, the maintainer's resume signal verbatim,
and the question-3 outcome token.
</output>
