---
phase: 02-volorder-t-minimal-instantiation
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - .planning/phases/02-volorder-t-minimal-instantiation/02-REGRESSION-ASSESSMENT.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
  - .planning/REQUIREMENTS.md
autonomous: false
requirements: [VORD-01, VORD-02, VORD-03]

must_haves:
  truths:
    - "Every 'survives untouched' row of the regression assessment carries BOTH halves of its evidence: a 0-byte diff against origin/develop AND the quoted green line from the phase gate run — no '(02-05 fills)' cell remains"
    - "The PR from feat/volorder-t-minimal is merged into develop on JMSBPP/cfmm-vol-markets with a merge commit, after branch protection was re-measured immediately before merging"
    - "The five Phase 2 criteria are verified against origin/develop (git show), not against the branch"
    - "The branch is retired locally and on origin with git branch -d (never -D) after git log origin/develop..feat/volorder-t-minimal is empty; the tracking issue is closed naming the merge SHA"
    - "STATE.md, ROADMAP.md and REQUIREMENTS.md are hand-edited (VORD-01..03 ticked), proven with git diff, milestone: v1.0 intact"
  artifacts:
    - path: ".planning/phases/02-volorder-t-minimal-instantiation/02-REGRESSION-ASSESSMENT.md"
      provides: "Criterion 1 FINAL — every row evidenced from the gate run"
      contains: "**Status:** FINAL"
    - path: ".planning/REQUIREMENTS.md"
      provides: "VORD-01, VORD-02, VORD-03 ticked and Complete in traceability"
      contains: "[x] **VORD-03**"
  key_links:
    - from: "develop"
      to: "feat/volorder-t-minimal"
      via: "merged pull request (merge commit)"
      pattern: "Merge pull request"
    - from: "02-REGRESSION-ASSESSMENT.md evidence cells"
      to: "02-04-GATE-EVIDENCE.md"
      via: "run id + quoted Suite result lines"
      pattern: "Suite result: ok"
---

## ⛔ EXECUTION GATE — READ BEFORE STARTING

> **CORRECTED 2026-08-28.** Six references in this plan described the design rejected at the 02-04
> chunk review (a local `none` tag, `vol_order_base`, `extra: bytes(T)`). They were not merely
> stale prose — one is the PR-body text, one an acceptance criterion that greps `STATE.md` for a
> line naming `none`, and two tell Phase 3 what it inherits. All six now state the design of record
> (`02-REGRESSION-ASSESSMENT.md` §4a). Evidence for the `(02-05 fills)` cells is run `33182346548`;
> the `--skip` retirement question is **MOOT (red)**, see `02-02-SKIP-PROBE.md` and #63.
This plan is executed by the **superpowers INLINE executor (`/superpowers:executing-plans`) in the maintainer's own session**. `gsd-executor`, `Agent`-spawned subagents, `Workflow` runs, and any other background or autonomous agent are **BARRED** from executing it. If you are a background agent reading this: **STOP and report that you were dispatched against a gated plan.** Reason (maintainer, verbatim intent): this phase is heavy on user intervention across test writing, implementation and design, so the decisions belong in a session the maintainer is present in.

<objective>
Close Phase 2. Finalise the regression assessment with the gate evidence (criterion 1 becomes
FINAL, criterion 4's traceability is checked against an empty elimination bucket), apply the
maintainer's `--skip` decision if it was `RETIRE-IN-02-05`, mark the PR ready, re-measure branch
protection, merge, verify the five criteria against `origin/develop`, retire the branch, close the
issue, and hand-edit the planning files.

Purpose: the criteria are statements about `develop`; nothing is true while it sits on a branch.
Output: merged PR; FINAL assessment; closed issue; retired branch; VORD-01..03 ticked.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-volorder-t-minimal-instantiation/02-REGRESSION-ASSESSMENT.md
@.planning/phases/02-volorder-t-minimal-instantiation/02-04-GATE-EVIDENCE.md
@.planning/phases/02-volorder-t-minimal-instantiation/02-02-SKIP-PROBE.md
@.planning/phases/01-red-differential-scaffold/01-06-PLAN.md
@.planning/STATE.md

**Branch protection is re-measured in Task 2, never assumed.** At planning time:
`required_status_checks.contexts: ["gate"]`, `required_pull_request_reviews: null`,
`enforce_admins: false` — i.e. NO review requirement. Phase 1 wrote two plans against a review
requirement the API does not report; do not write a third. If the live reading shows a review
requirement, record it and ask the maintainer — do not use `--admin` on an assumption.

**Never stage** the dirty list (02-01 context). Name every file on every `git add`. NO local build.

**STATE.md / ROADMAP.md / REQUIREMENTS.md are edited BY HAND.** The `gsd-tools` state and roadmap
verbs write zero bytes and/or corrupt `milestone: v1.0` → `v1.8`. Check
`grep -c '^milestone: v1.0$' .planning/STATE.md` → `1` before and after every edit.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Finalise the assessment from the gate evidence; apply the --skip decision if RETIRE-IN-02-05; mark the PR ready</name>
  <files>
    .planning/phases/02-volorder-t-minimal-instantiation/02-REGRESSION-ASSESSMENT.md
    .github/workflows/develop-gate.yml
    .github/workflows/push-build.yml
  </files>
  <read_first>
    - `02-REGRESSION-ASSESSMENT.md` — every row with `(02-05 fills)`; §6.3 / §7 for the
      `--skip` decision token (`RETIRE-IN-02-05` / `RETIRE-SEPARATE-PR` / `KEEP-MASKED` / `MOOT (red)`).
    - `02-04-GATE-EVIDENCE.md` — GATE_REF id and the quoted lines; and the forge log itself:
      `gh run view <GATE_REF> -R JMSBPP/cfmm-vol-markets --log --job <forge job id>` for the
      `Suite result` line of EVERY suite named in the assessment tables.
    - `02-02-SKIP-PROBE.md` — `PROBE_SHA` (only needed if RETIRE-IN-02-05).
    - `.github/workflows/develop-gate.yml` lines 140-149 — the ledger comment that must be
      rewritten if the two entries are retired.
  </read_first>
  <action>
1. Fill every evidence cell. For each `.sol` row: the diff half is
   `git diff origin/develop -- <file> | wc -c` → `0`; the run half is the quoted
   `Suite result: ok. N passed; 0 failed; K skipped` line for that file's suite from the GATE_REF
   forge log (for `SpecHelper.sol`, `VolOrderDecoder.sol`, `VolOrderRefMock.sol`, the `.s.sol`
   scripts and `src/modules/VolOrderManager.s.sol` — non-test files — the run half is "compiled in
   GATE_REF forge job" plus the suite that exercises them). For each `.plk` row: the diff half as
   above; the run half is the `   OK   <file>` line from the GATE_REF plank log for entrypoints,
   or the OK line of the entrypoint that imports it for libraries/types (name the entrypoint).
   For the four mechanical rows: the `git diff --numstat origin/develop -- <file>` figures and the
   green suite line. For the two masked rows: the probe run id and the decision token.

   Script the diff half so no row is hand-typed:
   ```bash
   cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets
   S=/tmp/claude-1000/-home-jmsbpp-cfmms-playground-cfmm-wt-vol-markets/efac9730-d3fe-4dd1-9b4c-7a6c916056fb/scratchpad
   for p in $(cat $S/census-plk.txt $S/census-sol.txt); do printf '%s %s\n' "$(git diff origin/develop -- "$p" | wc -c)" "$p"; done | sort -n
   # every line must be `0 <path>` EXCEPT the four mechanical files and the 02-01 std-move files
   ```
   (If `$S/census-*.txt` no longer exist, re-run the census commands from plan 02-03 Task 1 step 1.)

2. Flip the Status line to `**Status:** FINAL <date> — evidence from develop-gate <GATE_REF>`.
   §5 must still read `NONE PREDICTED` and now also `NONE FOUND (02-04)`; if 02-04 recorded a
   misprediction, write it in §5 with the maintainer's resolution verbatim (criterion 4
   traceability) — and if that resolution was never given, STOP: this plan cannot finalise an
   assessment with an unresolved elimination.

3. The `--skip` decision (§7 token):
   - `RETIRE-IN-02-05`: `git cherry-pick <PROBE_SHA>` (both workflows lose the two patterns), then
     in `develop-gate.yml` rewrite the ledger comment lines that describe `*VolRangeWidth* /
     *SpreadTickAssimetryHelper*` as "latent width-type fuzz bugs…" so the comment names ONLY the
     remaining entry (the lens) and records the retirement: `# (*VolRangeWidth* / *SpreadTickAssimetryHelper* retired
     in Phase 2 after probe run <GATE_PROBE> measured both green on plank 00c0a1a, seed 4880 -- #<PROBE_ISSUE>.)`
     — note: a comment line MAY name the globs UNQUOTED; it must NOT contain a `"*…*"` quoted glob.
     Run `bash scripts/check-ci-skip-ledger.sh` → `skip-ledger parity OK: 1 patterns, seed 4880`.
     Commit `ci(02-05): retire *VolRangeWidth* / *SpreadTickAssimetryHelper* from the --skip ledger (#<PROBE_ISSUE>)`
     naming the two workflow files. Push. Read the gate run: it must be green with the suite total
     INCREASED by exactly the two suites' counts recorded in 02-02-SKIP-PROBE.md and every other
     number unchanged; quote it in the assessment's §3 rows for the two files (now "survives
     untouched, unmasked in <run>").
   - `RETIRE-SEPARATE-PR`, `KEEP-MASKED`, `MOOT (red)`: no workflow edit; `git diff origin/develop --
     .github/workflows/develop-gate.yml | wc -c` stays `0`.

4. Commit the assessment (`docs(02-05): regression assessment FINAL — every row evidenced from gate <GATE_REF>`),
   push. Mark the PR ready and update its body with the actual shape of the diff (state what is
   true, not what the plan predicted):
   ```bash
   gh pr ready -R JMSBPP/cfmm-vol-markets feat/volorder-t-minimal
   gh pr view -R JMSBPP/cfmm-vol-markets feat/volorder-t-minimal --json files -q '[.files[].path]|sort|.[]' > $S/pr-files.txt; wc -l $S/pr-files.txt
   ```
   Body (via `gh pr edit --body-file`): `Closes #<ISSUE>`; one paragraph on what VolOrder(T) is and
   that absence is the std VALUE `Option`/`None` and the minimal instantiation is `VolOrder(calldata)` with `extra = None(Extra(calldata))`; a "Source diff" table listing the 4 refactor files, the
   8 std-move files, `.gitmodules` + `lib/plank-monorepo`, `Makefile` + `push-build.yml` (stamp
   step), `notes/TOOLCHAIN_PINS.md` §7, and — if retired — the two workflow ledger lines; a
   "Planning artifacts riding the branch" list (the evidence files, the assessment, the STATE/
   ROADMAP edits); "Untouched by design": `VolOrderToPanopticTokenId.t.sol`, `.diff.t.sol`,
   `SpecHelper.sol`, `develop-gate.yml` (unless retired — say so); and "What the gate run shows"
   quoting GATE_REF's suite line, the 10/10 floor, the two SKIP lines and `Selector set: IDENTICAL`.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets &amp;&amp; F=.planning/phases/02-volorder-t-minimal-instantiation/02-REGRESSION-ASSESSMENT.md &amp;&amp; grep -q '^\*\*Status:\*\* FINAL' "$F" &amp;&amp; [ "$(grep -c '(02-05 fills)' "$F")" -eq 0 ] &amp;&amp; grep -q 'NONE FOUND (02-04)' "$F" &amp;&amp; [ "$(grep -c 'Suite result: ok' "$F")" -ge 10 ] &amp;&amp; gh pr view -R JMSBPP/cfmm-vol-markets feat/volorder-t-minimal --json isDraft -q .isDraft | grep -qx false &amp;&amp; bash scripts/check-ci-skip-ledger.sh >/dev/null &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '(02-05 fills)' "$F"` = 0; `grep -c '^\*\*Status:\*\* FINAL' "$F"` = 1; `grep -c 'NONE FOUND (02-04)' "$F"` = 1 (or §5 carries a verbatim maintainer resolution and this criterion is recorded as amended in the SUMMARY).
    - `grep -c 'Suite result: ok' "$F"` ≥ 10 and `grep -c '   OK   ' "$F"` ≥ 5 (run-half evidence quoted for .sol suites and .plk entrypoints).
    - `grep -cE '\| 0 bytes' "$F"` ≥ 20 OR the appendix carries the scripted `0 <path>` listing with one line per untouched census file (the diff half is mechanical, not typed).
    - If `RETIRE-IN-02-05`: `bash scripts/check-ci-skip-ledger.sh` prints `skip-ledger parity OK: 1 patterns, seed 4880`; `grep -c '"\*VolRangeWidth\*"' .github/workflows/develop-gate.yml .github/workflows/push-build.yml` = 0; the gate run on the retirement commit is `success`; the assessment's two masked rows cite that run. Otherwise: `git diff origin/develop -- .github/workflows/develop-gate.yml | wc -c` = 0 and the parity script prints `3 patterns`.
    - `gh pr view -R JMSBPP/cfmm-vol-markets feat/volorder-t-minimal --json isDraft -q .isDraft` = `false`; the body contains `Closes #<ISSUE>`, `Selector set: IDENTICAL`, and the literal `VolOrderToPanopticTokenId.t.sol` under an "Untouched" heading.
    - `gh pr checks -R JMSBPP/cfmm-vol-markets feat/volorder-t-minimal` shows `gate` `pass` on the CURRENT head.
  </acceptance_criteria>
  <done>The assessment is FINAL with every row evidenced from a real run; the ledger decision is
  applied or explicitly deferred; the PR is ready with a body that describes the true diff.</done>
</task>

<task type="auto">
  <name>Task 2: Re-measure branch protection, merge, verify the five criteria against origin/develop</name>
  <files>(no repo files — the merge on JMSBPP/cfmm-vol-markets)</files>
  <read_first>
    - `.planning/phases/01-red-differential-scaffold/01-06-PLAN.md` — the merge precedent, and
      STATE.md's decision "`develop` has NO review requirement — the anticipated bypass was never used".
    - `.planning/ROADMAP.md` Phase 2 — the five criteria, verified below against `origin/develop`.
    - `gh pr view -R JMSBPP/cfmm-vol-markets feat/volorder-t-minimal --json mergeStateStatus,mergeable,reviewDecision`.
  </read_first>
  <action>
1. Re-measure IMMEDIATELY before merging — never carry a prior reading forward:
```bash
gh api /repos/JMSBPP/cfmm-vol-markets/branches/develop/protection -q '{contexts:.required_status_checks.contexts, strict:.required_status_checks.strict, reviews:.required_pull_request_reviews, enforce_admins:.enforce_admins.enabled}'
gh pr view -R JMSBPP/cfmm-vol-markets feat/volorder-t-minimal --json mergeStateStatus,mergeable,reviewDecision,headRefOid
```
Record the JSON verbatim in the SUMMARY. If `reviews` is non-null, STOP and ask the maintainer
how to proceed (do not `--admin` on your own). If `contexts` no longer contains `gate`, STOP —
something changed the protection and the phase's premise.

2. Merge with a MERGE COMMIT (the `#54/#55/#56/#59/#60` style), plain:
```bash
gh pr merge -R JMSBPP/cfmm-vol-markets feat/volorder-t-minimal --merge
git fetch origin develop
MERGE_SHA=$(git rev-parse origin/develop)
git log -1 --format='%H %s %ci' origin/develop      # subject must start with: Merge pull request #
```

3. Verify the five criteria AGAINST `origin/develop` (with `git show origin/develop:<path>` and
   the gate run that ran on the merged head's source — the last GATE run on the branch, whose
   head is `MERGE_SHA^2`; confirm `git diff MERGE_SHA^2 origin/develop -- src/ test/ .github/ Makefile | wc -c` → `0`
   so the run describes the merged SOURCE byte-for-byte):
```bash
# C1 — assessment on develop, FINAL, no unfilled cell, elimination bucket empty
git show origin/develop:.planning/phases/02-volorder-t-minimal-instantiation/02-REGRESSION-ASSESSMENT.md | grep -c '^\*\*Status:\*\* FINAL'      # 1
git show origin/develop:.planning/phases/02-volorder-t-minimal-instantiation/02-REGRESSION-ASSESSMENT.md | grep -c '(02-05 fills)'              # 0
# C2 — VolOrder(T) on develop, Shock-style
git show origin/develop:src/types/pos_spec/VolOrder.plk | grep -c '^const VolOrder = fn (comptime T: type) type {'                                  # 1
git show origin/develop:src/lib/protocol_integrations/PanopticTokenIdSetterLib.plk | grep -c 'fn (comptime T: type, vo: VolOrder(T), pool_id: u256)' # 1
# C3 — regression floor byte-identical to pre-phase develop
git diff b090b2e8cf026cf7a4ac4dad703ce772e36cf99e origin/develop -- test/protocol_integrations/VolOrderToPanopticTokenId.t.sol | wc -c              # 0
# C4 — nothing deleted/skipped: no test file removed, ledger not widened
git diff b090b2e8cf026cf7a4ac4dad703ce772e36cf99e origin/develop --diff-filter=D --name-only -- test/ | wc -l                                       # 0
git show origin/develop:.github/workflows/develop-gate.yml | grep -v '^\s*#' | grep -oE '"\*[^"]+\*"' | wc -l                                       # 3 (or 1 if retired) -- never more than 3
# C5 — differential + SpecHelper untouched since pre-phase develop
git diff b090b2e8cf026cf7a4ac4dad703ce772e36cf99e origin/develop -- test/protocol_integrations/VolOrderToPanopticTokenId.diff.t.sol test/protocol_integrations/SpecHelper.sol notes/DIFFERENTIAL_LAYOUT.md | wc -c   # 0
```
(`b090b2e` is the `develop` merge commit Phase 1 landed on — the pre-Phase-2 baseline for
"byte-identical" claims. Planning-time `develop` had since moved by `.planning/`-only commits.)
Quote each result in the SUMMARY next to its criterion, and cite the run id for the run-time half
(C2/C3/C5 green lines) from `02-04-GATE-EVIDENCE.md` (or the retirement run if that came later).

4. Comment on the tracking issue naming the merge SHA (the PR's `Closes #` auto-closes it; add the
   SHA explicitly), and confirm it is closed:
```bash
gh issue comment <ISSUE> -R JMSBPP/cfmm-vol-markets --body "Merged into develop as \`$MERGE_SHA\` (PR #<PR>). Criteria 1-5 verified against origin/develop — see 02-05-SUMMARY.md."
gh issue view <ISSUE> -R JMSBPP/cfmm-vol-markets --json state -q .state     # CLOSED
```
  </action>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets &amp;&amp; git fetch -q origin develop &amp;&amp; git log -1 --format=%s origin/develop | grep -q '^Merge pull request #' &amp;&amp; [ "$(git show origin/develop:src/types/pos_spec/VolOrder.plk | grep -c '^const VolOrder = fn (comptime T: type) type {')" -eq 1 ] &amp;&amp; [ "$(git diff b090b2e8cf026cf7a4ac4dad703ce772e36cf99e origin/develop -- test/protocol_integrations/VolOrderToPanopticTokenId.t.sol test/protocol_integrations/VolOrderToPanopticTokenId.diff.t.sol test/protocol_integrations/SpecHelper.sol | wc -c)" -eq 0 ] &amp;&amp; gh pr view -R JMSBPP/cfmm-vol-markets feat/volorder-t-minimal --json state -q .state | grep -qx MERGED &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - The SUMMARY quotes the live protection JSON taken immediately before the merge, and the merge used `--merge` with no `--admin` unless the maintainer explicitly authorised it in this session (quoted verbatim if so).
    - `gh pr view -R JMSBPP/cfmm-vol-markets feat/volorder-t-minimal --json state,mergeCommit -q '"\(.state) \(.mergeCommit.oid)"'` prints `MERGED <sha>` and that sha equals `git rev-parse origin/develop`.
    - `git log -1 --format=%s origin/develop` begins with `Merge pull request #`.
    - `git diff <MERGE_SHA>^2 origin/develop -- src/ test/ .github/ Makefile | wc -c` = 0 (the gate run on the branch head describes the merged source).
    - C1..C5 command outputs match the expected values listed in step 3 and are quoted in the SUMMARY with the run id backing each run-time claim.
    - `gh issue view <ISSUE> -R JMSBPP/cfmm-vol-markets --json state -q .state` = `CLOSED` and the issue has a comment containing the merge SHA.
  </acceptance_criteria>
  <done>The phase is on `develop` via a merge commit; each of the five criteria is verified
  against `origin/develop` with a quoted command result and a run id.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the branch; hand-edit STATE.md, ROADMAP.md, REQUIREMENTS.md; push the close-out to develop</name>
  <files>
    .planning/STATE.md
    .planning/ROADMAP.md
    .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - `AGENTS.md` §"Close the branch that did the merge" — the teardown rule (`-d`, never `-D`).
    - `.planning/STATE.md` — whole file; the frontmatter (`milestone: v1.0`, `progress:` block),
      "Current Position", "Accumulated Context → Decisions", "Session Continuity".
    - `.planning/ROADMAP.md` — the Phase 2 block and the Progress table row
      `| 2. VolOrder(T) Minimal Instantiation | 0/TBD | Not started | - |`.
    - `.planning/REQUIREMENTS.md` lines 21-23 (VORD-01..03 checkboxes) and the traceability rows
      `| VORD-0[123] | Phase 2 … | Pending |`.
    - `.planning/phases/02-volorder-t-minimal-instantiation/02-0[1-4]-SUMMARY.md` — the durations
      and facts the STATE.md metrics table and decisions absorb.
  </read_first>
  <action>
1. Return to `develop` and retire the branch:
```bash
cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets
git switch develop
git pull --ff-only origin develop
git log origin/develop..feat/volorder-t-minimal --oneline | wc -l     # MUST print 0
git branch -d feat/volorder-t-minimal                                  # -d ONLY. If it refuses: STOP and report; never -D
git push origin --delete feat/volorder-t-minimal
git ls-remote --heads origin feat/volorder-t-minimal | wc -l           # 0
git branch --list feat/volorder-t-minimal | wc -l                      # 0
```

2. Hand-edit the three planning files (check `grep -c '^milestone: v1.0$' .planning/STATE.md` → `1`
   BEFORE you start and AFTER you finish; if it ever reads anything else, restore from `git show HEAD:.planning/STATE.md`):
   - **REQUIREMENTS.md:** `- [ ] **VORD-01**` → `- [x] **VORD-01**`, same for VORD-02, VORD-03;
     traceability rows `| VORD-01 | Phase 2 — VolOrder(T) Minimal Instantiation | Pending |` →
     `… | Complete |` for all three.
   - **ROADMAP.md:** `- [ ] **Phase 2: VolOrder(T) Minimal Instantiation**` → `- [x] …`; the
     Phase 2 block's `**Plans**: 5 plans in 5 waves` (already set by the planner) and all five plan
     rows `[x]`; under each criterion append a `→ **VERIFIED ON origin/develop** (plan 05, merge <MERGE_SHA>): <one line quoting the check>`
     in the style of Phase 1's criteria 4-5; Progress table row → `| 2. VolOrder(T) Minimal Instantiation | 5/5 | Complete | <date> |`.
   - **STATE.md:** frontmatter `current_plan`, `status: phase-complete`, `stopped_at` (one
     paragraph: merge SHA, gate run id, the stamp verdict, the `--skip` decision token,
     `PROBE_ISSUE`), `last_updated`; `progress:` → `completed_phases: 3`, `total_plans: 17`,
     `completed_plans: 17`, `percent: 100` (with the same "read that 100% correctly" caveat);
     "Current Position" → Phase 2 COMPLETE, next Phase 3; Performance Metrics table gains one row
     per timed 02-0N plan; §Decisions gains: `[Phase 02]` the §4a design (absence is std `Option`/`None`, NOT a tag type; `Extra(T)` is a tagged descriptor `flags | offset | len` in `src/types/Extra.plk` carrying no tokenId), explicit
     `comptime T`, the stamp mechanism (push-build only, gate untouched), the probe result and its
     token, the `--evm-version` finding (taken or not), the `memptr`-in-v3 unreachable finding,
     and the protection reading at merge time; "Session Continuity" → next action
     `/gsd:plan-phase 3` with what Phase 3 inherits: `VolOrder(T)` carrying `extra: Option(Extra(T))`;
     `Extra(T)` a tagged descriptor (`flags` 248..255 | `offset` 216..247 | `len` 200..215) pointing at
     poolId 48 bits + 4 x 7-bit ratios = 76 bits packed, with `FLAG_PANOPTIC` the only flag defined;
     the builder generic over `T` but still returning `PanopticTokenId` on the no-payload path;
     every pre-Phase-2 caller concrete on `VolOrder(calldata)`; and at the pin, `@type_index` plus
     **`std::slice::Slice(R, T, arity)`** — the typed region-generic view Phase 3 should build over
     the four ratios rather than reinvent.
   - Do NOT touch `.planning/config.json`.
```bash
git diff --stat .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md     # all three must show changes
grep -c '^milestone: v1.0$' .planning/STATE.md                                         # 1
grep -c '\[x\] \*\*VORD-0[123]\*\*' .planning/REQUIREMENTS.md                          # 3
grep -c '| VORD-0[123] | Phase 2 — VolOrder(T) Minimal Instantiation | Complete |' .planning/REQUIREMENTS.md   # 3
grep -c '\[x\] 02-0[1-5]-PLAN.md' .planning/ROADMAP.md                                 # 5
grep -c '| 2. VolOrder(T) Minimal Instantiation | 5/5 | Complete |' .planning/ROADMAP.md   # 1
```

3. Commit by name and push to `develop` (a `.planning/`-only push; STATE.md records that such
   pushes land on `enforce_admins: false` — say so again in the SUMMARY rather than pretending
   the gate ran on them):
```bash
git add .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md .planning/phases/02-volorder-t-minimal-instantiation/02-05-SUMMARY.md
git commit -m "docs(02-05): Phase 2 complete — VolOrder(T) minimal instantiation merged as <MERGE_SHA>; VORD-01..03 ticked"
git push origin develop
```
  </action>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/vol-markets &amp;&amp; git branch --show-current | grep -qx develop &amp;&amp; [ "$(git branch --list feat/volorder-t-minimal | wc -l)" -eq 0 ] &amp;&amp; [ "$(git ls-remote --heads origin feat/volorder-t-minimal | wc -l)" -eq 0 ] &amp;&amp; [ "$(grep -c '^milestone: v1.0$' .planning/STATE.md)" -eq 1 ] &amp;&amp; [ "$(grep -c '\[x\] \*\*VORD-0[123]\*\*' .planning/REQUIREMENTS.md)" -eq 3 ] &amp;&amp; [ "$(grep -c '\[x\] 02-0[1-5]-PLAN.md' .planning/ROADMAP.md)" -eq 5 ] &amp;&amp; git diff --quiet origin/develop -- .planning/STATE.md .planning/ROADMAP.md .planning/REQUIREMENTS.md &amp;&amp; echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `git branch --show-current` = `develop`; `git log origin/develop..HEAD` empty after the push.
    - `git branch --list feat/volorder-t-minimal | wc -l` = 0 and `git ls-remote --heads origin feat/volorder-t-minimal | wc -l` = 0; the SUMMARY states `git branch -d` succeeded (and `-D` was never used).
    - `grep -c '^milestone: v1.0$' .planning/STATE.md` = 1 (before and after; recorded in the SUMMARY).
    - `grep -c '\[x\] \*\*VORD-0[123]\*\*' .planning/REQUIREMENTS.md` = 3; `grep -c '| VORD-0[123] | Phase 2 — VolOrder(T) Minimal Instantiation | Complete |' .planning/REQUIREMENTS.md` = 3.
    - `grep -c '\[x\] \*\*Phase 2: VolOrder(T) Minimal Instantiation\*\*' .planning/ROADMAP.md` = 1; `grep -c '\[x\] 02-0[1-5]-PLAN.md' .planning/ROADMAP.md` = 5; `grep -c 'VERIFIED ON origin/develop' .planning/ROADMAP.md` ≥ 5 (one per Phase 2 criterion); Progress row shows `5/5 | Complete`.
    - `grep -c 'status: phase-complete' .planning/STATE.md` = 1; `grep -c '^completed_phases: 3' .planning/STATE.md` = 1; STATE.md §Decisions contains at least one line beginning `- [Phase 02]` naming `Extra` and one naming `comptime T`.
    - `git show origin/develop --stat --format= | grep -c '|'` = 4 (STATE, ROADMAP, REQUIREMENTS, 02-05-SUMMARY) — nothing else rode the close-out commit; `.planning/config.json` still dirty and unstaged.
  </acceptance_criteria>
  <done>The branch is gone locally and on origin; the planning files say Phase 2 is complete and
  why, by hand, with the milestone header intact.</done>
</task>

</tasks>

<verification>
- Assessment FINAL on `develop` with zero unfilled cells and an empty, evidenced elimination bucket.
- PR merged with a merge commit after a live protection reading; five criteria verified with
  `git show origin/develop:` / `git diff b090b2e origin/develop`.
- Branch retired with `-d`; issue closed naming the merge SHA.
- STATE/ROADMAP/REQUIREMENTS hand-edited; `milestone: v1.0` count 1.
</verification>

<success_criteria>
- Phase 2's five ROADMAP criteria are true of `develop` and each is quoted from a command or a run.
- VORD-01, VORD-02, VORD-03 are Complete; Phase 3 inherits `VolOrder(T)` with `extra: Option(Extra(T))`,
  the `Extra(T)` tagged descriptor in `src/types/Extra.plk`, a builder still returning `PanopticTokenId`,
  and a pin that has `@type_index` and `std::slice::Slice`.
</success_criteria>

<output>
After completion, create
`.planning/phases/02-volorder-t-minimal-instantiation/02-05-SUMMARY.md`
recording: the live protection JSON, PR number and merge SHA, the C1..C5 command outputs with
their backing run ids, the `--skip` decision token and what was done with it, the branch teardown
commands' outputs, and the exact STATE/ROADMAP/REQUIREMENTS edits (as `git diff --stat`).
</output>
