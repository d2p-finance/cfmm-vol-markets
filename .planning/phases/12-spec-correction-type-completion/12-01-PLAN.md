---
phase: 12-spec-correction-type-completion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spec/protocol/entities/types/risk.md
  - src/types/risk/RiskDiscount.plk        # DELETED (refuted-concept .plk embodiment)
  - src/lib/risk/RiskMeasureLib.plk         # DELETED (imports the above; refuted-concept stub)
  - src/types/exposure/VegaExposure.plk
  - spec/protocol/entities/types/exposure.md
autonomous: true
requirements: [RISK-01, RISK-02]

must_haves:
  truths:
    - "A reader of risk.md sees `p_risk = oracle/(1−h)` and the per-operation integer realization (ceil p_risk, floor shares), not the refuted price-over-haircut formula"
    - "The refuted expression `collateralAmount* (self.price/haircut)` appears nowhere under spec/ or src/ (scoped grep is empty)"
    - "The refuted-concept .plk files (RiskDiscount.plk, RiskMeasureLib.plk) — empty-bodied, grep-invisible, unimported — are DELETED, so the concept exists nowhere in src/"
    - "risk.md records that `issuance_haircut_equiv` holds over ℝ ONLY, with the verified integer counterexample deposit=10, oracleX96=10·2^92, hX96=3·2^92 → composed 12 vs direct 13, and only composed ≤ direct transfers"
    - "VegaExposure.plk is a two-live-field record (`exposure`, `priceVolX96`) plus `RiskPriceX96`/`Haircut` newtypes, with the stub's `collateralUnits`/`priceVol` mis-naming fixed"
    - "exposure.md records that `collateralToken`/`underlyingToken`/`riskOracleId` return with the oracle-wiring milestone, and that in v1 `priceVolX96` carries `p_risk` (not `p_vol(σ̄)`)"
  artifacts:
    - path: "spec/protocol/entities/types/risk.md"
      provides: "H1 issuance spec: oracle/(1−h), integer realization, quote convention, share units, ℝ-only caveat + counterexample, inexact-division anchor"
      contains: "p_risk = oracle / (1 − h)"
    - path: "src/types/exposure/VegaExposure.plk"
      provides: "Two-live-field VegaExposure record + RiskPriceX96/Haircut newtypes"
      contains: "exposure"
    - path: "spec/protocol/entities/types/exposure.md"
      provides: "Deferred-fields note + v1 priceVolX96-carries-p_risk tension"
      contains: "oracle-wiring"
  key_links:
    - from: "spec/protocol/entities/types/risk.md"
      to: "RiskDesign.lean"
      via: "lemma citation by name and file"
      pattern: "haircutRiskPrice_ge_oracle|issuance_haircut_equiv"
    - from: "src/types/exposure/VegaExposure.plk"
      to: "spec/protocol/entities/types/exposure.md"
      via: "field names match the spec record (exposure, priceVolX96)"
      pattern: "priceVolX96"
---

<objective>
Phase 12 corrects the Lean-refuted issuance spec and completes the exposure type — a
DOC + TYPE-DECLARATION-ONLY phase that BLOCKS Phases 13–15. No arithmetic, no lib, no
module, no test may be written against the stale spec, so this lands first.

Three deliverable groups:
1. `spec/protocol/entities/types/risk.md` rewritten: the refuted price-over-haircut formula removed;
   `p_risk = oracle/(1−h)` stated with lemma citations; the per-operation integer realization,
   quote convention, share units, and ℝ-only caveat (with the verified counterexample) pinned.
2. The refuted concept's live `.plk` embodiment DELETED: `src/types/risk/RiskDiscount.plk` and
   `src/lib/risk/RiskMeasureLib.plk` (empty-bodied stubs that the grep gate misses and that
   nothing imports — "unmaintained code wearing a checkout", the same class this project deletes
   rather than quarantines: `src/exp/`, `src/ldf/`, `ReferenceMarket`).
3. `src/types/exposure/VegaExposure.plk` completed as the two-live-field record + `RiskPriceX96`/
   `Haircut` newtypes; `spec/protocol/entities/types/exposure.md` updated with the deferred-fields note and
   the v1 meaning of `priceVolX96`.

Purpose: kill the refuted economics — in prose AND in code — before any `.plk` arithmetic exists;
give Phase 13 a typed, verbatim-implementable integer spec and a rounding-sensitive anchor.

Output: five file changes (three edits, two deletions); a SCOPED grep gate that is empty; a
LABELLED compile precondition.

**No mutation gate. No CALLED test. "It compiles" is NOT a success claim** (roadmap Phase 12 note):
the type is proven only by being imported and exercised in Phase 13. The compile check in Task 4 is
a labelled PRECONDITION, never acceptance.
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-spec-correction-type-completion/12-CONTEXT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

# Design authority (machine-checked Lean — the content being transcribed):
#   ../cfmm-wt/lean4-spec/lean/vol_markets/RiskDesign.lean
#     haircutRiskPrice (~113), haircutRiskPrice_ge_oracle (~119), issuance_haircut_equiv (~123)
#   ../cfmm-wt/lean4-spec/protocol/model/vol_markets/RISK_ALTERNATIVES.md  (H1 recommendation §3, pipeline §4)

<conventions>
<!-- Verified against the codebase. Executor uses these directly — no exploration needed. -->

Plank type files have NO `init` block — they are pure libraries pulled in transitively
(`plank build` of one directly fails with `error: missing init block`; that is expected, see Task 4).

Unicode vs ASCII: spec MATH PROSE uses the Unicode minus `−` (U+2212) for readability (e.g.
`2^96 − hX96`). Actual `.plk` CODE must use ASCII hyphen-minus `-` (and `-%` for wrapping). Do not
copy the Unicode `−` into any `.plk` file.

Newtype pattern (from src/types/Numerics.plk):
```plank
const Q64_96 = struct {
      val: u256
};
```

Bounded-field pattern (from src/types/market_state_measurements/Timepoint.plk): fields are
declared `u256`; the bit-width bound lives in an inline comment, e.g.
```plank
const Timepoint = struct {
      timestamp: u256,                    // u32
      realizedVolatility :TickVolatility, // u88
      ...
};
```
VegaExposure needs NO packing (its two live fields alone are 128+160 = 288 bits > 256; it is a
plain record). Mirror Timepoint's documentation STYLE (bound-in-comment), not its packing helpers.

Current stub to replace — src/types/exposure/VegaExposure.plk:
```plank
const VegaExposure = struct{
      collateralUnits:u256,
      priceVol: u256
};
```

Zero-denominator revert (for Task 1's MAJOR-2 attribution) — lib/plankified-univ3/plank/lib/math/full_math.plk:13–24:
`mulDiv` (and therefore `mulDivRoundingUp`) calls `revert_empty()` when `denominator == 0`.
</conventions>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite risk.md to the machine-checked H1 issuance spec (RISK-01)</name>
  <files>spec/protocol/entities/types/risk.md</files>
  <read_first>
    - spec/protocol/entities/types/risk.md (current 24-line stub: refuted `price/haircut` at line 12, obsolete 64-bit-fraction haircut convention at line 10)
    - .planning/phases/12-spec-correction-type-completion/12-CONTEXT.md (decisions — quote convention USER-LOCKED)
    - ../cfmm-wt/lean4-spec/lean/vol_markets/RiskDesign.lean (confirm lemma names/line numbers AND the `haircutRiskPrice_ge_oracle` hypotheses at ~119–120: it assumes `0 ≤ oracle` and `0 ≤ h < 1`)
    - lib/plankified-univ3/plank/lib/math/full_math.plk lines ~13–24 (the zero-denominator `revert_empty()` that mulDivRoundingUp inherits — needed for the MAJOR-2 subtraction attribution below)
  </read_first>
  <action>
Replace the ENTIRE contents of `spec/protocol/entities/types/risk.md` with the markdown below, verbatim.
Delete the refuted `RiskDiscount`/`RiskMeasure` sketches (they carry the refuted price-over-haircut
formula and the obsolete 64-bit-fraction haircut convention — they mislead Phase 13 and are removed,
per CONTEXT discretion "delete what misleads").

CRITICAL — two forbidden literal substrings, both enforced by acceptance criteria:
  (1) The grep gate is `git grep -nF 'price/haircut' -- spec/ src/` and MUST be empty. Never write
      the literal `price/haircut` or `self.price/haircut`; describe the refuted formula as "the ratio
      of price to haircut".
  (2) Do NOT write the literal token `Q0.64` anywhere — a spec that names the refuted convention by
      its literal token reintroduces exactly the needle the grep gate exists to prevent. Refer to it
      as "the draft's obsolete 64-bit-fraction haircut convention" instead.

Verbatim content to write:

--- BEGIN risk.md ---
# Risk Price and H1 Issuance

## 1. Issuance risk price

The issuance risk price is the haircut-adjusted oracle price:

    p_risk = oracle / (1 − h)

Design authority — machine-checked Lean (`../cfmm-wt/lean4-spec/lean/vol_markets/RiskDesign.lean`, no `sorry`):

- `haircutRiskPrice` (RiskDesign.lean ~line 113) — the definition above.
- `haircutRiskPrice_ge_oracle` (RiskDesign.lean ~line 119) — `p_risk ≥ oracle` for all `0 ≤ oracle` and `0 ≤ h < 1`.
- `issuance_haircut_equiv` (RiskDesign.lean ~line 123) — the composed and direct issuance paths are equal OVER ℝ (see §4).

The earlier draft `RiskDiscount` formula — collateral scaled by the ratio of price to haircut — is
REFUTED by the Lean (singular at `h = 0`, inverted monotonicity: value grew as the haircut shrank)
and is removed, in prose AND in its `.plk` embodiment (the deleted `RiskDiscount.plk` /
`RiskMeasureLib.plk`). It must never be reintroduced in `spec/` or `src/`.

## 2. Quote convention

- `p_risk` is **Q64.96**, a **LINEAR** price (NOT a `sqrtPriceX96`), quoted as **collateral units per 1 vega-exposure unit**, in **raw smallest units on both sides** (collateral base units per vega-exposure base unit).
- `oracleX96` (the pre-haircut risk price) uses the **same** convention.
- Direction of conservatism: higher `p_risk` → fewer shares per deposit. The haircut RAISES `p_risk`
  (`oracle/(1−h) ≥ oracle`, `haircutRiskPrice_ge_oracle`), so it can only REDUCE issuance.

## 3. Per-operation integer realization

Fixed-point conventions: price **Q64.96** (`X96`); haircut **Q0.96** (`hX96 < 2^96`). (This REPLACES
the draft's obsolete 64-bit-fraction haircut convention.)

Note on notation: the `−` below is the Unicode minus for readability; `.plk` CODE must use ASCII `-`
(the checked subtraction operator — never the wrapping `-%`).

- Risk price — rounds UP (it is a divisor; larger is conservative), computed with Plank's default
  CHECKED subtraction `2^96 - hX96`:

      p_risk = mulDivRoundingUp(oracleX96, 2^96, 2^96 − hX96)

  Revert envelope: the checked `-` reverts (never wraps) for `hX96 > 2^96`. At the boundary
  `hX96 = 2^96` the subtraction yields `0` WITHOUT reverting — the revert then comes for free one
  operation later from `mulDivRoundingUp`'s zero-denominator check (`full_math.plk:13–24`,
  `revert_empty()`, inherited from `mulDiv`). So the `hX96 ≥ 2^96` inputs are fully excluded by the
  composition of the two operations; no separate explicit `h < 1` guard is required for the revert
  envelope. (Phase 13's `haircut_risk_price` MAY still add an explicit `hX96 < 2^96` guard for a
  clearer error, but a mutant that deletes it is EQUIVALENT here, not a kill — the zero-denominator
  revert masks it.)

- Shares, composed path — rounds DOWN / FLOOR:

      shares = mulDiv(deposit, 2^96, pRiskX96)

- Shares, direct path — rounds DOWN / FLOOR:

      shares_direct = mulDiv(deposit, 2^96 − hX96, oracleX96)

### Share units

Shares inherit the collateral token's **native decimals**: in `shares = mulDiv(deposit, 2^96, pRiskX96)`
the `2^96` in the numerator and the Q96 scale of `pRiskX96` in the denominator cancel, so `shares`
carries the same decimals (raw smallest units) as `deposit`. There is no separate "share decimals"
quantity.

## 4. ℝ-only status of `issuance_haircut_equiv`

`issuance_haircut_equiv` is proven over ℝ ONLY. In integers the composed path (§3, ceil then floor)
and the direct path round at different points, so exact cross-path equality is FALSE. Only the
one-sided transfer holds:

    composed ≤ direct        (verified: 0 violations in a 200k-sample random sweep)

### Verified counterexample — also the Phase 13 rounding anchor

Inputs: `deposit = 10`, `oracleX96 = 10·2^92`, `hX96 = 3·2^92`, so `2^96 − hX96 = 13·2^92`.

- `oracleX96 · 2^96 = 160·2^184`; `160 mod 13 = 4`, so the division is INEXACT and `p_risk` rounds up:
  `pRiskX96 = mulDivRoundingUp(10·2^92, 2^96, 13·2^92) = 60944740395587951995033807951`.
- Composed: `shares = mulDiv(10, 2^96, pRiskX96) = 12`.
- Direct:  `shares_direct = mulDiv(10, 13·2^92, 10·2^92) = 13`.

So composed = 12, direct = 13 (gap 1). The gap grows with `deposit / 2^96`: at
`deposit = 2^100, oracleX96 = 10·2^92, hX96 = 3·2^92` the gap is exactly 6 (verified). This
counterexample point is INEXACT at BOTH hops — the `p_risk` ceil and the `shares` floor are each
load-bearing here — which is why Phase 13's non-fuzz unit anchor must sit here: a floor-instead-of-
ceil `p_risk` mutant yields `pRiskX96 − 1` and flips `shares` 12 → 13, AND a ceil-instead-of-floor
`shares` mutant also yields 13. (An anchor whose divisions are exact would kill neither mutant; an
`h = 0` anchor still exercises the `shares` division, so it can catch a shares-rounding flip only if
that division happens to be inexact — the prescribed both-hops-inexact anchor is what pins BOTH
rounding sites at once.)

## 5. Deferred (future sections — NOT specified here)

Distance pipeline D2, risk-price composition P0/P2, stateful `setHaircut`, oracle wiring to
`RealizedVolatilityMod`, and `p_vol(σ̄)` from `pos_spec` are out of scope for v1 — see
`REQUIREMENTS.md` "Out of Scope". They may be referenced as future work but must not be specified here.
--- END risk.md ---

Before finishing, confirm the lemma line numbers and the `haircutRiskPrice_ge_oracle` hypotheses
against RiskDesign.lean (you read it in read_first). If a line number differs materially, use the
actual line; keep the lemma NAMES exactly as written (they are the Phase 13 test oracles).
  </action>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/plank && git grep -nF 'price/haircut' -- spec/ src/; test $? -eq 1 && ! grep -qF 'Q0.64' spec/protocol/entities/types/risk.md</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -nF 'price/haircut' -- spec/ src/` exits 1 (NO matches) — the SCOPED gate; never run it unscoped.
    - `git grep -nF 'collateralAmount* (self.price/haircut)' -- spec/ src/` exits 1 (the refuted line is gone).
    - `! grep -qF 'Q0.64' spec/protocol/entities/types/risk.md` succeeds (the literal token is absent; the convention is named in words).
    - `grep -qF 'p_risk = oracle / (1 − h)' spec/protocol/entities/types/risk.md` succeeds (H1 formula present).
    - `grep -qF 'mulDivRoundingUp(oracleX96, 2^96, 2^96 − hX96)' spec/protocol/entities/types/risk.md` succeeds.
    - `grep -qF 'mulDiv(deposit, 2^96, pRiskX96)' spec/protocol/entities/types/risk.md` succeeds.
    - `grep -qF 'mulDiv(deposit, 2^96 − hX96, oracleX96)' spec/protocol/entities/types/risk.md` succeeds (direct path).
    - `grep -qF 'haircutRiskPrice_ge_oracle' spec/protocol/entities/types/risk.md` AND `grep -qF 'issuance_haircut_equiv' spec/protocol/entities/types/risk.md` both succeed (lemmas cited by name).
    - `grep -qF '0 ≤ oracle' spec/protocol/entities/types/risk.md` succeeds (restored ge_oracle hypothesis).
    - `grep -qF 'composed = 12, direct = 13' spec/protocol/entities/types/risk.md` succeeds (verified counterexample).
    - `grep -qF 'deposit = 2^100' spec/protocol/entities/types/risk.md` succeeds (gap-of-6 reproducing input present).
    - `grep -qF 'collateral units per 1 vega-exposure unit' spec/protocol/entities/types/risk.md` AND `grep -qF 'raw smallest units on both sides' spec/protocol/entities/types/risk.md` both succeed (quote convention + units).
    - `grep -qF 'native decimals' spec/protocol/entities/types/risk.md` succeeds (share units stated).
    - `grep -qF 'zero-denominator' spec/protocol/entities/types/risk.md` succeeds (correct revert attribution: revert at hX96=2^96 comes from mulDiv, not the subtraction).
  </acceptance_criteria>
  <done>risk.md states `p_risk = oracle/(1−h)` with correctly-hypothesized lemma citations, the four integer operations with the correct checked-subtraction/zero-denominator revert attribution, the quote convention (raw smallest units), share units, and the ℝ-only caveat with the verified 12-vs-13 counterexample plus the gap-of-6 input; the refuted price-over-haircut expression and the literal `Q0.64` token are gone; the scoped grep gate is empty.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the refuted RiskDiscount/RiskMeasureLib .plk embodiment (RISK-01)</name>
  <files>src/types/risk/RiskDiscount.plk, src/lib/risk/RiskMeasureLib.plk</files>
  <read_first>
    - src/types/risk/RiskDiscount.plk (struct RiskDiscount{price,factor} + stubs factor_from_haircut_and_price / discounted_vega_amt_from_collateral — the refuted concept, empty bodies)
    - src/lib/risk/RiskMeasureLib.plk (imports RiskDiscount; malformed stub `risk_measure` — depends on the file being deleted)
  </read_first>
  <action>
These two files are the live `.plk` embodiment of the refuted risk-discount concept that Task 1
removes from `risk.md`. They have EMPTY bodies (so the `price/haircut` grep gate does not catch them)
and NOTHING imports them across the whole tree (verified at plan time: the only references to
`RiskDiscount` / `RiskMeasureLib` under `src/`+`test/` are inside these two files themselves;
`RiskMeasureLib` imports `RiskDiscount`, and both are otherwise orphaned). RISK-01 requires the
refuted concept to appear nowhere under `spec/` or `src/`, so quarantining is not enough — DELETE
them, following project precedent (`src/exp/`, `src/ldf/`, `ReferenceMarket` were deleted, not
quarantined; git history is the recovery path).

BEFORE deleting, re-verify zero live importers yourself (do not trust this plan blindly):
```
git grep -n 'RiskDiscount\|RiskMeasureLib' -- src/ test/ | grep -vE 'src/types/risk/RiskDiscount.plk|src/lib/risk/RiskMeasureLib.plk'
```
This MUST print nothing (exit 1). If it prints a live importer, STOP and report — do not delete.

Then delete both files (use `git rm` so the deletion is staged):
```
git rm src/types/risk/RiskDiscount.plk src/lib/risk/RiskMeasureLib.plk
```
Deleting both empties `src/types/risk/` and `src/lib/risk/`. Record the deletion and its rationale in
the SUMMARY so Phase 13 does not rediscover and rebuild the refuted concept.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/plank && test ! -f src/types/risk/RiskDiscount.plk && test ! -f src/lib/risk/RiskMeasureLib.plk && ! git grep -qn 'RiskDiscount\|RiskMeasureLib' -- src/ test/</automated>
  </verify>
  <acceptance_criteria>
    - `test ! -f src/types/risk/RiskDiscount.plk` succeeds (file gone).
    - `test ! -f src/lib/risk/RiskMeasureLib.plk` succeeds (file gone).
    - `git grep -n 'RiskDiscount' -- src/ test/` exits 1 (no references anywhere in code).
    - `git grep -n 'RiskMeasureLib' -- src/ test/` exits 1 (no references anywhere in code).
    - SUMMARY records the deletion of both files and the rationale (refuted concept, empty bodies, zero importers, git-history recovery).
  </acceptance_criteria>
  <done>Both refuted-concept .plk files are deleted with zero dangling importers; the concept exists nowhere under src/; the deletion + rationale are recorded in the SUMMARY.</done>
</task>

<task type="auto">
  <name>Task 3: Complete VegaExposure.plk record + risk newtypes, and sync exposure.md (RISK-02)</name>
  <files>src/types/exposure/VegaExposure.plk, spec/protocol/entities/types/exposure.md</files>
  <read_first>
    - src/types/exposure/VegaExposure.plk (current 4-line stub: `collateralUnits`/`priceVol` mis-named)
    - src/types/Numerics.plk (newtype pattern: `const Q64_96 = struct { val: u256 };`)
    - src/types/market_state_measurements/Timepoint.plk (bounded-field style: `u256` field + `// u32`-style bound comment; NO init block)
    - spec/protocol/entities/types/exposure.md (current: N_v = ΔM/p_vol derivation + 5-field solidity sketch)
    - .planning/phases/12-spec-correction-type-completion/12-CONTEXT.md (VegaExposure decisions — USER-LOCKED at milestone creation)
  </read_first>
  <action>
**3a — Replace the entire contents of `src/types/exposure/VegaExposure.plk` with** (ASCII only — no
Unicode `−`):

--- BEGIN VegaExposure.plk ---
// VegaExposure: internal accounting record for the deposit-only vault (v1, H1 issuance).
//
// Live fields only. No packing: 128 + 160 = 288 bits > 256, so this is a PLAIN record, not a
// bit-packed slot (contrast Timepoint.plk, which packs 241 bits into one u256). Plank struct
// fields are u256; the u128 / u160 widths below are DOCUMENTED semantic bounds, not enforced by
// the type -- the same convention Timepoint.plk uses for its sub-word fields.
const VegaExposure = struct {
      exposure: u256,     // u128 -- issued vega-exposure shares N_v
      priceVolX96: u256   // u160 -- Q64.96; carries the exogenous p_risk in v1 (see exposure.md v1 note)
};

// Risk Q-type newtypes, so Phase 13 lib signatures are typed rather than bare u256.
// Same newtype shape as Numerics.plk's Q64_96 / Q128_128.
const RiskPriceX96 = struct {
      val: u256   // Q64.96 -- the haircut-adjusted risk price p_risk = oracle/(1-h)
};

const Haircut = struct {
      val: u256   // Q0.96 -- hX96 < 2^96
};
--- END VegaExposure.plk ---

This is a pure library file: NO `init` block, NO `run` block (it is pulled in transitively, exactly
like Timepoint.plk and TickVolatility.plk). Keeping the newtypes here (rather than a sibling file)
holds the phase to the named files — this is the CONTEXT-granted discretion choice.

**3b — Update `spec/protocol/entities/types/exposure.md`:** KEEP the existing `N_v = ΔM/p_vol(σ̄)` derivation
(§2) — it is the v2+ target. Replace the §3 "Type structure" solidity sketch and append a v1 note so
the section reads:

--- BEGIN exposure.md §3 replacement ---
## 3. Type structure (v1 — live fields only)

```plank
const VegaExposure = struct {
      exposure: u256,     // u128 -- issued vega-exposure shares N_v
      priceVolX96: u256   // u160 -- Q64.96; carries the exogenous p_risk in v1
};
```

### v1 note — `priceVolX96` carries `p_risk`, not `p_vol(σ̄)`

In v1 there is no volatility oracle, so `priceVolX96` carries the EXOGENOUS, settable `p_risk`
(the haircut-adjusted risk price of `risk.md`), NOT the `p_vol(σ̄)` of §2. This tension is stated,
not silently renamed: the field keeps its `priceVol` lineage in the name while its v1 meaning is
`p_risk`. The §2 `N_v = ΔM/p_vol(σ̄)` derivation is the v2+ target that arrives with oracle wiring.

### Deferred fields

`collateralToken`, `underlyingToken`, and `riskOracleId` are intentionally ABSENT from the v1
`.plk` record. They return with the oracle-wiring milestone (see `REQUIREMENTS.md` "Out of Scope":
oracle wiring to `RealizedVolatilityMod`). The stub's `collateralUnits`/`priceVol` field names are
corrected to the spec names `exposure`/`priceVolX96`.
--- END exposure.md §3 replacement ---
  </action>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/plank && grep -qF 'exposure: u256' src/types/exposure/VegaExposure.plk && grep -qF 'priceVolX96: u256' src/types/exposure/VegaExposure.plk && grep -qF 'const RiskPriceX96' src/types/exposure/VegaExposure.plk && grep -qF 'const Haircut' src/types/exposure/VegaExposure.plk && ! grep -qF 'collateralUnits' src/types/exposure/VegaExposure.plk && ! grep -qE '^[[:space:]]*init' src/types/exposure/VegaExposure.plk && ! grep -qF '−' src/types/exposure/VegaExposure.plk && grep -qF 'oracle-wiring' spec/protocol/entities/types/exposure.md</automated>
  </verify>
  <acceptance_criteria>
    - VegaExposure.plk contains `exposure: u256` and `priceVolX96: u256` and does NOT contain `collateralUnits` or `priceVol:` (mis-naming fixed).
    - VegaExposure.plk contains `const RiskPriceX96` and `const Haircut` newtype declarations.
    - VegaExposure.plk contains NO line matching `^[[:space:]]*init` and no `run {` (pure library file).
    - VegaExposure.plk contains NO Unicode minus `−` (ASCII-only code): `! grep -qF '−' src/types/exposure/VegaExposure.plk` succeeds.
    - `grep -qF 'u128' src/types/exposure/VegaExposure.plk` and `grep -qF 'u160' src/types/exposure/VegaExposure.plk` both succeed (bounds documented in comments).
    - exposure.md contains `oracle-wiring` and lists `collateralToken`, `underlyingToken`, `riskOracleId` as deferred.
    - exposure.md states `priceVolX96` carries `p_risk` in v1 (`grep -qF 'carries the EXOGENOUS'` succeeds).
    - exposure.md still contains the `N_v` derivation (`grep -qF 'N_v'` succeeds).
  </acceptance_criteria>
  <done>VegaExposure.plk is the two-live-field record with `RiskPriceX96`/`Haircut` newtypes, no init block, ASCII-only; exposure.md keeps the N_v derivation, records the deferred fields, and states the v1 `priceVolX96 = p_risk` tension without silent renaming.</done>
</task>

<task type="auto">
  <name>Task 4: Final scoped-grep gate + labelled compile PRECONDITION</name>
  <files>(no files modified — verification only)</files>
  <read_first>
    - Makefile (compile-plank target at ~line 153; note lines ~88–92: type files have NO init block and are skipped by entrypoint discovery)
    - .planning/phases/12-spec-correction-type-completion/12-CONTEXT.md (the grep-scope and no-mutation-gate rules)
  </read_first>
  <action>
Run the two gates below and record their results in the SUMMARY. NEITHER is acceptance in the
CALLED-test sense; the grep gate IS a hard acceptance criterion (roadmap SC-1), and the compile
check is a LABELLED PRECONDITION only (the type is proven by import/exercise in Phase 13, never by
"it compiles" — roadmap Phase 12 note).

**Gate A — scoped grep (hard acceptance):**
```
git grep -nF 'price/haircut' -- spec/ src/
```
MUST print nothing and exit 1. NEVER run it unscoped: `.planning/` deliberately retains the string
as review history, so an unscoped grep contains its own needle and is unsatisfiable.

**Gate B — labelled compile precondition (NOT acceptance), two parts:**

B1. Whole-tree regression: `make compile-plank` MUST stay green. Baseline at plan time was
`10 ok, 0 failed, 1 skipped` (the 1 skip is `VegaAccountMod`, the Phase-14 target). This phase
DELETES two unimported files and edits only a doc + a non-entrypoint type, so the count must remain
`10 ok, 0 failed, 1 skipped`. Run `make compile-plank` and confirm `0 failed`.

B2. VegaExposure declarations parse/typecheck: VegaExposure.plk is a pure library (no init block), so
it is invisible to `make compile-plank` discovery and cannot be built directly as an entrypoint
(`plank build` of it exits with `error: missing init block` — that is EXPECTED, not a failure). This
proves only that the type + newtype DECLARATIONS parse and typecheck up to the entrypoint
requirement. Run:
```
plank build src/types/exposure/VegaExposure.plk \
  --dep v3=lib/plankified-univ3/plank/lib/ --dep std=lib/plank-monorepo/std/ \
  --dep pos_spec=src/types/pos_spec --dep lib=src/lib --dep types=src/types \
  --dep interfaces=src/interfaces --backend sona 2>&1 | grep '^error:'
```
There MUST be EXACTLY ONE `error:` line and it MUST be `error: missing init block`. Any OTHER
`error:` (a parse/type error in the record or newtypes) is a real failure to fix. Record verbatim in
the SUMMARY that this is a PRECONDITION, not acceptance.

CONDITIONAL note (do NOT pre-file a blocker): at plan time the tree was clean and `make compile-plank`
was green (`10 ok, 0 failed, 1 skipped`). If — and only if — you OBSERVE `make compile-plank` red,
inspect the failing entrypoint; if the failure is in a file this plan did NOT touch (e.g. an
unrelated dirty edit under `src/interfaces/` or `src/modules/`), flag it in the SUMMARY as
out-of-scope for the owning track and do NOT attempt to fix it in this phase. Do not report a blocker
you did not actually observe.
  </action>
  <verify>
    <automated>cd /home/jmsbpp/cfmms-playground/cfmm-wt/plank && git grep -nF 'price/haircut' -- spec/ src/; test $? -eq 1 && err=$(plank build src/types/exposure/VegaExposure.plk --dep v3=lib/plankified-univ3/plank/lib/ --dep std=lib/plank-monorepo/std/ --dep pos_spec=src/types/pos_spec --dep lib=src/lib --dep types=src/types --dep interfaces=src/interfaces --backend sona 2>&1 | grep -c '^error:'); test "$err" -eq 1 && plank build src/types/exposure/VegaExposure.plk --dep v3=lib/plankified-univ3/plank/lib/ --dep std=lib/plank-monorepo/std/ --dep pos_spec=src/types/pos_spec --dep lib=src/lib --dep types=src/types --dep interfaces=src/interfaces --backend sona 2>&1 | grep '^error:' | grep -qxF 'error: missing init block'</automated>
  </verify>
  <acceptance_criteria>
    - `git grep -nF 'price/haircut' -- spec/ src/` exits 1 with no output (hard acceptance, roadmap SC-1).
    - `make compile-plank` reports `0 failed` (regression gate; expected `10 ok, 0 failed, 1 skipped`).
    - The direct `plank build` of VegaExposure.plk emits EXACTLY one `error:` line (`grep -c '^error:'` == 1) and it is `error: missing init block` (declarations parse/typecheck; no parse/type error precedes it).
    - SUMMARY explicitly labels Gate B a PRECONDITION, not acceptance, and notes Phase 12 has no mutation gate / no CALLED test.
  </acceptance_criteria>
  <done>The scoped grep gate is empty; `make compile-plank` stays green (0 failed); the VegaExposure.plk direct build emits exactly one `error:` line, `missing init block` (proving the declarations parse); all results recorded in the SUMMARY with Gate B labelled a precondition.</done>
</task>

</tasks>

<verification>
Phase 12 is a DOC + type-declaration phase — verification is by grep/read and one labelled
compile precondition, NOT by any CALLED test (no mutation gate, per roadmap Phase 12 note).

Roadmap Phase 12 Success Criteria mapping:
- SC-1 (refuted formula gone from prose AND code, `oracle/(1−h)` stated, lemmas cited) → Task 1 + Task 2 acceptance_criteria + Task 4 Gate A.
- SC-2 (per-operation integer realization + quote convention + share units + ℝ-only counterexample) → Task 1 acceptance_criteria.
- SC-3 (VegaExposure two-live-field record + RiskPriceX96/Haircut newtypes, mis-naming fixed) → Task 3 acceptance_criteria.
- SC-4 (exposure.md deferred fields + v1 priceVolX96-carries-p_risk tension) → Task 3 acceptance_criteria.
</verification>

<success_criteria>
- `git grep -nF 'price/haircut' -- spec/ src/` returns nothing (exit 1).
- risk.md states `p_risk = oracle/(1−h)`, cites `haircutRiskPrice_ge_oracle` (with `0 ≤ oracle`, `0 ≤ h < 1`) and `issuance_haircut_equiv`, pins the four integer operations with the correct checked-subtraction/zero-denominator revert attribution, the quote convention (raw smallest units), share units, and the ℝ-only caveat with the verified 12-vs-13 counterexample + the gap-of-6 input.
- The literal token `Q0.64` no longer appears in risk.md.
- `src/types/risk/RiskDiscount.plk` and `src/lib/risk/RiskMeasureLib.plk` are deleted with zero dangling importers.
- VegaExposure.plk is the two-live-field record (`exposure`, `priceVolX96`) + `RiskPriceX96`/`Haircut` newtypes, no init block, ASCII-only, mis-naming fixed.
- exposure.md records the deferred fields and the v1 `priceVolX96 = p_risk` tension; the N_v derivation is retained.
- `make compile-plank` stays green (0 failed); the VegaExposure.plk direct build reaches ONLY `error: missing init block` (labelled PRECONDITION, not acceptance).
</success_criteria>

<output>
After completion, create `.planning/phases/12-spec-correction-type-completion/12-01-SUMMARY.md`.
Record: the scoped-grep result (Gate A); the deletion of RiskDiscount.plk / RiskMeasureLib.plk with
rationale; the compile-precondition result labelled as a PRECONDITION (Gate B, both parts); and an
explicit note that Phase 12 shipped NO CALLED test and that "it compiles" is not a success claim —
the type is proven only when Phase 13 imports and exercises it.
</output>
