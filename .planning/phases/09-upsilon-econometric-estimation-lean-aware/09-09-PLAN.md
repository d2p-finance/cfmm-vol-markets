---
phase: 09-upsilon-econometric-estimation-lean-aware
plan: 09
type: execute
wave: 5
depends_on: [4, 5, 6, 8]
files_modified:
  - econometrics/src/Alternatives.hs
  - econometrics/app/Main.hs
  - econometrics/package.yaml
  - econometrics/test/AlternativesSpec.hs
  - notes/structural-econometrcics/data/panel.csv
  - notes/structural-econometrcics/data/variance.csv
  - notes/structural-econometrcics/data/estimation-panel.csv
  - notes/structural-econometrcics/analysis/2026-07-19-upsilon-estimates.md
autonomous: true
requirements: [CTX-ALT]
must_haves:
  truths:
    - "The four alternative specifications run and emit comparable estimates (semiparametric, seed-linear, position-FE, collateral-channel)"
    - "The full pipeline runs on LIVE data: panel.csv + variance.csv built, primary + IV + alternatives estimated, the three tests reported"
    - "The analysis output is self-describing (data lineage) so audit-econ can trace it backward to raw data"
    - "The fitted (υ̂₀, κ̂) are plugged into exp_family_witnesses_ATMOTM's hypotheses, making κ̂>0 a literal witness of the Lean conjecture"
  artifacts:
    - path: "notes/structural-econometrcics/analysis/2026-07-19-upsilon-estimates.md"
      provides: "headline estimates, tests, alternatives, cross-walk, witness statement, full lineage"
      contains: "kappa"
    - path: "econometrics/src/Alternatives.hs"
      provides: "the four scheduled alternative specifications"
      exports: ["runAlternatives"]
    - path: "notes/structural-econometrcics/data/estimation-panel.csv"
      provides: "the final estimation panel (pi, sigma2, distance) exported for the GAMS cross-check"
  key_links:
    - from: "econometrics/app/Main.hs"
      to: "notes/structural-econometrcics/analysis/2026-07-19-upsilon-estimates.md"
      via: "estimate stage writes the self-describing analysis output"
      pattern: "kappa|upsilon"
    - from: "notes/structural-econometrcics/analysis/2026-07-19-upsilon-estimates.md"
      to: "lean/vol_markets/Upsilon.lean :: exp_family_witnesses_ATMOTM"
      via: "fitted params plugged into the bridging lemma hypotheses"
      pattern: "exp_family_witnesses_ATMOTM|witness"
---

<objective>
Run the headline estimation (CTX-ALT + the real run): implement the four locked alternative specifications, execute the full pipeline on LIVE data, and emit the self-describing analysis output — headline (beta0, upsilon0, kappa) with clustered SEs and the three tests, the four alternatives, the Lean-Haskell cross-walk, and the formal-witness statement (fitted kappa>0 plugged into `exp_family_witnesses_ATMOTM`). Also export the final estimation panel CSV for the GAMS cross-check (09-10).

Purpose: The phase's deliverable — estimates that test the null (kappa>0) and witness the machine-checked Lean conjecture, with lineage for the audit.
Output: `Alternatives.hs`, the live `panel.csv`/`variance.csv`/`estimation-panel.csv`, and `notes/structural-econometrcics/analysis/<date>-upsilon-estimates.md` (use the live `date +%Y-%m-%d` for the filename).
</objective>

<execution_context>
@/home/jmsbpp/.claude/get-shit-done/workflows/execute-plan.md
@/home/jmsbpp/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md
@notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md
@notes/structural-econometrcics/analysis/lean-haskell-crosswalk.md
@lean/vol_markets/Upsilon.lean
@econometrics/src/Model/NLS.hs
</context>

<constraints>
- The four alternatives are LOCKED (spec section 6.2): (1) semiparametric upsilon(moneyness) spline/kernel, sigma2 linear; (2) seed linear interaction pi = beta0 + upsilon(ibar)*sigma2 + gamma*(iK*sigma2); (3) position-FE / within (tokenId fixed effects); (4) collateral-channel robustness Q_M = Q_M(upsilon=0) + upsilon*sigma2. Implement all four; do NOT add others.
- The analysis output MUST be self-describing for audit-econ: record the subgraph endpoint used, the BigQuery dataset + query, the epoch boundary, the exact construction steps, and the input CSV paths — so the audit can trace lineage backward to raw data.
- Filename uses the LIVE date (`date +%Y-%m-%d`), not a hardcoded one.
- The witness statement must reference the PROVED `exp_family_witnesses_ATMOTM` (09-06) and show upsilon0>0, kappa>0 satisfy its hypotheses (hupsilon, hkappa).
- Haskell ONLY; no home paths / secrets in outputs.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alternatives.hs — the four scheduled alternative specifications</name>
  <files>econometrics/src/Alternatives.hs, econometrics/test/AlternativesSpec.hs, econometrics/package.yaml, econometrics/app/Main.hs</files>
  <read_first>
    - notes/structural-econometrcics/specs/2026-07-19-panoptic-upsilon-identification.md (section 6.2 — the four alternatives verbatim)
    - econometrics/src/Model/NLS.hs, econometrics/src/Model/Upsilon.hs (the primary estimator to reuse)
  </read_first>
  <behavior>
    - Test 1: `runAlternatives` on a synthetic panel returns four labelled estimate sets (semiparam, seed-linear, position-FE, collateral).
    - Test 2: the seed-linear spec recovers the first-order expansion coefficients on data generated near ATM (smoke check).
    - Test 3: adding tokenId fixed effects (position-FE) changes kappa-hat on data with planted strike-composition confounding (the diagnostic fires).
  </behavior>
  <action>
    Create `econometrics/src/Alternatives.hs` (module `Alternatives`). Implement `runAlternatives :: Panel -> [(Text, Estimates)]` covering the four specs:
      (1) semiparametric upsilon(moneyness) — spline or kernel vega profile with sigma2 entering linearly; report the estimated curve so the null is a shape read-off.
      (2) seed linear interaction — `pi = beta0 + upsilon(ibar)*sigma2 + gamma*(iK*sigma2) + v` (the original spec/protocol/panoptic.md tick-linearization as a reduced-form benchmark).
      (3) position-FE / within estimator — add tokenId fixed effects (demean within tokenId); a material move in kappa-hat vs the primary flags strike-composition selection.
      (4) collateral-channel robustness — re-run the demoted seed equation `Q_M = Q_M(upsilon=0) + upsilon*sigma2` on the collateral channel (Q_M from PanopticPoolAccount.collateral*Shares); compare upsilon-hat across premium vs collateral channels.
    Add `AlternativesSpec.hs` implementing the three behaviors on synthetic panels. Register the test. Wire an `alternatives` (or extend `estimate`) CLI stage. `stack test` green.
  </action>
  <verify>
    <automated>cd econometrics && stack test 2>&1 | tail -12; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `cd econometrics && stack test` exits 0 with AlternativesSpec passing
    - `grep -q 'runAlternatives' econometrics/src/Alternatives.hs`
    - all four specs named in source: `grep -qi 'semiparam' econometrics/src/Alternatives.hs` and `grep -qiE 'seed.?linear' econometrics/src/Alternatives.hs` and `grep -qiE 'fixed.?effect|position.?FE|within' econometrics/src/Alternatives.hs` and `grep -qi 'collateral' econometrics/src/Alternatives.hs`
    - `! grep -qIE '/home/[a-z0-9_-]+/|\$HOME|~/' econometrics/src/Alternatives.hs`
  </acceptance_criteria>
  <done>The four locked alternative specifications run and emit labelled, comparable estimates.</done>
</task>

<task type="auto">
  <name>Task 2: Run the full pipeline on live data and emit the self-describing analysis output + estimation-panel export</name>
  <files>econometrics/app/Main.hs, notes/structural-econometrcics/data/panel.csv, notes/structural-econometrcics/data/variance.csv, notes/structural-econometrcics/data/estimation-panel.csv, notes/structural-econometrcics/analysis/2026-07-19-upsilon-estimates.md</files>
  <read_first>
    - .planning/phases/09-upsilon-econometric-estimation-lean-aware/09-RESEARCH.md (section audit-econ Gate — the lineage/self-describing requirement)
    - notes/structural-econometrcics/data/DATA-SOURCES.md (the confirmed endpoint/dataset/pool — cite in the output)
    - notes/structural-econometrcics/analysis/lean-haskell-crosswalk.md (the cross-walk to embed)
    - lean/vol_markets/Upsilon.lean (exp_family_witnesses_ATMOTM — the proved lemma to cite for the witness)
  </read_first>
  <action>
    Run the end-to-end pipeline against LIVE data (endpoint/dataset from DATA-SOURCES.md; GRAPH_API_KEY from `.env` if required; BigQuery via the `mcp__bigquery__` tools, results serialized to CSV under `notes/structural-econometrcics/data/`):
      1. `build-panel` → live `panel.csv` (per-epoch pi_it, i_K, i_t for the deepest ETH/USDC market, full history, daily epochs).
      2. `variance` → live `variance.csv` (sigma2_t + instrument sigma2tilde_t).
      3. `estimate` → primary GSL-LM fit + EIV IV fit, clustered SEs, the three tests.
      4. `alternatives` → the four specs.
      5. Export `notes/structural-econometrcics/data/estimation-panel.csv` — the final estimation panel with a self-describing header: columns `tokenId, epoch, pi, sigma2, sigma2_instrument, distance` — this is the artifact handed to the GAMS cross-check (09-10).
    Write `notes/structural-econometrcics/analysis/<date>-upsilon-estimates.md` (`<date>` = `date +%Y-%m-%d`) containing:
      - Headline estimates: beta0-hat, upsilon0-hat, kappa-hat (primary + IV) with clustered SEs and 95% CIs.
      - The three tests: upsilon0>0, kappa>0 (the null-hypothesis test — state reject/fail-to-reject), kappa+ = kappa-.
      - The four alternatives table, with the position-FE and collateral-channel comparisons.
      - The Lean-Haskell-spec cross-walk table (embed or link lean-haskell-crosswalk.md).
      - The FORMAL WITNESS statement: cite the proved `Upsilon.exp_family_witnesses_ATMOTM`; show upsilon0-hat>0 and kappa-hat>0 satisfy its hypotheses (hupsilon, hkappa, hDelta) so the fitted exponential-moneyness profile is a literal witness of `ATMOTMNullHypothesis`. If kappa-hat <= 0 or upsilon0-hat <= 0, state clearly the witness is NOT established by the data and report the failed restriction.
      - DATA LINEAGE section: the subgraph endpoint, BigQuery dataset + query, epoch boundary, input CSV paths, construction steps — everything audit-econ needs to trace backward. No secrets, no home paths.
    Verify `cd lean && lake build vol_markets` is still green (the witness cites a real, proved lemma).
  </action>
  <verify>
    <automated>d=$(date +%Y-%m-%d); test -f notes/structural-econometrcics/analysis/${d}-upsilon-estimates.md && grep -qiE 'kappa|witness' notes/structural-econometrcics/analysis/${d}-upsilon-estimates.md && test -f notes/structural-econometrcics/data/estimation-panel.csv && echo OK; echo "EXIT=$?"</automated>
  </verify>
  <acceptance_criteria>
    - a file `notes/structural-econometrcics/analysis/<date>-upsilon-estimates.md` exists (date = `date +%Y-%m-%d`)
    - it contains headline estimates (`grep -qiE 'kappa'`), the three tests, the four alternatives, the cross-walk, and a WITNESS section referencing `exp_family_witnesses_ATMOTM`
    - it contains a DATA LINEAGE section: `grep -qiE 'lineage|endpoint|dataset' <file>`
    - `test -f notes/structural-econometrcics/data/estimation-panel.csv` with a header containing `distance` and `sigma2`
    - `cd lean && lake build vol_markets` exits 0 (witness cites a proved lemma)
    - `! grep -rIE '/home/[a-z0-9_-]+/|\$HOME|~/|arstl_|GRAPH_API_KEY=[^ ]' notes/structural-econometrcics/analysis/ notes/structural-econometrcics/data/estimation-panel.csv`
  </acceptance_criteria>
  <done>The live pipeline produces panel/variance/estimation-panel CSVs and a self-describing analysis output with headline estimates, the three tests, four alternatives, cross-walk, formal-witness statement, and full data lineage.</done>
</task>

</tasks>

<verification>
- `stack test` green (alternatives); live pipeline emits the analysis output + estimation-panel.csv.
- `lake build vol_markets` green; witness cites the proved lemma.
- No secrets / home paths in any output.
</verification>

<success_criteria>
Headline estimates (primary GSL-LM + IV), the three committed tests, and the four alternatives are produced on live data and written to a self-describing, audit-traceable analysis output, with the fitted kappa-hat>0 plugged into the proved bridging lemma as a formal witness of the Lean conjecture.
</success_criteria>

<output>
After completion, create `.planning/phases/09-upsilon-econometric-estimation-lean-aware/09-09-SUMMARY.md`.
</output>
